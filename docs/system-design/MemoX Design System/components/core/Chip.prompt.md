Filter / selection pill for horizontal chip rows (folders, tags, quick filters). `selected` fills with accent.

```jsx
<Chip selected>All</Chip>
<Chip dot="var(--memox-note-amber)">Work</Chip>
<Chip icon="pin">Pinned</Chip>
```

Lay chips out in a `display:flex; gap:8px; overflow-x:auto` row.
