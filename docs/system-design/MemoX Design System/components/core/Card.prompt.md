Generic surface container. Wrap any grouped content; add a colored `accent` bar to echo a note/space tint.

```jsx
<Card elevation="md" accent="var(--memox-note-amber)">…</Card>
```

Elevations: `none`, `sm`, `md`, `lg`.
