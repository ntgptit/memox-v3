# MemoX v3 Work Breakdown Structure (WBS)

Generated: 2026-06-19
Last reviewed: 2026-06-19 (rebuild reset)
Repository: `ntgptit/memox-v3`
Baseline reviewed: rebuilt from scratch on 2026-06-19; all Implemented/Partial rows reset to Specified; commit anchors cleared.

> **Prioritized backlog (2026-06-21):** for a priority-ordered view of all outstanding (not-yet-
> Implemented) work, see `docs/project-management/wbs-backlog.md`. This file remains the canonical
> full breakdown; a snapshot is archived at `docs/project-management/wbs.backup-2026-06-21.md`.

## 0. Purpose

This WBS is the delivery plan for the whole MemoX project. It decomposes every user-facing flow into function-level work packages so that each row is independently deliverable, testable, and reviewable by an AI agent in a single narrow prompt.

MemoX is a local-first flashcard learning app. Core learning must work offline; Google account and Drive backup are optional. The local database is the source of truth, and UI/business behavior must follow the business docs, wireframes, decision tables, and source contracts.

Terminology used by the `Layer` column:

- **BE** — MemoX local backend inside Flutter: domain model, use case, repository port, repository implementation, Drift table/DAO/query, transaction, provider wiring, backend/unit tests.
- **FE** — Flutter presentation: screen, widget, Riverpod presentation state, navigation, loading/empty/error states, ARB/l10n copy, widget tests.
- **Integration** — behavior that spans BE and FE (navigation wiring, cross-flow consistency, end-to-end transactions visible to the user).
- **Docs** — business docs, wireframes, decision tables, contracts.
- **Test** — quality gates that are not owned by a single feature row.

## 1. Status Legend

| Status | Meaning |
| --- | --- |
| Implemented | Confirmed by current source, tests, and/or docs evidence. |
| Partial | Some source pieces exist, but the function is not complete (e.g. UI shell without data wiring). |
| Specified | Product/spec docs exist; implementation work is still needed. |
| Target | Intended future behavior; not safe to implement without promotion/checklist. |
| Future | Future proposal; do not implement without explicit approval. |
| Blocked | Requires prerequisite schema/contract/product decision. |
| Rejected | Explicitly out of scope; do not implement. |
| Ongoing | Continuous quality gate; never "done", enforced on every task. |

## 2. Delivery Guidance

MemoX priority is a **usable flashcard/SRS app**. The WBS must drive real user flow, not architecture-only progress.

**Rebuild strategy (2026-06-19, adopted for the from-scratch rebuild).** Because the full spec set
already exists (a prior build proved the design), the rebuild uses a **phase-front-loaded** order
that front-loads the two highest-reuse layers, then assembles screens fast:

1. **Foundation first** (Phase 0) — boot, router, l10n, Drift infra so `tool/verify` runs.
2. **Core shared-widget kit next** (Phase 1) — the ~10–12 `Mx*` widgets used across most screens,
   built upfront from the design-system shots so feature screens become *assembly, not invention*.
3. **BE by entity group** (Phase 2/4/6) — CRUD/domain/data built per **FK-dependency subsystem**
   (content → study → reporting), not one screen at a time. This is allowed by the "up to 7
   tightly coupled rows sharing a subsystem and dependency chain" rule below — it is **not** a
   single "build all backend" task.
4. **Screen assembly** (Phase 3/5/7) — once the widget kit + that group's BE contracts exist,
   wire screens. Each screen depends on real providers + real `Mx*` widgets, so it lands in 1–2
   iterations instead of 4–6.

See §4.1 **Rebuild Delivery Phases** for the phase → WBS-ID map.

Standing rules (still apply within every phase):

- Do not create one huge "build all backend" task. BE batching is **per entity-group subsystem**, capped at the 7-row rule.
- Do not build isolated FE feature shells without a real data contract behind them (the shared-widget kit in Phase 1 is the deliberate exception: it is data-less by design and golden-tested in isolation).
- Build **core** shared widgets upfront (Phase 1); build **feature-specific** widgets (e.g. `MatchBoard`, `BoxStepper`) with their owning feature, not before.
- Preferred sequence inside each feature function: **BE function → BE tests → FE wiring → FE/widget tests → integration behavior → docs/decision-table parity → polish only if needed**.
- New tasks should move the app toward: create content → study content → resume study → finish session → see progress.
- One prompt may cover up to 7 tightly coupled WBS rows/subtasks when they share a subsystem and dependency chain. List every row/subtask explicitly, stop before unrelated subsystems, and do not mix BE/FE unless the prompt says to.

## 3. Source Evidence Reviewed

- Business/product docs: `docs/business/**` (system overview, navigation, folder, deck, flashcard, study, srs, study-actions, tags, bulk, search, tts, account-sync).
- Architecture/data docs: `docs/architecture/clean-architecture-contract.md`, `docs/database/schema-contract.md`, `docs/checklist/implementation-checklist.md`.
- UI kit: `docs/system-design/MemoX Design System/ui_kits/mobile/README.md` (23-screen gallery), `docs/system-design/MemoX Design System/ui_kits/mobile/AUDIT.md`.
- Source: `lib/app/router/**`, `lib/app/di/**`, `lib/domain/**`, `lib/data/**`, `lib/presentation/features/**`, `lib/presentation/shared/**`.
- Tests: `test/**` (49 test files at baseline).
- Commit history: `git log` on `main` (81 commits at baseline); commit anchors below were verified via `git log --diff-filter=A` per file.

## 4. Function-Level Delivery Plan

Row format: `WBS ID | Flow | Function | Layer | Deliverable | Status | Depends on | Evidence/Source | Commit ID | Next action`.

Commit ID rules: implemented rows carry the verified commit that landed the function (file-creation or feature commit verified from history); planned/future rows carry `TBD`. The commit of the current WBS update is never written into rows (it is unknown until commit); §10 tracks per-commit history.

### 4.1 Rebuild Delivery Phases (2026-06-19)

The **feature groups below (Group 1–10) define WHAT to build** and their dependencies. This phase
map defines the **ORDER to build them in** for the from-scratch rebuild — front-loading the two
highest-reuse layers (widget kit + per-group BE) so screens become assembly. Build phases in order;
inside a phase, follow the per-row `Depends on`.

| Phase | Goal | WBS IDs in scope | Exit criteria |
| --- | --- | --- | --- |
| **0 — Foundation** | `tool/verify` runs on code; app boots | 1.1.1, 1.1.3, 1.1.4, 1.1.5 | `node tool/verify/run.mjs` runs code chain; empty app boots; doc_guard re-baselined; `docs/_generated/` regenerated |
| **1 — Core widget kit** | ~10–12 `Mx*` widgets, golden-tested | 1.1.2, 1.2.1–1.2.5 | Every core widget has light+dark goldens; no feature screen needs a new shared surface |
| **2 — BE Group A: content** | Folder/Deck/Flashcard/Tag CRUD + import/export/bulk BE | 2.1.1–2.21.1 (BE rows), 3.1.1/3.2.1/3.4.1/3.5.1/3.7.1 (read models), **6.0.1 enabler (§4.3) then 6.x BE**, 8.7.1, 8.9.1 | Content + search + import/export use cases pass unit tests; FK chain `folders→decks→flashcards` solid |
| **3 — Library + Import screens** | Assemble content/library/import FE | 2.x FE rows, 3.1.2/3.2.2/3.4.2/3.5.2, 6.1.1/6.3.1/6.5.1 | Create→browse→import content works end-to-end |
| **4 — BE Group B: study/SRS** | Study session + SRS + mode strategies BE | **4.0.1/4.0.2 enablers first (§4.3)**, then 4.1.1–4.11.2 (BE rows), 4.5.x strategies | Enablers shipped; session create→answer→finalize→SRS transition pass table-driven tests |
| **5 — Study screens (5 modes)** | Assemble study/result FE | 4.1.2/4.1.3/4.1.4, 4.3.2, 4.4.2, 4.5.3/4.5.5/4.5.7/4.5.9/4.5.11, 4.7.2, 4.9.1 | Full study loop playable in all implemented modes |
| **6 — BE Group C: reporting** | Progress/history/dashboard read models | 7.1.1–7.6.2, 5.1.1/5.2.1/5.4.1/5.6.1 | Read models composed + tested |
| **7 — Dashboard + Progress + History screens** | Assemble "look back" FE | 5.x FE, 7.5.x, 7.6.3 | Dashboard + Progress + Card history render all states |
| **8 — Settings & cross-cutting** | Settings hub, TTS, learning settings, account | 8.1.x–8.5.x, TTS rows | Settings shells wired to real BE |
| **Ongoing** | Quality gates | Group 9 (9.1–9.18), Group 10 | Enforced every task; never "done" |

Phases 2/4/6 are **BE-by-subsystem** (allowed under the 7-row coupling rule); phases 3/5/7 assemble
screens on the Phase-1 widget kit + that group's BE contracts. Phase 8 and any feature-specific
widget (e.g. `MatchBoard`, `BoxStepper`, `ProgressRing`) land with their owning feature, not upfront.

### 4.2 Phase 3 (Content/Library FE) — delivery waves (2026-06-20)

Phase 3 is large (~28 §2/§3 FE rows). Because FE is **screen-centric** — most `x.2` rows are
*actions* (dialog/sheet) hosted on a §3 screen — it is sliced into **4 waves**, each a vertical
slice (one core screen + the shared dialogs/sheets it introduces, reused by later waves). All BE
these waves consume is already Implemented. One screen + its actions = a few PRs (1 work-package
per PR).

| Wave | Core screen(s) | Action rows hosted | New shared primitives (reused later) | ~PRs |
| --- | --- | --- | --- | --- |
| **W1 — Library overview** | `3.1.2` (+ `3.6.1` error/retry) | `2.1.2` create, `2.2.2` rename, `2.3.2`+`2.21.1` delete, `2.4.2` move, `2.5.2` reorder | folder-form dialog (name+color+icon), delete-confirm + blast-radius variant, move-target picker sheet, reorder-drag | ~4 |
| **W2 — Folder detail** | `3.2.2` (+ `3.3.1` nav→list) | `2.7.2` deck create, `2.8.2` rename, `2.9.2` delete, `2.19.2` move, `2.10.2` reorder, `2.16.2` parent-child guard UX | deck-create sheet (reuses dialogs/sheets from W1) | ~3 |
| **W3 — Flashcard list + editor** | `3.4.2` (+ `2.18.2` tag-filter UI) | `2.11.2`/`2.12.2` editor (create/edit), `2.15.2` tags, `2.20.2` dup soft-warning, `2.13.2` delete, `2.14.2` reorder | flashcard editor screen, tag-input chips | ~3 |
| **W4 — Global search** | `3.5.2` (+ `3.5.3` result nav) | — | search field already in kit | ~2 |

**Prerequisites (build before any Wave):**
- `1.2.6` — **screen-shell family (`MxScaffold`/`MxListScaffold`) + `MxFab` + `lib/app/app_shell.dart`.**
  Hard prerequisite for **every** Phase-3 FE screen row: the rebuild deleted the prior screen shell
  and the `no_raw_scaffold` guard (error) blocks any feature screen until it returns. Build first.
  (Surfaced by the Phase-3 overnight run 2026-06-20 — `PHASE3-OVERNIGHT.md` OQ-1.)
- `2.22.1` — ✅ Implemented (2026-06-20). `folders.color` + `folders.icon` columns + entity/create/
  rename wiring. Product chose full color/icon pickers, so the model stores them before `2.1.2`.
  Note: `2.1.2`'s picker still needs the design-system color palette + icon set defined (OQ-2).

**Resolved gating decisions (2026-06-20, product owner):**
- **Folder color/icon:** real + schema-backed (not preview-only / not deferred) → row `2.22.1`.
- **Folder-create/rename dialog contract:** the dialog **owns submit** (calls the create/rename use
  case, shows duplicate/empty errors **inline**, returns `Future<Folder?>`), not return-name-only.
  Reused by `2.1.2` and `2.2.2`.

**Blocked / out of this plan:** `2.17.2` (status filter/badges FE) waits on `2.17.1` (suspend/bury
schema); `3.8.1` (tags/recent/popular search sections) is Future. `3.2.3` (new-vs-due study split
on folder detail) waits on the Study epic (Phase 4) — render the deck rows now, add the study split
when §4 lands.

### 4.3 Critical Path & Blocker Register (2026-06-20)

As of 2026-06-20 the merged baseline is **content + tag + counts + learning/tag-settings BE**
(PRs #4–#8). Three latent blockers gate the rest of the roadmap. Each is now promoted to an
explicit **enabler work package on the critical path** so the project stops discovering them
mid-task. **Rule: do not start a dependent row until its enabler row is `Implemented`.** An enabler
is a normal WBS row (schema/contract only, narrow, testable) — not a research spike.

| # | Blocker (was implicit) | Enabler WP | Unblocks | Definition of Ready for dependents (enabler acceptance) | Risk if slipped |
| --- | --- | --- | --- | --- | --- |
| B1 | ✅ RESOLVED (v6, 4.0.1): `study_sessions` / `study_session_items` / `study_attempts` tables + `StudyRepository` port/impl skeleton + DI now shipped | **4.0.1** | All of Group 4 (4.1.x–4.11.x), 5.1.x (resume), 7.3.x (study stats) | Three tables shipped (migration + schema test); `StudyRepository` port + impl skeleton compiles + DI wired; schema/migration contracts updated | **Resolved.** Group 4 rows (4.1.x+) are now unblocked on the persistence backbone; they grow the port with the session lifecycle. |
| B2 | `flashcard_progress` has no `is_suspended` / `buried_until` columns | **4.0.2** | 2.17.1 status filter, 4.11.1/4.11.2 bury-suspend, 5.2.1 due-today, 7.1.1 due counts | Both columns shipped (migration v+1 + test); default values defined; eligibility/aggregate queries can filter on them | Medium. Blocks accurate due counts on dashboard/progress and the status-filter UX. Several rows silently 0-out today. |
| B3 | Import type-contract undefined after the pre-rebuild pipeline was deleted | **6.0.1** | 6.2.1–6.9.1 (entire import subsystem) | `FlashcardImportPreview`, row-issue enum, `ImportSourceFormat` pinned in `docs/contracts/types-catalog.md`; repo/use-case signatures agreed | Medium. Without it each import row re-invents the contract → rework + inconsistent preview model. |
| B4 | Card-history schema does not exist: no `card_events` table, no `flashcard_progress.last_reset_at`, no `study_attempts.duration_ms` (the `7.6.1` evidence column still cites phantom v6/v7 migrations that were never re-shipped after the rebuild) | **7.0.1** | 7.6.1 / 7.6.2 / 7.6.3 (card history) | `card_events` table + `flashcard_progress.last_reset_at` + `study_attempts.duration_ms` shipped (migration + schema test) | Low/Medium. Card-history feature is double-gated (also needs 4.0.1 for the attempt feed); fixes the phantom-migration drift in 7.6.1. |

**Critical-path sequence (recommended build order to keep flow unblocked):**

1. **4.0.2** (smallest; unblocks 4 rows incl. dashboard/progress accuracy) — can land immediately.
2. **6.0.1** (contract-only; unblocks the whole low-risk import subsystem for parallel work).
3. **4.0.1** (largest; gates the study epic) — schema-design review **before** coding (it is a
   multi-table backbone the ~20 Group-4 rows build on). Pair with the existing
   `4.5.12` mode-chain backbone so the two schema efforts converge, not collide.
4. **7.0.1** (card-history schema, B4) — after `4.0.1` (needs `study_attempts`); unblocks `7.6.x`.

**Already-resolved blocker (kept for traceability):** Folder color/icon + folder-dialog contract —
resolved 2026-06-20 (see §4.2). Enabler `2.22.1` is on Wave 1 of Phase 3.

**Discipline going forward (to prevent the next latent blocker):** every BE row whose `Depends on`
names a *table or column that is not yet shipped* must list the **enabler row** (not just `1.1.5`)
in its `Depends on`. A row that needs un-shipped schema is **Blocked**, not **Specified**.

### Group 1 — Project foundation

| WBS ID | Flow | Function | Layer | Deliverable | Status | Depends on | Evidence/Source | Commit ID | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1.1.1 | Foundation | Architecture baseline | BE | Clean Architecture skeleton (`app`/`core`/`domain`/`data`/`presentation`) with DI providers | Implemented | none | `lib/app/di/**`, `lib/domain/**`, `lib/data/**` | TBD | Done — `@riverpod` DI chain (DAO→repo→use case) wired + tested |
| 1.1.2 | Foundation | Theme tokens + design-system baseline | FE | Theme tokens (colors/type/spacing/radius), `MxTheme`, dark/light schemes from the design system | Implemented | none | `lib/core/theme/**` | TBD | Done — `MxColors`/`MxShadows` extensions + `MxSpacing`/`MxRadius`/`MxTypography` + `MxTheme` light/dark from `colors_and_type.css`; feeds the widget kit (1.2.x) |
| 1.1.3 | Foundation | Routing baseline | FE | GoRouter shell, `RouteNames`/`RoutePaths` constants, placeholder discipline | Implemented | none | `lib/app/router/app_router.dart`, `lib/app/router/redirect.dart`, `lib/app/router/route_placeholder.dart` | TBD | Done — GoRouter with top-level `/home`/`/library`/`/progress`/`/settings`, `/`→Library redirect, `RoutePlaceholder` discipline; feature route registries compose later |
| 1.1.4 | Foundation | l10n baseline | FE | ARB en/vi + generated localizations pipeline | Implemented | none | `lib/l10n/app_en.arb`, `lib/l10n/app_vi.arb`, `lib/l10n/generated/**` | TBD | Done — en/vi ARB + `gen-l10n` pipeline wired into `MaterialApp` (`localizationsDelegates`/`supportedLocales`/`onGenerateTitle`); add new keys per `docs/ui-ux/l10n-copy-contract.md` |
| 1.1.5 | Foundation | Drift/database baseline | BE | `.drift` schema layout, `AppDatabase`, migration infrastructure + first table | Implemented | none | `lib/data/datasources/local/app_database.dart`, `lib/data/datasources/local/drift/folders.drift`, `lib/data/datasources/local/connection/**`, `lib/data/datasources/local/migrations/**`, `lib/app/di/database_providers.dart`, `test/data/migrations/**` | TBD | Done — schema v1 (`folders` + index), `onCreate`/`onUpgrade`/`beforeOpen` migration infra, native/web connection, `appDatabaseProvider`; next tables follow §9 |
| 1.1.6 | Foundation | Guard/verification baseline | Test | `tool/verify` single-entry chain + `tool/doc_guard`; analyzer/format/targeted-test workflow | Implemented | none | `tool/verify/run.mjs`, `tool/doc_guard/run.mjs`, `docs/checklist/implementation-checklist.md` | TBD | Re-baseline doc_guard after Phase 0; add CI (9.10) |
| 1.1.7 | Foundation | Docs baseline | Docs | Business docs, wireframes, contracts, decision tables as source of truth | Implemented | none | `docs/business/index.md`, `docs/decision-tables/memox-core-decision-table.md` | TBD | Maintain parity per commit |
| 1.1.8 | Foundation | Observability / logging config | BE | Root `package:logging` → shared Talker sink (`MxLog.init`), level by `kReleaseMode`, guarded-zone + Flutter/platform SEVERE handlers, root `ProviderScope` Talker observer, app-boot log | Implemented (2026-06-21) | 1.1.1 | `lib/core/logging/log_config.dart`, `lib/app/bootstrap/app_bootstrap.dart`, `test/core/logging/log_config_test.dart`, `docs/quality/observability-contract.md` | TBD | Core config Current: `MxLog` pipes the root logger to one `Talker` (console in debug via `useConsoleLogs: !kReleaseMode`, in-memory history in all builds); `app_bootstrap` now logs uncaught zone/Flutter/platform errors `SEVERE` (was an empty handler) + `TalkerRiverpodObserver`. **Deferred:** persistent rotating log file (`persistent_log_writer.dart`, needs path_provider) + in-app Talker viewer screen (with Settings/About) + per-use-case entry/exit retrofit. Deps `logging`/`talker_flutter`/`talker_riverpod_logger` were already in pubspec. |
| 1.2.1 | Foundation | Core widget kit — surfaces & shell | FE | `MxContentShell`, `MxCard`, `MxListTile`, `MxAvatar` built from shots; golden per state (light+dark) | Implemented | 1.1.2 | `lib/presentation/shared/widgets/surfaces/**`, `lib/presentation/shared/layouts/**`, `lib/presentation/shared/widgets/mx_tappable.dart`, `test/presentation/shared/widgets/surfaces/**` | TBD | Done — `MxContentShell` gutter/cap, `MxCard` (token surface+border+sm shadow), `MxListTile`, `MxAvatar` (rounded/circle), `MxTappable` shaped-ink primitive; goldens + tests. Inter-row divider primitive deferred to a later kit row |
| 1.2.2 | Foundation | Core widget kit — navigation & app bar | FE | `MxAppBar`, `MxBottomNav`, `MxBreadcrumb`; selected/elevated states; goldens | Implemented | 1.1.2, 1.1.3 | `lib/presentation/shared/widgets/navigation/**`, `lib/presentation/shared/widgets/buttons/mx_icon_button.dart`, `test/presentation/shared/widgets/navigation/**` | TBD | Done — flat large-title `MxAppBar` (PreferredSizeWidget), `MxBottomNav` (accent-soft selected pill), `MxBreadcrumb` (scrollable trail), `MxIconButton` (toolbar, deferred from 1.2.3); shell golden + tests |
| 1.2.3 | Foundation | Core widget kit — actions & inputs | FE | Button system (`MxPrimaryButton`/`MxSecondaryButton` primitives + intent-driven `MxActionButton`/`MxActionIntent`/`MxButtonSize` + `MxCardActions`), `MxSearchField`, base `MxTextField`; goldens + density contract test | Implemented | 1.1.2 | `lib/presentation/shared/widgets/buttons/**`, `lib/presentation/shared/widgets/inputs/**`, `test/presentation/shared/widgets/{buttons,inputs}/**` | TBD | Done — built to `docs/ui-ux/action-hierarchy-contract.md` (base button is `MxPrimaryButton`/`MxSecondaryButton`, not a single `MxButton`); intent→size/full-width resolution + debug assert; light/dark goldens + density/behaviour tests. `MxIconButton` (toolbar) deferred to 1.2.2 |
| 1.2.4 | Foundation | Core widget kit — state widgets | FE | `MxLoadingState`, `MxEmptyState`, `MxErrorState`, `MxNoResultsState`; goldens; semantics | Implemented | 1.1.2 | `lib/presentation/shared/widgets/states/**`, `test/presentation/shared/widgets/states/**` | TBD | Done — card-agnostic centered panels (CTA via `action` slot), token-driven, light/dark goldens + functional tests; consumers wrap in `MxCard`/shell |
| 1.2.5 | Foundation | Core widget kit — dialogs & sheets | FE | `MxDialog`, `MxConfirmDialog`, `MxBottomSheet` shells with content slot; goldens | Implemented | 1.2.1 | `lib/presentation/shared/dialogs/**`, `test/presentation/shared/dialogs/**`, `docs/wireframes/24-shared-dialogs.md`, `docs/wireframes/25-shared-bottom-sheets.md` | TBD | Done — `MxDialog` shell, `MxConfirmDialog` (`show()`→`Future<bool>`, destructive=error+modal-locked), `MxBottomSheet`+`showMxBottomSheet` (drag handle, rounded top, 90% cap, root-nav); goldens + behaviour tests. Added `MxPrimaryButton.destructive` + `MxRadius.topSheet` tokens |
| 1.2.6 | Foundation | Core widget kit — screen-shell family & app shell | FE | `MxScaffold`/`MxListScaffold` (`+MxFormScaffold` as needed) screen-shell templates (appBar + body gutter via `MxContentShell` + optional FAB/bottomNav slots; `useShell` escape hatch), `MxFab`, and `lib/app/app_shell.dart` (bottom-nav host wired to the top-level routes via the shared navigation extension; **5 destinations after the design redesign** — Home · Library · Search · Progress · Settings) ; golden per template (light+dark) | Implemented (2026-06-20; nav extended to 5 tabs + `/search` by the design redesign) | 1.2.1, 1.2.2 | `lib/presentation/shared/layouts/mx_scaffold.dart`, `lib/presentation/shared/layouts/mx_list_scaffold.dart`, `lib/presentation/shared/widgets/buttons/mx_fab.dart`, `lib/app/app_shell.dart`, `lib/app/router/app_router.dart`, `test/presentation/shared/layouts/**`, `test/app/**` | TBD | **Critical-path foundation — unblocks ALL Phase-3 FE (Wave 1–4).** Done — `MxScaffold` (appBar + `MxContentShell` gutter + FAB/bottomNav/bottomSheet slots + `useShell` escape hatch), `MxListScaffold` (separated list + pinned header), `MxFab` (icon-only + `.extended`), and `MxAppShell` (`StatefulShellRoute.indexedStack` over the top-level branches — **5 after the design redesign: Home · Library · Search · Progress · Settings** — `MxBottomNav`, `goBranch` reset semantics). Router refactored to the shell; added `home/library/search/progress/settingsTitle` l10n; light+dark goldens for both scaffolds + behaviour tests + router/shell tests. `MxFormScaffold` deferred until a form screen needs it. Reference: `origin/backup/main-2026-06-19`, adapted to the current token/`Mx*` kit. |
| 1.2.7 | Foundation | Core widget kit — feature-enabling helpers (async / snackbar / text / hooks) | FE | `AppAsyncBuilder` (sanctioned `AsyncValue` renderer), `MxText`(`+MxTextRole`) (sanctioned typography widget), `showMxSnackbar` (sanctioned snackbar), `showMxDialog` (sanctioned custom-dialog host), and the `useMx*` text/search controller hooks; resolve the `MxTypography`-doc vs `no_direct_text_theme`-guard contradiction | Implemented (2026-06-20) | 1.2.4, 1.2.5 | `lib/presentation/shared/async/app_async_builder.dart`, `lib/presentation/shared/widgets/mx_text.dart`, `lib/presentation/shared/feedback/mx_snackbar.dart`, `lib/presentation/shared/dialogs/mx_dialog.dart`, `lib/presentation/shared/hooks/`, `lib/core/util/string_utils.dart` | TBD | **Second critical-path foundation tier — also deleted in the rebuild reset (`717f5b82`) but required by the guard for every feature screen.** Surfaced while building 3.1.2: guards `use_app_async_builder`, `no_direct_text_theme` (needs `MxText`), `no_raw_snackbar`/`no_direct_scaffold_messenger`, `no_direct_show_dialog`, `text_controller_requires_mx_hook`/`search_field_uses_shared_hook` all demand shared helpers that no longer existed. Ported from `origin/backup/main-2026-06-19`, adapted to the rebuilt token/util layout. Resolved the doc/guard contradiction via `MxText` (the feature-facing API). |

### Group 2 — Content management

| WBS ID | Flow | Function | Layer | Deliverable | Status | Depends on | Evidence/Source | Commit ID | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2.1.1 | Content management | Folder Create BE V1 | BE | Validated root/subfolder create use cases + repo + DAO + tests | Implemented | 1.1.5 | `lib/domain/usecases/folder/create_root_folder_usecase.dart`, `lib/domain/usecases/folder/create_subfolder_usecase.dart`, `test/data/repositories/folder_repository_impl_test.dart` | TBD | Done — `CreateRootFolderUseCase`/`CreateSubfolderUseCase` over `FolderRepository`(`Impl`)+`FolderDao`+`FolderMapper`; trim/empty/dup + parent-mode lock; rows F1-F4 tested |
| 2.1.2 | Content management | Folder Create FE V1 | FE | Create-folder dialog (name + color + icon pickers), form state, submit, error/loading | Implemented (2026-06-20) | 2.1.1, 2.22.1 | `lib/presentation/features/folders/widgets/folder_create_dialog.dart` (`showFolderCreateDialog`, `FolderDraft`, color+icon pickers from `folder_visual_tokens.dart`), `lib/presentation/features/folders/widgets/library_create_folder_action.dart` (`runCreateFolder` — FAB + empty-CTA entry), `LibraryActionController.create`, `test/presentation/features/folders/library_overview_test.dart` | TBD | **Implemented as part of the Library Overview full-parity revision (2026-06-20).** Color/icon palette = OQ-2 approved set (8 `note-*` tints + 12 icons), resolved via `folder_visual_tokens.dart`. Dialog returns `FolderDraft?` (`null` = cancel); `runCreateFolder` owns submit + snackbar. Subfolder-create reuse + dedicated dialog test remain follow-ups. |
| 2.2.1 | Content management | Folder Rename BE V1 | BE | Validated rename use case + repo + tests | Implemented | 2.1.1 | `lib/domain/usecases/folder/rename_folder_usecase.dart`, `test/data/repositories/folder_repository_impl_test.dart` | TBD | Done — `RenameFolderUseCase`/`FolderRepository.renameFolder`; trim/empty/sibling-dup + unchanged-name no-op; row F8 tested |
| 2.2.2 | Content management | Folder Rename FE V1 | FE | Rename dialog via folder actions sheet | Implemented (2026-06-20) | 2.2.1 | `lib/presentation/features/folders/widgets/library_folder_actions_sheet.dart`, `test/presentation/features/folders/library_folder_actions_sheet_test.dart` | TBD | Implement |
| 2.3.1 | Content management | Folder Delete BE V1 | BE | Transactional cascade delete per persistence rules + tests | Implemented | 2.1.1 | `lib/domain/usecases/folder/delete_folder_usecase.dart`, `lib/data/repositories/folder_repo_impl_mutation_helpers.dart`, `test/data/repositories/folder_repository_impl_test.dart` | TBD | Done — `DeleteFolderUseCase`/`FolderRepository.deleteFolder`; recursive deepest-first folder cascade + revert-emptied-parent-to-unlocked, one transaction; rows F8/F9 tested. Deck/card/session cascade deferred to WBS 2.7.x+ (tables not in schema yet) |
| 2.3.2 | Content management | Folder Delete FE V1 | FE | Confirmation dialog + success/error state | Implemented (2026-06-20) | 2.3.1 | `lib/presentation/shared/dialogs/mx_folder_delete_dialog.dart`, `test/presentation/shared/dialogs/mx_folder_delete_dialog_test.dart` | TBD | Implement |
| 2.4.1 | Content management | Folder Move BE V1 | BE | Cycle-safe move + move-target query + tests | Implemented | 2.1.1 | `lib/domain/usecases/folder/move_folder_usecase.dart`, `lib/domain/usecases/folder/get_folder_move_targets_usecase.dart`, `lib/domain/models/folder_move_target.dart`, `lib/data/repositories/folder_repository_impl.dart`, `test/data/repositories/folder_repository_impl_test.dart`, `test/domain/usecases/folder/move_folder_usecase_test.dart` | TBD | Done — `MoveFolderUseCase`/`GetFolderMoveTargetsUseCase` over `FolderRepository.{moveFolder,getFolderMoveTargets}`; transactional move recomputes `sort_order`, locks unlocked destination, reverts emptied old parent; rejects cycle (self/descendant)/decks-lock/dup/missing; targets list annotates root + cycle + lockedToDecks + currentParent; rows F7/F14-F18 tested. Deck/card subtree relocation deferred to WBS 2.7.x+ |
| 2.4.2 | Content management | Folder Move FE V1 | FE | Move picker sheet + states | Implemented (2026-06-20) | 2.4.1 | `lib/presentation/features/folders/widgets/folder_move_picker_sheet.dart`, `test/presentation/features/folders/folder_move_picker_sheet_test.dart` | TBD | Implement |
| 2.5.1 | Content management | Folder Reorder BE V1 | BE | `sort_order` reorder use case + DAO + tests | Implemented | 2.1.1 | `lib/domain/usecases/folder/reorder_folders_usecase.dart`, `lib/data/repositories/folder_repo_impl_mutation_helpers.dart`, `lib/data/repositories/folder_repository_impl.dart`, `test/data/repositories/folder_repository_impl_test.dart`, `test/domain/usecases/folder/reorder_folders_usecase_test.dart` | TBD | Done — `ReorderFoldersUseCase`/`FolderRepository.reorderFolders`; full-sibling-list validation (rejects dup/missing/extra/cross-parent/partial, preserves order) + transactional positional `sort_order` write; rows F10/F11 tested |
| 2.5.2 | Content management | Folder Reorder FE V1 | FE | Manual reorder UI when sort mode allows | Specified | 2.5.1 | TBD | TBD | Wire reorder gesture to BE; widget tests |
| 2.6.1 | Content management | Folder content-mode guard BE | BE | Parent holds subfolders or decks, never both; enforced outside UI | Implemented | 2.1.1 | `lib/domain/types/content_mode.dart`, `lib/data/repositories/folder_repo_impl_mutation_helpers.dart`, `test/data/repositories/folder_repository_impl_test.dart` | TBD | Done — `ContentMode` enum + repo-layer subfolder guard (`folder_contains_decks`) + parent auto-lock on subfolder create; row F4 tested. Symmetric deck-side guard (`folder_contains_subfolders`, F6) lands with deck create (WBS 2.7.x) |
| 2.7.1 | Content management | Deck Create BE V1 | BE | Validated deck create (folder-owned, mode lock) + tests | Implemented (2026-06-20) | 2.6.1 | `lib/data/datasources/local/drift/decks.drift`, `lib/data/datasources/local/migrations/v2_add_decks.dart`, `lib/domain/entities/deck.dart`, `lib/domain/types/target_language.dart`, `lib/data/datasources/local/daos/deck_dao.dart`, `lib/data/mappers/deck_mapper.dart`, `lib/domain/usecases/deck/create_deck_usecase.dart`, `test/data/repositories/folder_repository_impl_deck_test.dart`, `test/domain/usecases/deck/create_deck_usecase_test.dart` | Yes | Done — schema v2 `decks` table (FK→folders cascade, `target_language` default `'korean'`); `CreateDeckUseCase` over `FolderRepository.createDeck`; appends `sort_order`, locks unlocked folder to decks, rejects empty/dup/subfolders-locked/missing; rows D1/D2. FE 2.7.2 next |
| 2.7.2 | Content management | Deck Create FE V1 | FE | Create-deck flow from folder detail | Specified | 2.7.1 | `lib/presentation/features/folders/screens/folder_detail_screen.dart`, `test/presentation/features/folders/folder_detail_test.dart` | TBD | Implement |
| 2.8.1 | Content management | Deck Rename BE V1 | BE | Validated deck rename use case + repo method + tests | Implemented (2026-06-20) | 2.7.1 | `lib/domain/usecases/deck/rename_deck_usecase.dart`, `lib/data/repositories/folder_repo_impl_mutation_helpers.dart`, `lib/data/repositories/folder_repository_impl.dart`, `test/data/repositories/folder_repository_impl_deck_test.dart`, `test/domain/usecases/deck/rename_deck_usecase_test.dart` | Yes | Done — `RenameDeckUseCase` over `FolderRepository.renameDeck`; trims, no-ops on unchanged, preserves folder/`sort_order`, rejects empty/dup/missing; rows D6/D7. FE 2.8.2 next |
| 2.8.2 | Content management | Deck Rename FE V1 | FE | Rename entry in deck actions sheet + dialog + states | Specified | 2.8.1 | `lib/presentation/features/flashcards/widgets/deck_actions_sheet.dart` (sheet exists; rename action TBD) | TBD | Wire rename action to BE; widget tests |
| 2.9.1 | Content management | Deck Delete BE V1 | BE | Transactional deck delete with flashcard/progress cleanup + tests | Implemented | 2.7.1, 2.11.1 | `lib/domain/usecases/deck/delete_deck_usecase.dart`, `lib/domain/repositories/folder_repository.dart` (`deleteDeck`), `lib/data/repositories/folder_repository_impl.dart`, `lib/app/di/folder_providers.dart`, `test/domain/usecases/deck/delete_deck_usecase_test.dart`, `test/data/repositories/folder_repository_impl_delete_deck_test.dart` | D3 | Done (2026-06-20) — `FolderRepository.deleteDeck` + `DeleteDeckUseCase`: deck-row delete cascades to flashcards + `flashcard_progress` + `flashcard_tags` via the `flashcards.deck_id` ON DELETE CASCADE chain (schema v3); source folder reverts to `unlocked` when it loses its last deck; missing deck → `NotFoundFailure`. Study attempt/session cascade deferred to WBS 4.x. |
| 2.9.2 | Content management | Deck Delete FE V1 | FE | Confirmation + success/error state from deck actions | Specified | 2.9.1 | `lib/presentation/features/flashcards/widgets/deck_actions_sheet.dart` | TBD | Implement |
| 2.10.1 | Content management | Deck Reorder BE V1 | BE | `sort_order` reorder use case + tests | Implemented (2026-06-20) | 2.7.1 | `lib/domain/usecases/deck/reorder_decks_usecase.dart`, `lib/data/repositories/folder_repo_impl_mutation_helpers.dart`, `lib/data/repositories/folder_repository_impl.dart`, `test/data/repositories/folder_repository_impl_deck_test.dart`, `test/domain/usecases/deck/reorder_decks_usecase_test.dart` | Yes | Done — `ReorderDecksUseCase` over `FolderRepository.reorderDecks`; writes `sort_order` by list position in one transaction; rejects duplicate/missing/extra/cross-folder/partial preserving prior order; rows D4/D8. FE 2.10.2 next |
| 2.10.2 | Content management | Deck Reorder FE V1 | FE | Manual reorder UI | Specified | 2.10.1 | TBD | TBD | Wire to BE; widget tests |
| 2.11.1 | Content management | Flashcard Create BE V1 | BE | Front/back required-after-trim validation, optional fields, default SRS progress + tests | Implemented | 1.1.5 | `lib/domain/usecases/flashcard/create_flashcard_usecase.dart`, `lib/domain/repositories/flashcard_repository.dart`, `lib/data/repositories/flashcard_repository_impl.dart`, `lib/data/datasources/local/drift/flashcards.drift`, `lib/data/datasources/local/migrations/v3_add_flashcards.dart`, `test/domain/usecases/flashcard/flashcard_usecases_test.dart`, `test/data/repositories/flashcard_repository_impl_test.dart` | C1/C2/C3/C8 | Done — schema v3 + create use case |
| 2.11.2 | Content management | Flashcard Create FE V1 | FE | Editor **screen shell** (create mode): X/Save app bar + breadcrumb + FRONT/BACK + front+back-required gating | Implemented (2026-06-22, WP-FL2a) | 2.11.1 | `lib/presentation/features/decks/screens/flashcard_editor_screen.dart`, `lib/presentation/features/decks/widgets/flashcard_editor_body.dart`, `lib/app/router/{route_names,route_paths}.dart`, `lib/presentation/features/folders/routes/folder_routes.dart`, `test/presentation/features/decks/flashcard_editor_test.dart` | TBD | **Shell shipped (WP-FL2a)** + dirty-form discard-confirm + Details expander (example/pronunciation/hint, **WP-FL2b2**) + **saving spinner + save-failed banner (WP-FL2b3a, 2026-06-22)** (`07` Saving/Save-failed; rows C29/C45) + **loading skeleton + load-error/Retry (WP-FL2b3b, 2026-06-22)** (`07` Loading/Load error; rows C46/C28). Remaining: Tags chip input (**WP-FL2b2b**) and save-and-add-another. |
| 2.12.1 | Content management | Flashcard Update BE V1 | BE | Validated update use case + tests | Implemented | 2.11.1 | `lib/domain/usecases/flashcard/update_flashcard_usecase.dart`, `lib/data/repositories/flashcard_repository_impl.dart`, `test/domain/usecases/flashcard/flashcard_usecases_test.dart`, `test/data/repositories/flashcard_repository_impl_test.dart` | C5 | Done — update + keep/reset progress |
| 2.12.2 | Content management | Flashcard Edit FE V1 | FE | Editor **screen shell** (edit mode): prefilled FRONT/BACK, back-arrow/Save app bar | Implemented (2026-06-22, WP-FL2a) | 2.12.1 | `lib/presentation/features/decks/screens/flashcard_editor_screen.dart`, `lib/presentation/features/decks/widgets/flashcard_editor_body.dart`, `test/presentation/features/decks/flashcard_editor_test.dart` | TBD | **Shell shipped (WP-FL2a)** + dirty-discard confirm + **trash/delete-from-editor (WP-FL2b1, 2026-06-22)** (shares the editor with 2.11.2; create vs edit by `cardId`) + Details expander (**WP-FL2b2**) + **saving spinner + save-failed banner (WP-FL2b3a, 2026-06-22)** (`08` Saving/Save-failed; rows C29/C45) + **loading skeleton + load-error/Retry (WP-FL2b3b, 2026-06-22)** (`08` Loading/Load error; rows C46/C28). Remaining: Tags chip input (**WP-FL2b2b**). |
| 2.13.1 | Content management | Flashcard Delete BE V1 | BE | Delete use case with cascade per persistence rules | Implemented | 2.11.1 | `lib/domain/usecases/flashcard/delete_flashcard_usecase.dart`, `lib/data/repositories/flashcard_repository_impl.dart`, `test/domain/usecases/flashcard/flashcard_usecases_test.dart`, `test/data/repositories/flashcard_repository_impl_test.dart` | C6 | Done — delete + FK cascade |
| 2.13.2 | Content management | Flashcard Delete FE V1 | FE | Card long-press → destructive confirm → delete + snackbar | Implemented (2026-06-21) | 2.13.1 | `lib/presentation/features/decks/widgets/flashcard_list_actions.dart` (`runDeleteCard`), `test/presentation/features/decks/flashcard_list_test.dart` | C6 | **Done (WP-FL4).** A card long-press (`FlashcardTile.onActions`) opens `MxConfirmDialog` (destructive, `cardDeleteTitle`/`cardDeleteMessage`) → `FlashcardActionController.delete` → `cardDeletedSnack`. BE cascade tested (C6); FE affordance widget-tested (long-press → confirm → cancel keeps the card). Corrected phantom `features/flashcards/...` source paths → real `features/decks/`. |
| 2.14.1 | Content management | Flashcard Reorder BE V1 | BE | `sort_order` reorder use case + repo method | Implemented | 2.11.1 | `lib/domain/usecases/flashcard/reorder_flashcards_usecase.dart`, `lib/data/repositories/flashcard_repository_impl.dart`, `test/data/repositories/flashcard_repository_impl_test.dart` | C33/C34 | Done — full-list reorder |
| 2.14.2 | Content management | Flashcard Reorder FE V1 | FE | Reorder mode (overflow → Reorder cards) in flashcard list | Implemented (2026-06-21) | 2.14.1 | `lib/presentation/features/decks/widgets/flashcard_deck_overflow_sheet.dart`, `lib/presentation/features/decks/widgets/flashcard_list_body.dart` (`_reorderContent`/`_ReorderRow`), `lib/presentation/features/decks/widgets/flashcard_list_actions.dart` (`runDeckOverflow`/`runReorderCards`), `lib/presentation/features/decks/screens/flashcard_list_screen.dart`, `lib/presentation/features/decks/viewmodels/flashcard_list_viewmodel.dart` (`FlashcardReorderActive`), `lib/presentation/features/decks/controllers/flashcard_action_controller.dart` (`reorder`), `test/presentation/features/decks/flashcard_list_test.dart` | C33, C34 | **Done (WP-FL3).** Deck overflow kebab → sheet (Reorder cards / Delete deck); Reorder cards enters reorder mode: X/Done `MxAppBar`, `{n} CARDS · DRAG TO REORDER` overline, `ReorderableListView` (`ValueKey('flashcard_reorder_list')`, `buildDefaultDragHandles:false` + trailing `ReorderableDragStartListener`); a drop persists via `ReorderFlashcardsUseCase` (`runReorderCards`). 4 ARB keys (en+vi). Widget test (enter/handles/Done/exit) + reorder golden (light+dark). |
| 2.15.1 | Content management | Flashcard Tags BE V1 | BE | Create-time tag validation (trim/lowercase/dedupe) + `flashcard_tags` table + migration shipped inside 2.11.1 (schema v3); standalone tag editing/management (rename/delete/study-by-tag) remains Future per `docs/business/tags/tag-system.md` | Implemented (BE create-time tags landed in 2.11.1) | 1.1.5 | `lib/data/datasources/local/drift/flashcards.drift` (`flashcard_tags`), `lib/data/datasources/local/migrations/v3_add_flashcards.dart`, `lib/data/repositories/flashcard_repository_impl.dart` (`_normalizeTags`), `test/data/repositories/flashcard_repository_impl_test.dart` | C1/C5 | Done (create-time tags) — standalone tag mgmt Future |
| 2.15.2 | Content management | Flashcard Tags FE V1 | FE | Tag input section in editor | Specified | 2.15.1 | `lib/presentation/features/flashcards/widgets/flashcard_editor_tags_section.dart` | TBD | Implement |
| 2.16.1 | Content management | Parent-child validation BE | BE | Deck must belong to folder; flashcard must belong to deck (non-null FK + repo checks) | Implemented | 1.1.5 | `lib/data/datasources/local/drift/decks.drift` (non-null `folder_id`), `lib/data/datasources/local/drift/flashcards.drift` (non-null `deck_id`), `lib/data/repositories/flashcard_repository_impl.dart`, `test/data/migrations/app_database_schema_test.dart` | TBD | Done — non-null `folder_id` (decks→folders cascade) + non-null `deck_id` (flashcards→decks cascade) FKs; repo checks: `FolderRepositoryImpl.createDeck` rejects missing folder (`NotFoundFailure`), `FlashcardRepositoryImpl.createFlashcard` rejects missing deck (`NotFoundFailure`); FK-reject + cascade tests; row C41 |
| 2.16.2 | Content management | Parent-child guard FE | Integration | Invalid actions prevented in UI with controlled error, not crash | Specified | 2.16.1 | feature screens use `Result`/failure mapping | TBD | Audit controlled-error coverage for invalid parent actions |
| 2.17.1 | Content management | Flashcard status filter BE V1 | BE | List query filters for active/suspended/buried/due + tests | Implemented | 2.15.1, 4.0.2 | `lib/domain/types/flashcard_status_filter.dart`, `lib/domain/repositories/flashcard_repository.dart`, `lib/domain/usecases/flashcard/watch_flashcard_list_usecase.dart`, `lib/data/datasources/local/daos/flashcard_dao.dart`, `lib/data/repositories/flashcard_repository_impl.dart`, `test/data/repositories/flashcard_repository_impl_test.dart`, `test/domain/usecases/flashcard/watch_flashcard_list_usecase_test.dart` | C36, C37, BS8, BS9 | Done (2026-06-21) — `watchFlashcardList` gains a `FlashcardStatusFilter status` param (all/active/due/suspended/buried). State derived per card from `flashcard_progress` (no row = new active card); `active` excludes suspended + currently-buried (expired bury counts active), `due` = active AND `due_at <= now`, `now` read once per emission via the injected clock. Composes with search + tags; `totalCount` stays full deck total. New `FlashcardDao.progressForFlashcards` batch reader. **unblocked: deps met** (2.15.1 + 4.0.2 both Implemented; row was stale `Blocked`/`Specified`). |
| 2.17.2 | Content management | Flashcard status filter/badges FE V1 | FE | Filter chips + suspended/buried badges in flashcard list | Specified | 2.17.1 | `docs/business/study-actions/bury-suspend.md` | TBD | Wire chips/badges to BE; widget tests |
| 2.18.1 | Content management | Flashcard tag filter BE V1 | BE | Multi-select AND tag filter inside deck + tests | Implemented | 2.15.1 | `lib/domain/repositories/flashcard_repository.dart`, `lib/domain/usecases/flashcard/watch_flashcard_list_usecase.dart`, `lib/data/repositories/flashcard_repository_impl.dart`, `test/data/repositories/flashcard_repository_impl_test.dart`, `test/domain/usecases/flashcard/watch_flashcard_list_usecase_test.dart` | C38, C39 | Done (2026-06-20) — `watchFlashcardList` gains a `List<TagName> tags` param: cards filtered to those carrying **every** selected tag (AND), each normalized with the storage rule (`_normalizeTags`: trim+lowercase+dedup) so it matches stored tags; empty selection = no filter. Composes with the front/back search; `totalCount` stays the full deck total (filter-independent). In-Dart filter over the already-loaded `tagsByCard` map (no new query). Status filter (active/suspended/buried/due) remains 2.17.1 — blocked on suspend/bury columns. |
| 2.18.2 | Content management | Flashcard tag filter FE V1 | FE | Tag filter chips + clear-filters empty state | Specified | 2.18.1 | `docs/business/tags/tag-system.md` | TBD | Wire to BE; widget tests |
| 2.19.1 | Content management | Deck Move BE V1 | BE | Move deck to another folder (mode validation, sort append, source unlock) + tests | Implemented (2026-06-20) | 2.7.1 | `lib/domain/usecases/deck/move_deck_usecase.dart`, `lib/data/repositories/folder_repo_impl_mutation_helpers.dart`, `lib/data/repositories/folder_repository_impl.dart`, `test/data/repositories/folder_repository_impl_move_deck_test.dart`, `test/domain/usecases/deck/move_deck_usecase_test.dart` | Yes | Done — `MoveDeckUseCase` over `FolderRepository.moveDeck`; appends to destination `sort_order`, locks unlocked destination to decks, reverts emptied source to unlocked, no-ops on same folder; rejects missing deck/dest, subfolders-locked dest, dup sibling; never touches flashcards/progress/tags; rows D9/D10. FE 2.19.2 next |
| 2.19.2 | Content management | Deck Move FE V1 | FE | Move action in deck actions sheet + folder picker | Implemented (2026-06-21) | 2.19.1 | `lib/domain/models/deck_move_target.dart`, `lib/domain/usecases/deck/get_deck_move_targets_usecase.dart`, `lib/data/repositories/folder_repository_impl.dart` (`getDeckMoveTargets`), `lib/app/di/folder_providers.dart`, `lib/presentation/features/folders/widgets/deck_move_picker_sheet.dart`, `lib/presentation/features/folders/widgets/deck_actions_sheet.dart`, `lib/presentation/features/folders/widgets/folder_detail_actions.dart`, `lib/presentation/features/folders/controllers/library_action_controller.dart`, `test/data/repositories/get_deck_move_targets_test.dart`, `test/domain/usecases/deck/get_deck_move_targets_usecase_test.dart`, `test/presentation/features/folders/deck_move_test.dart` | D9–D12 | **Done (BE+FE).** WP-FD5a: `GetDeckMoveTargetsUseCase` + `DeckMoveTarget`/`DeckMoveBlock` annotate every folder (no Library root; subfolders-locked blocked). WP-FD5b: deck sheet `DeckAction.move` → `showDeckMovePicker` (tap-to-select, blocked/current disabled) → `MoveDeckUseCase` → snackbar; 4 ARB keys (en+vi); behavior widget tests. Variance: reuses the tap-based folder-picker pattern, not the kit's radio + "Move here" styling (deferred refinement for both pickers). |
| 2.20.1 | Content management | Manual duplicate soft-warning BE V1 | BE | Case-insensitive front+back duplicate check on create/edit save | Implemented | 2.11.1 | `lib/domain/usecases/flashcard/check_manual_duplicate_flashcard_usecase.dart`, `lib/domain/models/flashcard_duplicate_check_result.dart`, `test/domain/usecases/flashcard/check_manual_duplicate_flashcard_usecase_test.dart`, `test/data/repositories/flashcard_repository_impl_duplicate_behavior_test.dart` | TBD | Done — `CheckManualDuplicateFlashcardUseCase` over `FlashcardRepository.checkManualDuplicate`; trimmed + case-insensitive `front`+`back`, deck-scoped, `excludeId` on edit; non-blocking (create/update never reject); `FlashcardDuplicateCheckResult`; row C40 |
| 2.20.2 | Content management | Manual duplicate soft-warning FE V1 | FE | Non-blocking "save anyway?" confirm in editor | Specified | 2.20.1 | `docs/business/flashcard/flashcard-management.md` §Rules | TBD | Wire to BE; widget tests |
| 2.21.1 | Content management | Folder delete blast-radius confirm FE V1 | FE | Delete dialog shows subtree counts; stronger confirm above threshold | Implemented (2026-06-20) | 2.3.2 | `docs/business/folder/folder-management.md` §Rules | TBD | Extend delete dialog + tests |
| 2.21.1 | Content management | Folder delete blast-radius confirm FE V1 | FE | Delete dialog shows subtree counts; stronger confirm above threshold | Specified | 2.3.2 | `docs/business/folder/folder-management.md` §Rules | TBD | Extend delete dialog + tests |
| 2.23.1 | Content management | Content sort FE (shared sheet + global pref) | FE | Shared `showContentSortSheet` (Manual/Name/Newest; `lastStudied` deferred) + global `library.sort` pref via `ContentSortRepository`; presentation-side sort applied across Library / Folder detail / Deck / Flashcard | Implemented (2026-06-21) | 3.1.2, 3.2.2, 3.4.2 | `lib/presentation/shared/sort/content_sort.dart` (`sortByContentMode`), `content_sort_sheet.dart`, `library_sort_provider.dart`, `lib/app/di/content_sort_providers.dart`, `lib/domain/repositories/content_sort_repository.dart`, `lib/data/repositories/content_sort_repository_impl.dart`, `lib/data/datasources/local/preferences/content_sort_store.dart`, `lib/domain/types/content_sort_mode.dart` (`contentSortModeFromToken`), `test/presentation/shared/sort/content_sort_test.dart` | TBD | Sort is applied **presentation-side** via the shared `sortByContentMode<T>` core over the loaded read-model list (no `.drift`/repo ordering change): manual=DB order, name=case-folded A→Z, newest=created_at desc. `lastStudied` deferred (needs a last-studied aggregate read model). **All four screens wired** — each app bar gains a `swap_vert` icon → the shared sheet → a **per-scope** pref (`library.sort.<scope>`: `library` / `folder:<id>` / `deck:<id>`, so each object keeps its own sort): Library (`sortLibraryFolders`), Folder detail (subfolders + decks), Flashcard list (cards). Sheet rows carry a leading mode glyph (`contentSortModeIcon`). Resolves the enum(4)-vs-wireframe(5) sort-options drift in favour of the enum. |
| 2.22.1 | Content management | Folder color/icon schema + BE | BE | Add `folders.color` + `folders.icon` columns (migration v+1), extend `Folder` entity/mapper + `createRootFolder`/`createSubfolder`/`renameFolder` to carry them; defaults + schema/migration tests | Implemented (2026-06-20) | 1.1.5 | `lib/data/datasources/local/drift/folders.drift`, `lib/data/datasources/local/migrations/**`, `lib/domain/entities/folder.dart`, `lib/data/mappers/folder_mapper.dart`, `lib/domain/repositories/folder_repository.dart`, `lib/data/repositories/folder_repository_impl.dart`, `docs/database/schema-contract.md`, `docs/database/migration-contract.md`, `test/data/migrations/**` | TBD | **Prerequisite for 2.1.2 (Wave 1)** — product chose full color/icon pickers (2026-06-20), so the folder model must store them before the create dialog. Build first in Wave 1. |

### Group 3 — Library flow

| WBS ID | Flow | Function | Layer | Deliverable | Status | Depends on | Evidence/Source | Commit ID | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 3.1.1 | Library | Library overview load BE | BE | `watchLibraryOverview` stream query, stable ordering + tests | Implemented | 1.1.5 | `lib/domain/usecases/folder/watch_library_overview_usecase.dart`, `test/data/repositories/folder_read_queries_test.dart` | TBD | Done — `WatchLibraryOverviewUseCase`/`FolderRepository.watchLibraryOverview` → `LibraryOverview` (root folders + `subfolderCount`, stable `sort_order,created_at,id` order). Deck rows + non-zero card/due counts (F13) deferred to WBS 2.7.x/2.11.x |
| 3.1.2 | Library | Library overview FE | FE | Overview screen, sections, loading/empty/error, new-user empty state | Implemented (2026-06-20) | 3.1.1 | `lib/presentation/features/folders/screens/library_overview_screen.dart`, `test/presentation/features/folders/library_overview_test.dart` | TBD | Implement |
| 3.1.3 | Library | Library root anchor FE | FE | Root orientation dock on the loaded Library Overview: non-tappable `Library` breadcrumb crumb + `{n} folders` count, gating-matched to the FAB | Implemented (2026-06-21) | 3.1.2 | `lib/presentation/features/folders/widgets/library_root_anchor.dart`, `lib/presentation/features/folders/screens/library_overview_screen.dart`, `test/presentation/features/folders/library_overview_test.dart` | TBD | Owner-requested orientation aid (2026-06-21): the root reuses `MxBreadcrumb`/`buildLibraryBreadcrumb` (no ancestry → single current `Library` crumb) + `libraryFolderCountHeader`. Shown only in loaded-with-folders, non-search; hidden in search/loading/empty/error. Not in the kit `03a` mock — documented variance in `docs/design/screens/library-overview.visual-contract.md`. Loaded goldens (light+dark) regenerated. |
| 3.2.1 | Library | Folder detail load BE | BE | `watchFolderDetail` stream (children, decks, counts) + tests | Implemented | 3.1.1 | `lib/domain/usecases/folder/watch_folder_detail_usecase.dart`, `test/data/repositories/folder_read_queries_test.dart` | TBD | Done — `WatchFolderDetailUseCase`/`FolderRepository.watchFolderDetail` → `FolderDetail` (folder + root→leaf breadcrumb + child-folder summaries + counts); emits null when folder gone. Child decks + non-zero counts (F12) deferred to WBS 2.7.x/2.11.x |
| 3.2.2 | Library | Folder detail FE | FE | Folder detail screen, tiles, summary, states, navigation | Implemented (2026-06-20) | 3.2.1 | `lib/presentation/features/folders/screens/folder_detail_screen.dart`, `lib/presentation/features/folders/viewmodels/folder_detail_viewmodel.dart`, `lib/presentation/features/folders/widgets/{folder_detail_body,folder_stats_card,deck_tile,folder_detail_search,folder_detail_actions,deck_create_dialog,deck_actions_sheet}.dart`, `test/presentation/features/folders/folder_detail_test.dart` | TBD | **Implemented (2026-06-20).** `FolderDetail` extended with child `DeckSummary` decks + `subtreeDeckCount` (new `folderDeckSummaries` query, `subtree_deck_count` column). Library row tap → detail; subfolder tap → nested; deck tap → `/library/deck/:deckId/flashcards`. Stats card, content-aware FAB, create subfolder/deck, folder+deck actions, search, kit-04 states + goldens. Deck move + folder/deck reorder + study entry deferred (2.19.2 / 2.5.2 / 2.10.2). Also advances **2.7.2** deck-create, **2.8.2** deck-rename, **2.9.2** deck-delete (screen-hosted). |
| 3.2.3 | Library | Folder detail new vs due study split | BE+FE | New cards (`due_at NULL`) counted as NEW not due (data fix + migration v11); `new_count` read model on deck/subfolder rows; folder summary shows `{n} new`/`{m} due` + **Study new** (→ `study_type=new`) and **Review due** (→ `srs_review`) CTAs; deck-row new badge | Specified | 3.2.2, 4.5.10 | `lib/data/repositories/flashcard_repository_impl_imports.dart`, `lib/data/datasources/local/migrations/v11_clear_new_card_due_at.dart`, `lib/data/datasources/local/drift/folder_queries.drift`, `lib/presentation/features/folders/widgets/folder_detail_summary.dart`, `lib/presentation/features/folders/widgets/folder_deck_tile.dart`, `test/presentation/features/folders/folder_detail_test.dart`, `test/data/migrations/clear_new_card_due_at_migration_test.dart`, `docs/business/srs/srs-review.md`, `docs/wireframes/05-folder-detail.md` | TBD | Implement |
| 3.3.1 | Library | Deck → flashcard list navigation | Integration | Deck tile opens `/library/deck/:deckId/flashcards` | Implemented (2026-06-20) | 3.2.2, 3.4.2 | `lib/presentation/features/folders/routes/folder_routes.dart` (deckFlashcards child route), `lib/presentation/features/folders/widgets/folder_detail_body.dart` (`_openDeck` → `pushNamed`) | TBD | Done — registered as a child of the Library branch; deck tap pushes it. |
| 3.4.1 | Library | Flashcard list load BE | BE | `watchFlashcardList` stream + tests | Implemented | 2.11.1 | `lib/domain/usecases/flashcard/watch_flashcard_list_usecase.dart`, `lib/domain/models/flashcard_list_detail.dart`, `lib/data/repositories/flashcard_repository_impl.dart`, `test/data/repositories/flashcard_repository_impl_test.dart` | C35 | Done — list-watch + search (status/tag filters Future) |
| 3.4.2 | Library | Flashcard list FE | FE | List screen (8 states incl. empty/loading/error/search) | Implemented (2026-06-20, V1) | 3.4.1 | `lib/presentation/features/decks/screens/flashcard_list_screen.dart`, `lib/presentation/features/decks/viewmodels/flashcard_list_viewmodel.dart`, `lib/presentation/features/decks/controllers/flashcard_action_controller.dart`, `lib/presentation/features/decks/widgets/{flashcard_list_body,flashcard_tile,flashcard_list_search,flashcard_list_actions}.dart`, `test/presentation/features/decks/flashcard_list_test.dart` | TBD | **Implemented V1 (2026-06-20)** under `features/decks/` (not `flashcards/`). Card list (front/back rows + SRS subtitle WP-FL1), loaded/empty/loading/error/search/no-results states + goldens. Card add/edit → **editor screen** (`flashcard_editor_screen.dart`, WP-FL2a; the original dialog was retired) + delete + deck delete. **Deferred:** status chip (mock visual gap — business model New/Due only), editor Details/state-matrix (WP-FL2b), reorder done (2.14.2), import. |
| 3.5.1 | Library | Global search BE | BE | Search use case + repo, LIKE escaping, ranking, min-query/debounce contract + tests | Implemented | 1.1.5 | `lib/domain/usecases/search/global_search_usecase.dart`, `lib/domain/repositories/search_repository.dart`, `lib/data/repositories/search_repository_impl.dart`, `lib/data/datasources/local/drift/search_queries.drift`, `test/data/repositories/search_repository_impl_test.dart`, `test/domain/usecases/global_search_usecase_test.dart` | TBD | Done — `GlobalSearchUseCase` over `SearchRepository`(`Impl`)+`SearchDao`; normalize (trim/lowercase/collapse), 2-char min (`tooShort`), LIKE `%`/`_`/`\` escaping w/ `ESCAPE '\'`, exact→starts-with→substring→recency ranking, per-section cap 5 + un-capped totals; 3 sections (folders/decks/flashcards); Tags + recent + 300ms debounce are FE/Future; rows SR1-SR4, SR-rank, SR-cap, SR-empty, SR-err |
| 3.5.2 | Library | Global search FE | FE | Top-level **`/search`** screen (design redesign — bottom search dock, primary bottom-nav destination) with 5 states, grouped sections | Implemented (2026-06-21) | 3.5.1 | `lib/presentation/features/search/screens/global_search_screen.dart`, `lib/presentation/features/search/widgets/{global_search_body,global_search_results_view}.dart`, `lib/presentation/features/search/viewmodels/global_search_viewmodel.dart`, `lib/presentation/features/search/routes/search_routes.dart`, `lib/presentation/shared/widgets/inputs/mx_search_dock.dart`, `lib/presentation/shared/widgets/surfaces/mx_icon_tile.dart`, `test/presentation/features/search/global_search_test.dart` | SR5, SR6, SR-screen | Done — wired the ready `GlobalSearchUseCase` into the `/search` branch via `GlobalSearchBody` (debounced 300ms `GlobalSearchUseCase.inputDebounce`, 2-char min, in-band `Result` → error). `MxSearchDock` at the foot (shell stays watch-free). Built shared `MxSearchDock` + `MxIconTile` (FolderIconTile now delegates to it). 5 states + light/dark goldens + state tests. Advances **3.5.3** (result nav). |
| 3.5.3 | Library | Search result navigation | Integration | Folder → detail; deck → flashcard list; flashcard → owning deck | Implemented (2026-06-21) | 3.5.2 | `lib/presentation/features/search/widgets/global_search_results_view.dart`, `test/presentation/features/search/global_search_test.dart` | SR5, SR6 | Done — result rows `pushNamed` folder detail / deck flashcard list / owning deck (per-card scroll/select still Future). |
| 3.6.1 | Library | Error retry state | FE | Centralized failure mapping + refetch feedback on library screens | Specified | 3.1.2 | `lib/app/feedback/**`, `test/app/feedback/mx_app_feedback_observer_test.dart` | TBD | Implement |
| 3.7.1 | Library | Folder/deck due+card counts BE | BE | Counts stream from DB; due excludes buried/suspended | Implemented | 3.2.1, 2.11.1 | `lib/data/datasources/local/drift/folder_queries.drift`, `lib/data/datasources/local/daos/folder_dao.dart`, `lib/data/repositories/folder_repository_impl.dart`, `lib/domain/models/folder_summary.dart`, `lib/domain/models/folder_detail.dart`, `test/data/repositories/folder_deck_due_counts_test.dart`, `test/data/repositories/folder_read_queries_test.dart` | F12, F13 | Done (2026-06-20) — `rootFolderSummaries`/`childFolderSummaries`/`folderSubtreeCounts` drift queries: `deckCount` = direct child decks, `cardCount`/`dueCount` recursive over the folder subtree (whole-forest recursive CTE joined to folder rows, reactive over folders+decks+flashcards+flashcard_progress). `dueCount` = scheduled cards `due_at IS NOT NULL AND due_at <= :now` (NEW cards excluded). **F13 active-eligibility exclusion implemented (2026-06-21):** all four count queries (`rootFolderSummaries`/`childFolderSummaries`/`folderSubtreeCounts`/`folderDeckSummaries`) plus `dashboardDueSummary` exclude suspended (`COALESCE(is_suspended,0)=1`) and currently-buried (`buried_until > :now`) cards from `dueCount`, mirroring `study_scope_queries.drift` so count ↔ queue cannot diverge; an expired bury counts as due. `cardCount` still includes them. `FolderSummary`/`FolderDetail` now carry live counts. |
| 3.8.1 | Library | Tags/recent/popular search sections | FE | Tag search section, recent searches, popular tags landing | Future | 8.5.1 | `docs/business/search/global-search.md` | TBD | Do not implement before tag subsystem promotion |

### Group 4 — Study/SRS flow

| WBS ID | Flow | Function | Layer | Deliverable | Status | Depends on | Evidence/Source | Commit ID | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 4.0.1 | Study/SRS | **Study persistence schema + repo skeleton (ENABLER — B1)** | BE | `study_sessions` + `study_session_items` + `study_attempts` Drift tables (FK chain, indexes) + migration v6 + schema test; `StudyRepository` domain port + impl skeleton (`expireOldSessions` only, no SRS/grading logic) + DI wiring; schema/migration contracts updated | Implemented | 1.1.5 | `lib/data/datasources/local/drift/study_tables.drift`, `lib/data/datasources/local/migrations/v6_add_study_tables.dart`, `lib/data/datasources/local/app_database.dart`, `lib/domain/repositories/study_repository.dart`, `lib/data/repositories/study_repository_impl.dart`, `lib/data/datasources/local/daos/study_session_dao.dart`, `lib/app/di/study_providers.dart`, `docs/database/schema-contract.md`, `docs/database/migration-contract.md`, `test/data/migrations/v6_add_study_tables_migration_test.dart`, `test/data/migrations/app_database_schema_test.dart`, `test/data/repositories/study_repository_impl_test.dart` | TBD | **Critical-path enabler (shipped schema v6).** Gates ALL Group 4 + 5.1.x + 7.3.x. Port follows `lib/domain/repositories/**` convention (not the prior-iteration `lib/domain/study/ports/` path). Session lifecycle (create/load/finalize/SRS) grows the port in 4.1.x+; `study_flow`/`current_mode` mode-chain columns land with 4.5.12. |
| 4.0.2 | Study/SRS | **Bury/suspend progress columns + migration (ENABLER — B2)** | BE | Add `flashcard_progress.is_suspended` + `flashcard_progress.buried_until` columns (migration v+1, defaults: not-suspended / null), schema + migration tests; no behavior yet (eligibility logic lands in 4.11.1) | Implemented | 1.1.5 | `lib/data/datasources/local/drift/flashcards.drift`, `lib/data/datasources/local/migrations/v4_add_bury_suspend.dart`, `lib/data/datasources/local/app_database.dart`, `docs/database/schema-contract.md`, `docs/database/migration-contract.md`, `test/data/migrations/v4_add_bury_suspend_migration_test.dart`, `test/data/migrations/app_database_schema_test.dart` | TBD | **Enabler (shipped schema v4).** Columns only — unblocks 2.17.1 / 4.11.x / 5.2.1 / 7.1.1; eligibility index + read logic land with 4.11.1. |
| 4.1.1 | Study/SRS | Study entry eligibility BE | BE | Scope queries (deck/folder/today), empty/all-suspended outcomes + tests | Implemented | 4.0.1, 4.0.2 | `lib/domain/types/{entry_type,study_type,study_scope}.dart`, `lib/domain/models/study_entry_eligibility.dart`, `lib/domain/repositories/study_entry_repository.dart`, `lib/data/repositories/study_entry_repository_impl.dart`, `lib/data/datasources/local/drift/study_entry_queries.drift`, `lib/data/datasources/local/daos/study_entry_dao.dart`, `lib/domain/usecases/study/resolve_study_entry_eligibility_usecase.dart`, `lib/app/di/study_entry_providers.dart`, `test/data/repositories/study_entry_repository_impl_test.dart`, `test/domain/usecases/study/resolve_study_entry_eligibility_usecase_test.dart` | TBD | **BE eligibility classification only** (counts → `StudyScopeEmptyReason` / eligible). Empty-scope matrix S4/S4b/S4c/S4d/S4e/S4j/S4f/S4g covered by the repo test. Card-list load + `maxSessionItems` batching = 4.2.1; FE empty-state rendering = 4.1.2; `EntryType.tag` deferred. |
| 4.1.2 | Study/SRS | Study entry FE | FE | Entry gate screen with empty/error/resume-required states | Implemented (2026-06-22, WP-SR1a) | 4.1.1 | `lib/app/router/{route_names,route_paths}.dart`, `lib/app/router/app_router.dart`, `lib/presentation/features/study/routes/study_routes.dart`, `lib/presentation/features/study/screens/study_entry_screen.dart`, `lib/presentation/features/study/screens/study_session_screen.dart`, `lib/presentation/features/study/controllers/{study_entry_controller,study_entry_outcome}.dart`, `lib/presentation/shared/widgets/buttons/mx_icon_button.dart` (`.toolbar`), `test/presentation/features/study/study_entry_screen_test.dart` | S84/S85/S27/S28 | **Gate built (WP-SR1a):** top-level immersive study routes + `StudyEntryScreen` resolving `ResolveStudyEntryStartUseCase` → preparing / generic empty (blocks zero-card sessions) / Resume-Start-over-Back / error, and auto-create (`ResolveEligibleStudyCardsUseCase` + `CreateStudySessionUseCase`) → `pushReplacement` to the session placeholder. **WP-SR1b-1 added** the `today` route + `?study_type=` override (`StudyType.fromStorage`, canonical `new_cards`/`srs_review`; rows S85). **Remaining:** the per-reason empty matrix (8 `StudyScopeEmptyReason` variants + CTAs, WP-SR1b-2). |
| 4.1.3 | Study/SRS | Deck study CTA FE V1 | FE | Study-entry section on Flashcard List: Study deck + Today CTAs routing through the gate | Specified | 4.1.1 | `docs/wireframes/06-flashcard-list.md` (component row 4 target) | TBD | **Top priority** — the only study entry today is the Dashboard Today CTA; the study button must live next to the content |
| 4.1.4 | Study/SRS | Folder study CTA FE V1 | FE | Study folder + Today CTAs on Folder Detail routing through the gate | Specified | 4.1.1 | `docs/wireframes/05-folder-detail.md` | TBD | Wire CTAs to gate routes; widget tests |
| 4.2.1 | Study/SRS | Session creation BE | BE | Transactional `study_sessions` + `study_session_items` insert + tests | Implemented | 4.1.1 | `lib/domain/entities/study_session.dart`, `lib/domain/types/session_status.dart`, `lib/data/mappers/study_session_mapper.dart`, `lib/domain/repositories/study_repository.dart` (`createSession`), `lib/data/repositories/study_repository_impl.dart`, `lib/data/datasources/local/daos/study_session_dao.dart` (`createSessionWithItems`), `lib/domain/usecases/study/create_study_session_usecase.dart`, `lib/app/di/study_providers.dart`, `test/data/repositories/study_repository_create_session_test.dart` | TBD | **Transactional insert** (session+items, in_progress, queue order preserved, rolls back as a unit). Caller supplies the ordered `flashcardIds`; `maxSessionItems` cap = 4.2.4; ordered-eligible-id resolution + FE wiring separate. |
| 4.2.2 | Study/SRS | No-silent-resume gate BE | BE | Existing resumable session returns controlled `resumeRequired` | Implemented | 4.2.1 | `lib/domain/study/study_entry_start_result.dart`, `lib/domain/repositories/study_repository.dart` (`findResumable`), `lib/data/repositories/study_repository_impl.dart`, `lib/data/datasources/local/daos/study_session_dao.dart` (`findResumableSession`), `lib/domain/usecases/study/resolve_study_entry_start_usecase.dart`, `lib/app/di/study_providers.dart`, `test/data/repositories/study_repository_find_resumable_test.dart`, `test/domain/usecases/study/resolve_study_entry_start_usecase_test.dart` | TBD | `findResumable` (NULL-safe scope match, status draft/in_progress, 30-day window, most-recent) + `StudyEntryStartResult` (resumeRequired/canStart/blocked); `ResolveStudyEntryStartUseCase` gate composes resume + eligibility (4.1.1). Richer `ResumableSession` projection deferred to 5.1.1. |
| 4.2.3 | Study/SRS | Resume/start-over choice FE | FE | Explicit Resume / Start over / Back actions; transactional restart | Partial (2026-06-22, WP-SR1a) | 4.2.2 | `lib/presentation/features/study/screens/study_entry_screen.dart` (`_resumeBody`), `lib/presentation/features/study/controllers/study_entry_controller.dart` (`startOver` → cancel + create), `test/presentation/features/study/study_entry_screen_test.dart` | S28 | **Resume choice rendered inline in the gate (WP-SR1a):** Resume opens the session; Start over cancels the resumable session then creates a fresh one; Back pops. Built in the gate screen, not the previously-anticipated `study_entry_resume_required_state.dart` widget. A start-over confirm dialog + the dedicated extracted widget remain (WP-SR1b/SR4). |
| 4.2.4 | Study/SRS | Session batch limit BE V1 | BE | Cap session at `maxSessionItems` (default 20) before persisting session items; backend-only slice | Implemented | 4.2.1 | `lib/domain/usecases/study/create_study_session_usecase.dart` (`maxSessionItems` const + cap), `test/domain/usecases/study/create_study_session_usecase_test.dart`, `docs/business/study/study-flow.md` §Rules | TBD | Cap lives in the use case (owns the batching rule), not the repo; takes the first N in the caller's resolved order. FE next-batch CTA deferred. |
| 4.3.1 | Study/SRS | Session item loading BE | BE | Load persisted session + ordered items by sessionId | Implemented | 4.2.1 | `lib/domain/entities/study_session_review.dart`, `lib/domain/repositories/study_repository.dart` (`loadStudySessionReview`), `lib/data/repositories/study_repository_impl.dart`, `lib/data/datasources/local/daos/study_session_dao.dart` (`reviewItems` join), `lib/data/mappers/study_session_mapper.dart` (`toReviewItem`), `lib/domain/usecases/study/load_study_session_review_usecase.dart`, `lib/app/di/study_providers.dart`, `test/data/repositories/study_repository_load_review_test.dart` | TBD | `StudySessionReview` (header + ordered flashcard-joined items + answered status; `firstUnansweredIndex`/`isComplete` getters). Missing → `NotFoundFailure`; item-less session → `ValidationFailure`. Powers review shell (4.3.2) + recovery (4.8.1). |
| 4.3.2 | Study/SRS | Session review shell FE | FE | Current card, reveal toggle, Previous/Next navigation | Specified | 4.3.1 | `lib/presentation/features/study/screens/study_session_screen.dart`, `test/presentation/features/study/study_session_screen_test.dart` | TBD | Implement |
| 4.4.1 | Study/SRS | Submit self-grade BE | BE | `recordStudySessionAnswer` attempt persistence + tests | Implemented | 4.3.1 | `lib/domain/srs/srs_box.dart`, `lib/domain/repositories/study_repository.dart` (`recordStudySessionAnswer`), `lib/data/repositories/study_repository_impl.dart`, `lib/data/datasources/local/daos/study_session_dao.dart` (`recordAnswer`/`itemById`/`flashcardProgressBox`), `lib/data/mappers/study_session_mapper.dart` (result/study_mode tokens), `lib/domain/usecases/study/record_study_session_answer_usecase.dart`, `lib/app/di/study_providers.dart`, `test/data/repositories/study_repository_record_answer_test.dart`, `test/domain/srs/srs_box_test.dart` | TBD | One terminal attempt/item: inserts `study_attempts` (result/study_mode tokens + `box_before`→`box_after` via `SrsBox.nextBox`) + marks item answered + touches `updated_at`, transactional; `flashcard_progress` untouched (box = finalization 4.6.x). Validates terminal session / already-answered → `UnsupportedActionFailure`. Box-only; `due_at`/interval (S11-S14) land with 4.6.x. |
| 4.4.2 | Study/SRS | Self-grade FE (Forgot / Got it) | FE | Grading controls + answered-state advancement | Specified | 4.4.1 | `test/presentation/features/study/study_session_review_viewmodel_test.dart` | TBD | Implement |
| 4.4.3 | Study/SRS | Answer re-grade before finalize | BE | Allow correcting an answered item's grade until finalization (append attempt) | Specified | 4.4.1 | `docs/business/study/study-flow.md` §Retry behavior | TBD | Ship together with the first retry mode; do not change the current one-attempt rule alone |
| 4.5.1 | Study/SRS | Study mode strategy V1 BE | BE | `StudyModeStrategyFactory`: implemented mode dispatch matrix; sealed interaction families (`BinaryGrade`/`TypedAnswer`/`Board`) replace the throwing fat interface | Implemented | 4.3.1 | `lib/domain/types/attempt_result.dart`, `lib/domain/types/study_mode.dart`, `lib/domain/study/modes/study_mode_strategy.dart`, `lib/domain/study/modes/study_mode_strategy_factory.dart`, `test/domain/study/modes/study_mode_strategy_factory_test.dart` | TBD | Sealed base + 3 families (BinaryGrade review/recall/guess, TypedAnswer fill, Board match) + 5 concrete leaves; exhaustive factory switch. Per-mode deepening (option builder / fill evaluator / review semantics) = 4.5.2/4.5.6/4.5.8. Decision S48/S49/S58. |
| 4.5.2 | Study/SRS | Review mode BE V1 | BE | Both-sides item strategy + attempt semantics + tests | Specified | 4.5.1 | `lib/domain/study/modes/review_study_mode_strategy.dart`, `docs/business/study/study-flow.md`, `test/domain/study/modes/study_mode_strategy_factory_test.dart` | TBD | Implement |
| 4.5.3 | Study/SRS | Review mode FE V1 | FE | Review mode UI per wireframe 13 | Specified | 4.5.2 | `docs/wireframes/13-study-session-review.md` | TBD | Wire UI to strategy; widget tests |
| 4.5.4 | Study/SRS | Match mode BE V1 | BE | 5-pair board strategy, append-only evaluation persistence, transactional finalization derivation + tests | Specified | 4.5.1 | `docs/business/study/study-flow.md` | TBD | Implement |
| 4.5.5 | Study/SRS | Match mode FE V1 | FE | Match board UI | Specified | 4.5.4 | `docs/wireframes/14-study-session-match.md` | TBD | Wire UI; widget tests |
| 4.5.6 | Study/SRS | Guess mode BE V1 | BE | 5-option selection strategy + tests | Implemented | 4.5.1 | `lib/domain/study/modes/study_mode_strategy.dart`, `lib/domain/models/guess_option.dart`, `test/domain/study/modes/guess_option_builder_test.dart` | TBD | `GuessStudyModeStrategy.buildOptions`: correct back + up to 4 distinct seed-deterministic distractors; degrades on small pools; excludes target/blank/duplicate backs. New `GuessOption` value object. Grading stays the binary 4.4.1 path. Decision S47/S80-S83. |
| 4.5.7 | Study/SRS | Guess mode FE V1 | FE | Guess UI with auto-advance countdown | Specified | 4.5.6 | `lib/presentation/features/study/screens/study_session_screen.dart`, `lib/presentation/features/study/widgets/study_session_guess_mode_view.dart`, `lib/presentation/features/study/viewmodels/study_session_guess_viewmodel.dart`, `test/presentation/features/study/study_session_screen_test.dart`, `docs/wireframes/15-study-session-guess.md` | TBD | Implement |
| 4.5.8 | Study/SRS | Fill mode BE V1 | BE | Strict character match, mark-correct override, hint taint + tests | Implemented | 4.5.1 | `lib/domain/study/modes/study_mode_strategy.dart`, `test/domain/study/modes/fill_evaluator_test.dart` | TBD | `TypedAnswerStudyModeStrategy.evaluate(input, expected, hintUsed, markCorrect)`: trim-only case-sensitive match → `perfect`; hint taint or mark-correct override cap at `recovered`; else `forgot`. Grading flows through the 4.4.1 path. Decision S68/S69/S72. |
| 4.5.9 | Study/SRS | Fill mode FE V1 | FE | Typed-input fill UI | Specified | 4.5.8 | `docs/wireframes/17-study-session-fill.md`, `lib/presentation/features/study/screens/study_session_screen.dart`, `lib/presentation/features/study/widgets/study_session_fill_mode_view.dart`, `lib/presentation/features/study/viewmodels/study_session_fill_viewmodel.dart` | TBD | Implement |
| 4.5.10 | Study/SRS | Daily new limit BE V1 | BE | Cap new-card eligibility at `dailyNewLimit` (default 20) per local day; due review cards remain eligible; daily usage is derived from persisted new-card session items | Implemented | 4.5.1 | `lib/domain/repositories/study_entry_repository.dart` (`dailyNewLimit`), `lib/data/repositories/study_entry_repository_impl.dart` (`_capByDailyNewLimit`), `lib/data/datasources/local/drift/study_scope_queries.drift` (`newCardItemsUsedInWindow`), `lib/data/datasources/local/daos/study_scope_dao.dart`; `test/data/repositories/study_repository_daily_new_limit_test.dart` | TBD | New-card eligibility cap applied in `resolveEligibleCardIds` (local-day window in Dart; cancelled new-card sessions consume quota; review unaffected). Settings override deferred. S77–S79 |
| 4.5.11 | Study/SRS | Recall mode FE V1 | FE | Timed reveal/self-grade recall shell with 20s countdown, timeout reveal, and edit-pause resume | Specified | 4.5.2, 4.4.1 | `lib/presentation/features/study/screens/study_session_screen.dart`, `lib/presentation/features/study/widgets/study_session_recall_mode_view.dart`, `lib/presentation/features/study/viewmodels/study_session_recall_viewmodel.dart`, `test/presentation/features/study/study_session_screen_test.dart`, `docs/wireframes/16-study-session-recall.md` | TBD | Recall V1 only; no typed input / recovered path |
| 4.5.12 | Study/SRS | Mode-chain persistence backbone BE | BE | `StudyFlow` type + `StudyFlowPlan` (ordered phases) + `StudyFlowResolver`; schema v10 (`study_sessions.study_flow`, `current_mode`) + migration/test; persist flow + firstMode at create (new deck/folder → `new_full_cycle`). Backbone only — FE consumption + per-phase advance pending 4.5.13 | Specified | 4.5.1 | `lib/domain/types/study_flow.dart`, `lib/domain/study/study_flow_resolver.dart`, `lib/data/datasources/local/migrations/v10_add_study_flow_and_current_mode.dart`, `lib/data/mappers/study_mapper.dart`, `test/domain/types/study_flow_test.dart`, `test/data/migrations/study_flow_current_mode_migration_test.dart`, `docs/contracts/types-catalog.md`, `docs/business/study/study-flow.md` | TBD | FE/orchestration + cross-phase finalization tracked under 4.5.13 |
| 4.5.13 | Study/SRS | Per-phase chain runtime (advance + cross-phase finalization + FE) | BE+FE | Consume `current_mode`; `AdvanceStudyPhaseUseCase`; unify finalization to aggregate all-mode attempts + derived match result per item; session screen drives/advances phases (intermediate advance, last finalizes) | Specified | 4.5.12 | `docs/business/study/study-flow.md`, `docs/business/srs/srs-review.md` | TBD | Cross-phase SRS finalization is NEW spec — define before coding; ship advance + finalization-unification + FE together |
| 4.6.1 | Study/SRS | Finish session BE | BE | Finalization transaction: attempts → SRS outcome → session complete, rollback on failure | Implemented | 4.4.1 | `lib/domain/repositories/study_repository.dart` (`finalizeStudySession`), `lib/data/repositories/study_repository_impl.dart`, `lib/data/datasources/local/daos/study_session_dao.dart` (`finalizeSession`/`progressById`), `lib/domain/usecases/study/finalize_study_session_usecase.dart`, `lib/app/di/study_providers.dart`, `test/data/repositories/study_repository_finalize_test.dart` | TBD | One-transaction finalize: validate all answered (else `FinalizationFailure`, session stays open) → upsert progress + mark completed → rollback as a unit. Missing→NotFound, terminal→UnsupportedAction. |
| 4.6.2 | Study/SRS | SRS progress update BE | BE | Leitner outcome transitions + due-date computation in finalization | Implemented | 4.6.1 | `lib/domain/srs/srs_box.dart` (`nextBox`), `lib/domain/srs/box_intervals.dart`, `lib/data/repositories/study_repository_impl.dart` (`_terminalResult`/`_dueAtFor`), `test/data/repositories/study_srs_transition_test.dart` (table-driven, equal to `docs/business/srs/srs-review.md` transition + interval tables, decision rows S11–S15/S17–S18) | TBD | box_after=`SrsBox.nextBox`; due=interval[box]; review_count+1, lapse+1 on forgot; new card finalizes from box 1; suspend/bury preserved. Done together with 4.6.1+4.6.4. |
| 4.6.3 | Study/SRS | Finalization failure recovery | Integration | Finish failure keeps session open with controlled error; retry affordance | Specified | 4.6.1 | `test/data/repositories/study_repository_test.dart` (rollback) | TBD | Add retry affordance on result/finish failure path |
| 4.6.4 | Study/SRS | Due-time local-midnight normalization | BE | `due_at = localMidnight(studyDay + interval)` so due-today counts are stable across the day | Implemented | 4.6.2 | `docs/business/srs/srs-review.md` §Interval table, `lib/data/repositories/study_repository_impl.dart` (`_dueAtFor`), `test/data/repositories/study_srs_transition_test.dart` | TBD | `due_at = localMidnight(now) + interval[box]` computed in Dart local time (never SQL `localtime` — Windows-test NULL gotcha). Done with 4.6.1/4.6.2. |
| 4.7.1 | Study/SRS | Result summary BE | BE | `loadStudySessionResult` completed-session summary | Implemented | 4.6.1 | `lib/domain/models/study_session_result.dart`, `lib/data/repositories/study_repository_impl.dart`, `lib/domain/usecases/study/load_study_session_result_usecase.dart`; `test/data/repositories/study_repository_load_result_test.dart`, `test/domain/usecases/study/load_study_session_result_usecase_test.dart` | TBD | Header + ordered joined items + terminal result per item (last-attempt classifier) + total/answered/forgot/passed getters |
| 4.7.2 | Study/SRS | Result screen FE | FE | `/library/study/session/:sessionId/result` with fallback states | Specified | 4.7.1 | `lib/presentation/features/study/screens/study_result_screen.dart` | TBD | Implement |
| 4.8.1 | Study/SRS | Session persistence recovery | Integration | In-progress sessions reload by sessionId preserving answered items | Specified | 4.3.1 | `test/presentation/features/study/study_session_screen_test.dart` (recovery coverage) | TBD | Implement |
| 4.9.1 | Study/SRS | Protected active-session exit FE | FE | Exit confirmation; confirmed exit keeps session resumable | Specified | 4.3.2 | `lib/presentation/features/study/screens/study_session_screen.dart` | TBD | Implement |
| 4.10.1 | Study/SRS | Cancel/discard session BE | BE | `cancelStudySession` used by transactional start-over | Implemented | 4.2.1 | `lib/domain/repositories/study_repository.dart` (`cancelSession`), `lib/data/repositories/study_repository_impl.dart`, `lib/data/datasources/local/daos/study_session_dao.dart` (`markCancelled`), `lib/domain/usecases/study/cancel_study_session_usecase.dart`, `lib/app/di/study_providers.dart`, `test/data/repositories/study_repository_cancel_session_test.dart`, `test/domain/usecases/study/cancel_study_session_usecase_test.dart` | TBD | Status→`cancelled` for resumable (`draft`/`in_progress`) only — guarded UPDATE; row never deleted, attempts/items preserved. 0-rows → `sessionById` read → `NotFoundFailure` (missing) vs `UnsupportedActionFailure` (terminal). Powers start-over (4.2.3). |
| 4.10.2 | Study/SRS | Resume expiry anchor `updated_at` | BE | 30-day resumable filter anchors on `updated_at` (activity), not `started_at` | Specified | 4.2.1 | `docs/business/resume/resume-session.md` §Auto-expiry, `lib/data/datasources/local/daos/study_session_dao.dart`, `lib/data/repositories/study_repo_record_answer.dart`, `test/data/repositories/study_repository_test.dart` | TBD | Read-only resume/open remains non-mutating |
| 4.11.1 | Study/SRS | Bury/suspend queue exclusion BE | BE | Due/new queries exclude suspended and currently-buried cards | Implemented | 4.1.1, 4.0.2 | `lib/data/datasources/local/drift/study_scope_queries.drift`, `lib/data/datasources/local/daos/study_scope_dao.dart`, `lib/data/repositories/study_entry_repository_impl.dart`, `lib/domain/usecases/study/resolve_eligible_study_cards_usecase.dart`; `test/data/repositories/study_eligibility_bury_suspend_test.dart`, `test/domain/usecases/study/resolve_eligible_study_cards_usecase_test.dart` | TBD | Ordered eligible-id queue read (`resolveEligibleCardIds`): due→due_at order, new→sort order; suspended + currently-buried excluded (BS3/BS10-BS14). Eligibility index deferred (perf). In-session action 4.11.2 + status filter 2.17.1 separate |
| 4.11.2 | Study/SRS | In-session bury/suspend action BE | BE | Bury/suspend current card: set fields, no attempt, preserve SRS + tests | Implemented | 4.4.1, 4.0.2 | `lib/domain/repositories/study_repository.dart`, `lib/data/repositories/study_repository_impl.dart`, `lib/data/repositories/study_session_card_actions.dart`, `lib/data/datasources/local/daos/study_session_dao.dart`, `lib/domain/usecases/study/bury_study_session_card_usecase.dart`, `lib/domain/usecases/study/suspend_study_session_card_usecase.dart`; `test/data/repositories/study_session_card_action_test.dart`, `test/domain/usecases/study/bury_suspend_card_usecase_test.dart` | TBD | `buryStudySessionCard`/`suspendStudySessionCard`: transactional (upsert progress one-field + delete queue item + touch updated_at), no attempt, box/due/counters preserved, new-card default progress; bury→tomorrow-local-midnight+1s, suspend→is_suspended. Guards in_progress + unanswered queued (BS1/BS2/BS4/BS5) |
| 4.11.3 | Study/SRS | In-session bury/suspend action FE | FE | Action UI, queue removal, undo affordance | Specified | 4.11.2 | `docs/business/study-actions/bury-suspend.md` | TBD | Wire UI to BE; widget tests |
| 4.12.1 | Study/SRS | Study by tag | BE | `StudyEntryType.tag` scope queries + routes + tests | Blocked | 8.5.1 | `docs/business/tags/tag-system.md` | TBD | Requires tag subsystem promotion |

### Group 5 — Dashboard flow

| WBS ID | Flow | Function | Layer | Deliverable | Status | Depends on | Evidence/Source | Commit ID | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 5.1.1 | Dashboard | Continue Studying summary BE | BE | `loadDashboardResumeSessionSummary` (scope, progress, last active) | Implemented | 4.2.1 | `lib/domain/models/dashboard_resume_session_summary.dart`, `lib/domain/repositories/dashboard_repository.dart` (`loadResumeSessionSummary`), `lib/data/repositories/dashboard_repository_impl.dart`, `lib/data/datasources/local/drift/dashboard_queries.drift` (`dashboardResumeSession`), `lib/data/datasources/local/daos/dashboard_dao.dart` (`resumeSession`), `lib/domain/usecases/dashboard/load_dashboard_resume_summary_usecase.dart`, `lib/app/di/dashboard_providers.dart`, `test/data/repositories/dashboard_resume_summary_test.dart` | TBD | Most-recent resumable session (any scope, 30-day window) + answered/total progress via `.drift` join+aggregate; `null` when none (FE hides card, 5.1.2). |
| 5.1.2 | Dashboard | Continue Studying card FE | FE | Resume card with Continue CTA; hidden when none | Specified | 5.1.1 | `lib/presentation/features/dashboard/screens/dashboard_screen.dart`, `test/presentation/features/dashboard/dashboard_screen_test.dart` | TBD | Implement |
| 5.2.1 | Dashboard | Due-today summary BE | BE | Global due snapshot (cards due + decks with due) | Implemented (2026-06-21, redesign V1) | 3.1.1 | `lib/data/datasources/local/drift/dashboard_queries.drift`, `lib/data/datasources/local/daos/dashboard_dao.dart`, `lib/domain/models/dashboard_summary.dart`, `lib/domain/repositories/dashboard_repository.dart`, `lib/data/repositories/dashboard_repository_impl.dart`, `lib/domain/usecases/dashboard/load_dashboard_summary_usecase.dart`, `lib/app/di/dashboard_providers.dart` | TBD | Done — dedicated `DashboardSummary` read model + `dashboardDueSummary` query (`due_at IS NOT NULL AND due_at <= :now`, matching folder_queries) + `LoadDashboardSummaryUseCase` (owns the clock). **Buried/suspended exclusion deferred** (4.0.2 not read anywhere yet — consistent with folder due_count). |
| 5.2.2 | Dashboard | Today due card FE | FE | Quiet due card: due count + due-deck count (redesign — a quiet "refer", no Today study CTA) | Implemented (2026-06-21, redesign V1) | 5.2.1 | `lib/presentation/features/dashboard/screens/dashboard_screen.dart`, `lib/presentation/features/dashboard/widgets/dashboard_body.dart`, `lib/presentation/features/dashboard/viewmodels/dashboard_viewmodel.dart`, `test/presentation/features/dashboard/dashboard_test.dart` | N12, N13 | Done — `MxDueSummary` (count + decks). **Redesign divergence:** the Dashboard is a quiet refer surface, so there is **no Today study CTA / estimated-minutes** here (strong study CTAs live on Study/Progress); the original Today-CTA spec is superseded. Shortcut rows to Progress + Library added (advances 5.6.2 shortcut). |
| 5.3.1 | Dashboard | New-user empty / caught-up dashboard FE | FE | Controlled caught-up state | Implemented (2026-06-21, redesign V1) | 5.2.2 | `lib/presentation/features/dashboard/widgets/dashboard_body.dart`, `test/presentation/features/dashboard/dashboard_test.dart` | TBD | Done — `MxDueSummary.caughtUp` (all-clear) when `cardsDue == 0`; loading/error states + light/dark goldens. |
| 5.4.1 | Dashboard | Progress summary on dashboard | BE | Dashboard progress summary read model + provider wiring (due today, goal, streak) | Specified | 7.4.1 | `lib/domain/models/dashboard_progress_summary.dart`, `lib/domain/usecases/progress/load_dashboard_progress_summary_usecase.dart`, `lib/data/repositories/progress_repository_impl.dart`, `lib/app/di/progress_providers.dart`, `test/domain/usecases/progress/load_dashboard_progress_summary_usecase_test.dart`, `test/data/repositories/progress_repository_impl_test.dart`, `docs/business/engagement/dashboard-engagement.md` | TBD | Implement |
| 5.4.2 | Dashboard | Streak chip + daily-goal ring FE | FE | Streak chip (hidden when `< 1`) + daily-goal ring (hidden when goal disabled) consuming `dashboardProgressSummaryQueryProvider` | Specified | 5.4.1 | `lib/presentation/features/dashboard/screens/dashboard_screen.dart` (`_DashboardStatsRow`), `lib/presentation/features/dashboard/viewmodels/dashboard_viewmodel.dart`, `test/presentation/features/dashboard/dashboard_screen_test.dart` | TBD | Implement |
| 5.5.1 | Dashboard | Dashboard data refresh | Integration | Refresh on retry and on return from study flow | Specified | 5.1.2 | `lib/presentation/features/dashboard/viewmodels/dashboard_viewmodel.dart` (invalidate on retry) | TBD | Verify/refresh on study-flow return; add test |
| 5.6.1 | Dashboard | Recent decks + new-card highlights BE | BE | `loadDashboardDeckHighlights` (recent decks by `updated_at` with card/due/last-studied + library-wide never-studied count) | Specified | 7.4.1 | `lib/data/datasources/local/drift/progress_queries.drift` (`dashboardRecentDecks`, `dashboardNewCardCount`), `lib/data/datasources/local/daos/progress_dao.dart`, `lib/data/repositories/progress_repository_impl.dart`, `lib/domain/models/dashboard_deck_highlights.dart`, `lib/domain/usecases/progress/load_dashboard_deck_highlights_usecase.dart`, `lib/app/di/progress_providers.dart`, `test/data/repositories/progress_repository_impl_test.dart` | TBD | Implement |
| 5.6.2 | Dashboard | Recent decks section FE | FE | Recent-decks section (top 3, card/due/last-studied, deck-row → flashcard list) + Library shortcut | Specified | 5.6.1 | `lib/presentation/features/dashboard/screens/dashboard_screen.dart` (`_DashboardRecentDecksSection`), `test/presentation/features/dashboard/dashboard_screen_test.dart` | TBD | Implement |
| 5.7.1 | Dashboard | Start new learning CTA FE | FE | New-learning card with never-studied count badge → global new-cards study entry (deck/folder scope picker deferred) | Specified | 5.6.1 | `lib/presentation/features/dashboard/screens/dashboard_screen.dart` (`_DashboardNewLearningCard`), `test/presentation/features/dashboard/dashboard_screen_test.dart` | TBD | Implement |
| 5.8.1 | Dashboard | App-bar search shortcut FE | FE | ~~Dashboard search icon → `/library/search`~~ | Rejected | 3.5.1 | — | — | **Superseded by the design redesign:** Search is now a primary bottom-nav destination (`/search`, top-level), so the Dashboard no longer carries an app-bar search shortcut. No Dashboard search affordance is built. |

### Group 6 — Import flow

| WBS ID | Flow | Function | Layer | Deliverable | Status | Depends on | Evidence/Source | Commit ID | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 6.0.1 | Import | **Import type-contract & preview model (ENABLER — B3)** | BE | Pin `FlashcardImportPreview`, row-issue enum, `ImportSourceFormat` value objects + repo/use-case signatures in domain + `docs/contracts/types-catalog.md`; no parsing logic yet (lands in 6.2.x) | Implemented | 2.11.1 | `lib/domain/types/import_source_format.dart`, `lib/domain/types/import_text_separator.dart`, `lib/domain/types/flashcard_import_duplicate.dart`, `lib/domain/types/import_row_issue_type.dart`, `lib/domain/models/flashcard_import_preview.dart`, `docs/contracts/types-catalog.md`, `docs/contracts/usecase-contracts/flashcard.md`, `docs/business/flashcard/flashcard-management.md`, `test/domain/models/flashcard_import_preview_test.dart` | TBD | **Enabler (contract pinned).** Gates 6.2.1–6.9.1; reconciled the `DeckImportPreview` synonym → `FlashcardImportPreview`. Contract-only (no parse/dedup/commit logic — lands in 6.2.x). |
| 6.1.1 | Import | Import route + screen shell | FE | `/library/deck/:deckId/import` opens `DeckImportScreen` | Specified | 3.3.1 | `lib/presentation/features/flashcards/screens/deck_import_screen.dart` | TBD | Implement |
| 6.2.1 | Import | CSV parse BE | BE | Pasted-CSV parse use case + tests | Implemented | 2.11.1, 6.0.1 | `lib/domain/usecases/flashcard/parse_deck_import_csv_usecase.dart`, `lib/core/util/csv_tokenizer.dart`, `lib/app/di/flashcard_providers.dart`, `pubspec.yaml` (`csv` dep), `test/domain/usecases/flashcard/deck_import_usecases_test.dart` | TBD | `ParseDeckImportCsvUseCase` (pure, `csv`-package-backed): RFC-4180 quoting (quoted comma/newline/`""`), optional `front,back` header, skip blank lines, ≥2 cols→first-two (extras ignored, C7), <2→malformedRow. Content validation 6.2.2, separators 6.9.1. C7/C32 |
| 6.2.2 | Import | Row validation BE | BE | Per-row validation issues in preview model | Implemented | 6.2.1 | `lib/domain/usecases/flashcard/parse_deck_import_csv_usecase.dart`, `test/domain/usecases/flashcard/deck_import_usecases_test.dart` | TBD | `ParseDeckImportCsvUseCase` content validation: front/back required-after-trim (parity w/ create) → `missingFront`/`missingBack` `ImportValidationIssue` (line-numbered), row excluded from committable `rows` (C30). No max-length anywhere → `*TooLong` reserved. |
| 6.3.1 | Import | Preview FE (valid/invalid rows) | FE | In-screen preview: rows + issues + summary; clean preview enables CTA | Specified | 6.2.2 | `test/presentation/features/flashcards/deck_import_screen_test.dart` | TBD | Implement |
| 6.4.1 | Import | Transactional commit BE | BE | Insert valid rows + default SRS progress in one transaction; rollback tests | Implemented | 6.2.2 | `lib/domain/usecases/flashcard/commit_deck_import_usecase.dart`, `lib/domain/repositories/flashcard_repository.dart`, `lib/data/repositories/flashcard_repository_impl.dart` (`commitDeckImport`), `lib/app/di/flashcard_providers.dart`, `test/domain/usecases/flashcard/deck_import_usecases_test.dart`, `test/data/repositories/flashcard_repository_impl_test.dart` | TBD | `CommitDeckImportUseCase` (reject blank deckId / empty items) → `FlashcardRepository.commitDeckImport` all-or-nothing batch insert (flashcard + default progress, appended sort_order; missing deck→NotFound). Returns count. FE gate = 6.4.2/6.5.1 |
| 6.4.2 | Import | No silent partial import | Integration | Commit only proceeds on clean preview; all-or-nothing insert | Specified | 6.4.1 | `test/presentation/features/flashcards/deck_import_screen_test.dart` | TBD | Implement |
| 6.5.1 | Import | Result summary FE | FE | V1 snackbar + pop back (standalone result screen deferred) | Specified | 6.4.1 | `lib/presentation/features/flashcards/screens/deck_import_screen.dart` | TBD | Implement |
| 6.6.1 | Import | Duplicate detection BE V1 | BE | Case-insensitive front/back dup check vs file + existing deck + tests | Implemented | 6.2.2 | `lib/domain/usecases/flashcard/prepare_deck_import_usecase.dart`, `lib/domain/repositories/flashcard_repository.dart`, `lib/data/repositories/flashcard_repository_impl.dart` (`loadDeckCardContents`), `lib/app/di/flashcard_providers.dart`, `test/domain/usecases/flashcard/deck_import_usecases_test.dart`, `test/data/repositories/flashcard_repository_impl_test.dart` | TBD | `PrepareDeckImportUseCase` (skipExactDuplicates): trim+case-insensitive front back key; skip vs existing deck card (source=deck) + earlier file row (source=importFile, first kept); deck precedence; `FlashcardImportPreparation` (previewItems + skippedDuplicates). `loadDeckCardContents` repo read. I7 |
| 6.6.2 | Import | Duplicate preview FE V1 | FE | Duplicate rows surfaced in preview with skip policy | Specified | 6.6.1 | `docs/wireframes/10-deck-import.md` | TBD | Wire to BE; widget tests |
| 6.7.1 | Import | File picker entry FE | FE | File selection (UTF-8 CSV file) replacing paste-only input | Specified | 6.2.1 | `docs/wireframes/10-deck-import.md` | TBD | Needs dependency approval if picker package required |
| 6.8.1 | Import | Excel import BE | BE | First-sheet read, header toggle | Future | 6.6.1 | `docs/business/flashcard/flashcard-management.md` | TBD | Do not implement without dependency approval |
| 6.9.1 | Import | Structured text import BE | BE | Separator auto/tab/comma/colon/slash/semicolon/pipe | Implemented | 6.2.1 | `lib/domain/usecases/flashcard/parse_deck_import_csv_usecase.dart`, `test/domain/usecases/flashcard/deck_import_usecases_test.dart` | TBD | `ParseDeckImportCsvUseCase` takes `ImportTextSeparator` (default comma) → delimiter char via `CsvTokenizer`; `auto` = frequency analysis of first non-empty line, strict-highest wins, tie/none → fail closed (malformedRow issue, no rows). Same validate/dedup/commit pipeline (I8). |

### Group 7 — Progress/reporting flow

| WBS ID | Flow | Function | Layer | Deliverable | Status | Depends on | Evidence/Source | Commit ID | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 7.1.1 | Progress | Due summary query BE V1 | BE | Aggregate due-card counts (global/per-deck) excluding buried/suspended + tests | Implemented | 4.0.2, 4.11.1 | `lib/data/datasources/local/drift/progress_queries.drift`, `lib/data/datasources/local/daos/progress_dao.dart`, `lib/domain/models/due_summary.dart`, `lib/domain/repositories/progress_repository.dart`, `lib/data/repositories/progress_repository_impl.dart`, `lib/domain/usecases/progress/load_due_summary_usecase.dart`; `test/data/repositories/progress_repository_impl_test.dart` | TBD | `loadDueSummary`: per-deck grouped query (excl. suspended/currently-buried/future-due/new) + global total = sum; `DueSummary`/`DeckDueCount` models. Unblocked: deps met (4.0.2 + 4.11.1). P8 |
| 7.2.1 | Progress | Box distribution query BE V1 | BE | Card counts per Leitner box from current progress table + tests | Implemented | 7.1.1 | `lib/data/datasources/local/drift/progress_queries.drift`, `lib/data/datasources/local/daos/progress_dao.dart`, `lib/domain/models/box_distribution.dart`, `lib/data/repositories/progress_repository_impl.dart`, `lib/domain/usecases/progress/load_box_distribution_usecase.dart`; `test/data/repositories/progress_repository_impl_test.dart` | TBD | `loadBoxDistribution`: count flashcard_progress by box, zero-fill 1..8, fail-fast (IntegrityFailure) on box ∉ 1..8. `BoxDistribution` model. P9 |
| 7.3.1 | Progress | Study statistics BE V1 | BE | Session/attempt-based stats (sessions finished, answers recorded) + tests | Implemented | 4.6.1 | `lib/data/datasources/local/drift/progress_queries.drift`, `lib/data/datasources/local/daos/progress_dao.dart`, `lib/domain/models/study_statistics.dart`, `lib/data/repositories/progress_repository_impl.dart`, `lib/domain/usecases/progress/load_study_statistics_usecase.dart`; `test/data/repositories/progress_repository_impl_test.dart` | TBD | `loadStudyStatistics`: completed-session count + attempt rollup (total, forgot, correct=total-forgot, last-studied); pure read. `StudyStatistics` model. P10 |
| 7.4.1 | Progress | Progress read model BE V1 | BE | Combined progress read model + provider wiring | Implemented | 7.1.1, 7.2.1, 7.3.1 | `lib/domain/models/progress_read_model.dart`, `lib/domain/repositories/progress_repository.dart`, `lib/data/repositories/progress_repository_impl.dart`, `lib/domain/usecases/progress/load_progress_read_model_usecase.dart`, `lib/app/di/progress_providers.dart`, `test/domain/usecases/progress/load_progress_read_model_usecase_test.dart`, `test/data/repositories/progress_repository_impl_test.dart` | TBD | `loadProgressReadModel`: composes due summary + box distribution + statistics in one call, first-failure short-circuits, empty DB zero-safe. P11 |
| 7.4.2 | Progress | Progress overview read model BE | BE | Range activity (per-local-day buckets + previous-range delta), study-day streak, suspended/buried counts composed into `ProgressOverview` + tests | Specified | 7.4.1 | `lib/domain/models/progress_read_model.dart`, `lib/domain/types/progress_range.dart`, `lib/domain/usecases/progress/load_progress_overview_usecase.dart`, `lib/data/repositories/progress_repository_impl.dart`, `test/data/repositories/progress_repository_overview_test.dart` | TBD | Implement |
| 7.5.1 | Progress | Progress screen FE V1 | FE | `/progress` renders the kit-mock screen: range tabs, cards-studied chart, accuracy + delta + sparkline, box distribution, streak, card states | Specified | 7.4.2 | `lib/presentation/features/progress/**`, `test/presentation/features/progress/progress_screen_test.dart`, `test/presentation/features/progress/progress_screen_golden_test.dart` | TBD | Implement |
| 7.5.2 | Progress | Progress states FE | FE | Loading/empty/error/insufficient/partial states per kit mock, data-driven per section | Specified | 7.5.1 | `test/presentation/features/progress/progress_screen_test.dart` (states), goldens for 7 states × light/dark | TBD | Implement |
| 7.0.1 | Progress | **Card-history schema (ENABLER — B4)** | BE | `card_events` table (created/edited/reset events, FK→flashcards) + `flashcard_progress.last_reset_at` column + `study_attempts.duration_ms` column; migration v+1 + schema/migration tests; no read logic yet (lands in 7.6.1) | Implemented | 4.0.1 | `lib/data/datasources/local/drift/card_events.drift`, `lib/data/datasources/local/drift/flashcards.drift`, `lib/data/datasources/local/drift/study_tables.drift`, `lib/data/datasources/local/migrations/v7_add_card_history.dart`, `lib/data/datasources/local/app_database.dart`, `docs/database/schema-contract.md`, `docs/database/migration-contract.md`, `test/data/migrations/v7_add_card_history_migration_test.dart`, `test/data/migrations/app_database_schema_test.dart` | TBD | **Enabler shipped (schema v7).** Gates 7.6.x. Additive: `card_events`+`idx_card_events_flashcard`, `flashcard_progress.last_reset_at`, `study_attempts.duration_ms`; columns/table only, read logic lands 7.6.1. |
| 7.6.1 | Progress | Review history query BE | BE | Per-card header (preview + SRS + cumulative counters + last_reset_at + correct streak + deck/breadcrumb) and the unified activity feed merging `study_attempts` (+ `duration_ms`) with `card_events` (created/edited/reset); reset writes a `card_events` row | Specified | 7.0.1, 4.0.1 | `lib/data/datasources/local/drift/history_queries.drift`, `lib/data/datasources/local/daos/card_history_dao.dart`, `lib/data/repositories/card_history_repository_impl.dart`, `lib/domain/repositories/card_history_repository.dart`, `test/data/repositories/card_history_repository_impl_test.dart` | TBD | **Unblocked 2026-06-21: 7.0.1 (schema v7) + 4.0.1 both Implemented.** Eligible. Reads the v7 `card_events` + `duration_ms` + `last_reset_at`. |
| 7.6.2 | Progress | Card history use cases | Domain | `GetCardHistoryHeaderUseCase`, `GetCardHistoryPageUseCase`, `ResetFlashcardProgressUseCase` + DI | Specified | 7.6.1 | `lib/domain/usecases/history/**`, `lib/domain/models/card_history.dart`, `lib/app/di/card_history_providers.dart`, `test/data/repositories/card_history_repository_impl_test.dart` | TBD | Implement |
| 7.6.3 | Progress | Card history screen FE | FE | `CardHistoryScreen` mock-parity (kit screen 09, all 5 states): breadcrumb + header (box chip) + CURRENT PROGRESS card (box stepper + 6-stat grid) + unified activity feed on a left rail (attempt rows with chip/desc/box→box·mode·duration; lifecycle rows; "All events" filter; "Beginning of history") + Edit pill + Reset/Delete overflow; duration measured in study review viewmodel; route `/library/deck/:deckId/flashcards/:flashcardId/history` | Specified | 7.6.2 | `lib/presentation/features/history/**`, `lib/presentation/features/flashcards/widgets/flashcard_row_actions_sheet.dart`, `lib/app/router/route_paths.dart`, `lib/app/router/route_names.dart`, `lib/presentation/features/flashcards/routes/flashcard_routes.dart`, `test/presentation/features/history/card_history_screen_test.dart` | TBD | Suspend/unsuspend overflow + timeline-row→result deferred (4.11.x) |
| 7.7.1 | Progress | Dashboard/progress consistency | Integration | Same due/progress numbers on dashboard and progress screen | Specified | 7.5.1, 5.2.1 | `test/domain/usecases/progress/load_dashboard_progress_summary_usecase_test.dart`, `test/data/repositories/progress_repository_impl_test.dart` | TBD | Implement |

### Group 8 — Settings/app operations

| WBS ID | Flow | Function | Layer | Deliverable | Status | Depends on | Evidence/Source | Commit ID | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 8.1.1 | Settings | Settings hub FE shell | FE | `/settings` hub rendering mock sections (account row uses mock data) | Specified | 1.1.3 | `lib/presentation/features/settings/screens/settings_screen.dart` (static mock preview, `_mockAppVersion`) | TBD | Replace mock account/version data when real contracts exist |
| 8.1.2 | Settings | Hide fabricated state before release | FE | Hide/disable mock account row + mock version on Settings hub. (Dashboard dead `0 days` placeholder resolved by WBS 5.4.2 — replaced with computed streak chip hidden at 0.) | Specified | 8.1.1 | `docs/wireframes/04-settings-hub.md` + `docs/wireframes/01-dashboard.md` | TBD | Settings-hub mock state still pending; Dashboard part done |
| 8.2.1 | Settings | Learning settings BE persistence | BE | Persisted study-default settings contract + storage + tests. Contract MUST include `dailyNewLimit` (default 20) and `goalDisabledSince` from day one | Implemented | 1.1.5 | `lib/domain/entities/learning_settings.dart`, `lib/domain/repositories/learning_settings_repository.dart`, `lib/data/datasources/local/preferences/learning_settings_store.dart`, `lib/data/repositories/learning_settings_repository_impl.dart`, `lib/domain/usecases/learning_settings_usecases.dart`, `lib/app/di/{app,learning_settings}_providers.dart`, `test/domain/usecases/learning_settings_usecases_test.dart`, `test/data/repositories/learning_settings_repository_impl_test.dart` | TBD | Done (2026-06-20) — `LearningSettings` entity + `Load`/`Update` use cases over `LearningSettingsRepository`(`Impl`) + `LearningSettingsStore` (SharedPreferences, keys `learning.dailyNewLimit`/`learning.goalDisabledSince`); `dailyNewLimit` default 20 / valid 5..200 step 5 (`outOfRange`); `goalDisabledSince` nullable local `YYYY-MM-DD`; defaults + corrupt-recovery on load; centralized `sharedPreferencesProvider` in `app_providers.dart`. |
| 8.2.2 | Settings | Learning settings FE wiring | FE | Wire `/settings/learning` shell to real persisted state | Specified | 8.2.1 | `lib/presentation/features/settings/screens/learning_settings_screen.dart` (static mock preview) | TBD | Wire screen to real provider after 8.2.1 |
| 8.3.1 | Settings | Tag management BE V1 | BE | Distinct tag list/count/search + transactional rename/merge/delete over `flashcard_tags` + tests | Implemented | 2.15.1 | `lib/domain/tag/tag_validator.dart`, `lib/domain/models/{tag_with_count,merge_result}.dart`, `lib/domain/repositories/tag_repository.dart`, `lib/domain/usecases/tag/{watch_tags_with_count,rename_tag,merge_tags,delete_tag}_usecase.dart`, `lib/data/datasources/local/drift/tag_queries.drift`, `lib/data/datasources/local/daos/flashcard_tag_dao.dart`, `lib/data/repositories/tag_repository_impl.dart`, `lib/app/di/tag_providers.dart`, `test/data/repositories/tag_repository_impl_test.dart`, `test/domain/usecases/tag/tag_usecases_test.dart` | TG1/TG5/TG6/TG7/TG9/TG10 | Done (2026-06-20) — `TagRepository`(`Impl`) + `FlashcardTagDao` + `TagValidator` + 4 use cases over `Result`. `watchAllWithCount` (count desc/name asc), `rename` (no-op on equal, `ConflictFailure` on collision), `merge` (per-card de-dup via `INSERT OR IGNORE` + delete source, one txn, returns affected count), `delete` (keeps cards, returns count). Normalization centralized in `StringUtils.normalizeTag`. Search = screen filters the watched list; add/remove-to-card on flashcard editor; study-by-tag Future. |
| 8.3.2 | Settings | Tag management FE wiring | FE | Wire `/settings/learning/tags` shell to real data + operations | Specified | 8.3.1 | `lib/presentation/features/settings/screens/tag_management_screen.dart` (static mock preview), `test/presentation/features/settings/tag_management_screen_test.dart` | TBD | Wire screen to real provider after 8.3.1 |
| 8.4.1 | Settings | TTS service BE | BE | `TtsService` abstraction + platform implementation + settings storage + tests | Specified | 1.1.5 | `lib/domain/services/tts_service.dart`, `lib/domain/services/tts_playback_policy.dart`, `lib/domain/models/tts_settings.dart`, `lib/domain/models/tts_voice.dart`, `lib/domain/repositories/tts_settings_repository.dart`, `lib/domain/usecases/tts_usecases.dart`, `lib/data/services/flutter_tts_service.dart`, `lib/data/datasources/local/drift/tts_settings.drift`, `lib/data/datasources/local/migrations/v9_add_tts_settings.dart`, `lib/data/datasources/local/daos/tts_settings_dao.dart`, `lib/data/repositories/tts_settings_repository_impl.dart`, `lib/app/di/tts_providers.dart`, `lib/presentation/features/settings/providers/tts_settings_notifier.dart`, `test/domain/services/tts_settings_normalization_test.dart`, `test/domain/usecases/speak_flashcard_test.dart`, `test/data/migrations/tts_settings_migration_test.dart` | TBD | **NOT built (2026-06-20 audit)** — none of these files exist in the rebuilt tree (no `tts_*`, no `tts_settings.drift`, no v9 migration); the evidence column describes the pre-rebuild build. Self-contained (creates its own `tts_settings` table) so not blocked — just unimplemented. Implement when Settings/TTS is in scope. |
| 8.4.2 | Settings | Audio/speech settings FE wiring | FE | Wire `/settings/audio-speech` shell to real TTS settings | Specified | 8.4.1 | `lib/presentation/features/settings/screens/audio_speech_settings_screen.dart` (static mock preview); `lib/presentation/features/settings/providers/tts_settings_notifier.dart` (provider ready) | TBD | Wire screen to `ttsSettingsNotifierProvider` after 8.4.1 landed |
| 8.4.3 | Settings | Auto-play on reveal | Integration | Study-session reveal triggers TTS per settings | Specified | 8.4.1, 4.3.2 | `docs/business/tts/tts-settings.md` (study-mode views do not exist yet) | TBD | **NOT built (2026-06-20 audit)** — depends on 8.4.1 (TTS BE, unbuilt) AND the study-session reveal flow (4.3.2, blocked on 4.0.1); the cited `study_session_recall_mode_view.dart` does not exist. |
| 8.5.1 | Settings | Account settings screen V1 | FE | Replace `/settings/account` placeholder with linked/unlinked display | Specified | 1.1.3 | `lib/presentation/features/settings/screens/account_settings_screen.dart`, `lib/presentation/features/settings/widgets/account_settings_group.dart`, `lib/presentation/features/settings/viewmodels/account_settings_viewmodel.dart`, `lib/domain/models/cloud_account_link.dart`, `lib/data/datasources/local/preferences/cloud_account_store.dart`, `test/presentation/features/settings/account_settings_screen_test.dart`, `test/presentation/features/settings/account_settings_screen_golden_test.dart`, `test/data/datasources/local/preferences/cloud_account_store_test.dart` | TBD | Display-only V1; sign-in/Drive actions land in 8.6.1/8.6.2 |
| 8.6.1 | Settings | Google account linking BE | BE | Optional sign-in, account statuses, SharedPreferences link store | Specified | 8.5.1 | `docs/business/account-sync/account-sync.md` | TBD | High-risk; defer until core loop complete |
| 8.6.2 | Settings | Drive backup/restore BE | BE | AppData-scope upload/restore via platform snapshot gateways | Specified | 8.6.1 | `docs/business/account-sync/account-sync.md` | TBD | Defer; requires 8.6.1 |
| 8.6.3 | Settings | Silent token refresh + reconnect banner | BE+FE | Per-call silent Drive token refresh (no UI on ~1h expiry); interactive reconnect banner only when silent re-auth fails | Future | 8.6.2 | `docs/business/account-sync/account-sync.md` (§Token lifetime & silent refresh) | TBD | Avoids manual re-login each hour; no backend offline token |
| 8.6.4 | Settings | Auto-backup (opt-in) | BE+FE | Debounced/dirty-flag + periodic background upload-only backup, unmetered-gated, idempotent via fingerprint; never auto-restore | Future | 8.6.3 | `docs/business/account-sync/account-sync.md` (§Auto-backup) | TBD | Needs approved dep (workmanager/bg scheduler) + perf/security review; web best-effort |
| 8.6.5 | Settings | Server-API-backed sync (own DB) | BE+FE | Conflict-free multi-device sync via a backend API with its own database (record/operation-level merge) + server-mediated token exchange; replaces blob last-write-wins | Future | 8.6.2 | `docs/business/account-sync/account-sync.md` (§Design phasing & non-goals) | TBD | Deferred — product decision pending; explicitly NOT client-side CRDT/oplog. Resolves the V1 concurrent-divergence non-goal |
| 8.7.1 | Settings | Deck export CSV BE V1 | BE | CSV export of one deck (escaping, file name sanitizing) + tests | Implemented | 2.11.1 | `lib/domain/models/deck_csv_export.dart`, `lib/domain/usecases/flashcard/export_deck_csv_usecase.dart`, `lib/data/repositories/flashcard_export_writer.dart`, `lib/data/repositories/flashcard_repository_impl.dart`, `lib/domain/repositories/flashcard_repository.dart`, `lib/app/di/flashcard_providers.dart`, `test/domain/usecases/flashcard/export_deck_csv_usecase_test.dart`, `test/data/repositories/deck_export_test.dart`, `test/data/repositories/flashcard_export_writer_test.dart` | TBD | **Early priority** — only data-out path until Drive sync; V1 cut: CSV, deck scope only. `exportDeckCsv` read-only → `DeckCsvExport`; RFC-4180 escaping + filename sanitize in `FlashcardExportWriter`; empty deck → header-only |
| 8.7.2 | Settings | Deck export FE V1 | FE | Export action on deck actions sheet → share/save (`share_plus`, needs dependency approval) | Specified | 8.7.1 | `docs/business/export/export.md` | TBD | Stop-and-ask for `share_plus` approval before wiring |
| 8.8.1 | Settings | Appearance/locale settings | FE | Theme/language switches | Future | 8.2.1 | settings hub disabled rows | TBD | Do not implement without promotion |
| 8.9.1 | Settings | Bulk operations V1 | BE | Transactional bulk delete only (selected-ID snapshot, skip missing rows, cascade local data) + tests | Specified | 2.13.1 | `lib/domain/repositories/flashcard_bulk_repository.dart`, `lib/domain/usecases/bulk/delete_flashcards_usecase.dart`, `lib/data/repositories/flashcard_bulk_repository_impl.dart`, `test/data/repositories/flashcard_bulk_repository_impl_test.dart`, `test/domain/usecases/bulk/delete_flashcards_usecase_test.dart` | TBD | Implement |
| 8.9.2 | Settings | Bulk selection mode FE | FE | Long-press selection mode in flashcard list | Specified | 8.9.1 | `docs/business/bulk/bulk-operations.md` | TBD | Implement with first bulk action |

### Group 9 — Cross-cutting quality

| WBS ID | Flow | Function | Layer | Deliverable | Status | Depends on | Evidence/Source | Commit ID | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 9.1 | Quality | Route contract gate | Test | No raw route strings; `RouteNames`/`RoutePaths` only; route-order guard | Ongoing | 1.1.3 | router tests under `test/**` | TBD | Enforce on every route change |
| 9.2 | Quality | Riverpod usage gate | Test | No `ref.watch` in callbacks; watch/read/listen per state contract | Ongoing | 1.1.1 | `docs/state/state-management-contract.md` | TBD | Enforce per task |
| 9.3 | Quality | Hooks boundary gate | Test | Hooks only in approved presentation scopes | Ongoing | 1.1.1 | `lib/presentation/features/flashcards/hooks/**` | TBD | Enforce per task |
| 9.4 | Quality | Transaction safety gate | Test | Multi-table writes always transactional (import, session, finalize, future bulk/tags) | Ongoing | 1.1.5 | `test/data/repositories/study_repository_test.dart` | TBD | Enforce per task |
| 9.5 | Quality | Schema/migration gate | Test | Version bump + `onUpgrade` + migration test + schema docs per change | Ongoing | 1.1.5 | `test/data/migrations/**` | TBD | Enforce per schema change |
| 9.6 | Quality | l10n coverage gate | Test | No hardcoded user-facing strings; ARB keys per copy change | Ongoing | 1.1.4 | `lib/l10n/app_en.arb`, `lib/l10n/app_vi.arb` | TBD | Enforce per task |
| 9.7 | Quality | State-coverage gate | Test | Loading/empty/error/no-results states on every screen | Ongoing | 1.1.2 | shared `Mx*` state widgets | TBD | Enforce per screen task |
| 9.8 | Quality | Docs-code parity gate | Docs | 8-step pre-commit parity check per `CLAUDE.md` | Ongoing | 1.1.7 | `CLAUDE.md`, `docs/checklist/implementation-checklist.md` | TBD | Enforce per commit |
| 9.9 | Quality | Guard/analyzer/test gate | Test | `dart fix` + `dart format` + `flutter analyze` + targeted tests (+ guard when present) | Ongoing | 1.1.6 | `docs/checklist/implementation-checklist.md` | TBD | Enforce per task |
| 9.10 | Quality | CI status checks | Test | GitHub workflow running analyze/test on PRs | Specified | 1.1.6 | TBD (no workflow visible) | TBD | Add minimal CI workflow |
| 9.11 | Quality | doc_guard docs/process gate | Test | `node tool/doc_guard/run.mjs check`: doc path/symbol/test-ref existence, WBS hygiene (columns, status vocab, commit hashes), ARB duplicate/missing keys, schema version drift; baseline mechanism for pre-existing findings | Specified | 1.1.7 | `tool/doc_guard/run.mjs`, `tool/doc_guard/baseline.json`, `CLAUDE.md` §Verification commands | TBD | Run in every task's verification chain |
| 9.12 | Quality | doc_guard baseline burn-down | Docs | Reduce `tool/doc_guard/baseline.json` (57 findings: decision-table phantom test refs, wireframe target-widget refs, contract stale paths) to zero | Specified | 9.11 | `tool/doc_guard/baseline.json` | TBD | Fix per area when touching it; refresh with `--update-baseline` |
| 9.13 | Quality | Repo-map cold-start snapshot | Docs | `docs/_generated/repo-map.md` generated summary (schema, routes+placeholders, use cases, screens, tests) for agent session bootstrap | Specified | 9.11 | `docs/_generated/repo-map.md`, `tool/doc_guard/run.mjs` (`generate`) | TBD | Regenerate in commits that change routes/schema/use cases/screens |
| 9.14 | Quality | Golden-diff visual parity runner | Test | `python tool/golden_diff/diff.py`: pixel-diff Flutter golden vs mock shot, mismatch % + diff region; text feedback loop for no-vision agents | Specified | 1.1.6 | `tool/golden_diff/diff.py` (runner only; the per-screen golden-test gate remains Specified) | TBD | Add golden tests with each UI task (start at 4.1.3) and wire diff into its verification |
| 9.15 | Quality | Unified verify entry point — ENFORCED | Test | `node tool/verify/run.mjs`: one command, canonical chain, scope auto-detection, single summary. **Enforcement (2026-06-12, after piecemeal-verify relapse):** `--quick` legalizes the inner dev loop through the same entry; a docs/code PASS writes a content-state-bound pass-marker; the pre-commit hook (`--check-marker`) rejects commits without a matching marker and requires a code-chain marker when code is staged. Running verification commands individually is now a hard-rule violation AND technically uncommittable | Specified | 9.11 | `tool/verify/run.mjs`, `.githooks/pre-commit`, `CLAUDE.md` §Hard rules + §Verification; enforcement live-tested (commit blocked without marker → PASS → commit allowed) | TBD | New clones: `git config core.hooksPath .githooks` |
| 9.16 | Quality | Where-is feature index | Docs | `docs/_generated/where-is.md`: deterministic feature → docs/source/tests/mock-shots/WBS cross-reference (42 features; file lists resolved LIVE per generate; output linted by doc_guard); AGENTS.md fast-lookup table routes Codex to the same infra | Specified | 9.13 | `docs/_generated/where-is.md`, `tool/doc_guard/run.mjs` (`WHERE_IS` registry), `AGENTS.md` §Where to look for what | TBD | Add a registry row when a new feature area lands; regen with `generate` |
| 9.17 | Quality | Pre-commit hook + repo hygiene | Test | `.githooks/pre-commit` runs doc_guard + whitespace check on every commit (opt-in `git config core.hooksPath .githooks`, activated in this clone); `.gitattributes` normalizes line endings ending the CRLF warning flood | Specified | 9.11 | `.githooks/pre-commit`, `.gitattributes`; live-tested on the landing commit | TBD | New clones run the one-time `core.hooksPath` config (documented in hook header + `tool/README.md`) |
| 9.18 | Quality | Task prompt generator | Test | `tool/prompt_gen/run.mjs` — generates Claude Code prompts from WBS IDs embedding the full 6-step dev loop (Read→Drift→Implement→InnerLoop→DesignParity→FullVerify+Commit); supports single ID, multi-ID batch, `--phase N`, `--list`, `--next` | Implemented | 1.1.6 | `tool/prompt_gen/run.mjs`, `tool/README.md` | `7dd04e0` | Use before each task: `node tool/prompt_gen/run.mjs <WBS_ID>` |

### Group 10 — Release readiness

| WBS ID | Flow | Function | Layer | Deliverable | Status | Depends on | Evidence/Source | Commit ID | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 10.1 | Release | MVP smoke path | Integration | Create folder → deck → cards → study → finish → see progress, verified end-to-end | Specified | 2.x, 4.x, 7.5.1 | study/content flows and progress screen implemented; end-to-end script missing | TBD | Script/checklist the smoke path |
| 10.2 | Release | Android readiness | Integration | DB path, lifecycle, back behavior verified on Android | Specified | 10.1 | `lib/data/datasources/local/connection/database_connection_native.dart` | TBD | Manual verification pass + fixes |
| 10.3 | Release | Web readiness | Integration | Wasm/worker DB connection verified on web | Specified | 10.1 | `lib/data/datasources/local/connection/database_connection_web.dart` | TBD | Verify persistence + study flow on web |
| 10.4 | Release | Windows readiness | Integration | Desktop DB + layout verified on Windows | Specified | 10.1 | `lib/data/datasources/local/connection/database_connection_native.dart` | TBD | Manual verification pass + fixes |
| 10.5 | Release | Known deferred list | Docs | Maintained register of deferred/Future/Rejected scope | Specified | 1.1.7 | §6 of this file | `177c03153` | Keep updated per promotion/deferral |
| 10.6 | Release | Release acceptance checklist | Docs | Final acceptance checklist (smoke path, platforms, quality gates) | Specified | 10.1 | TBD | TBD | Author when Group 7 lands |

## 5. Next 10 Tasks (in delivery order)

> **Rebuild reset (2026-06-19):** Code deleted; rebuild from scratch. Order follows §4.1 Rebuild
> Delivery Phases — Phase 0 → 1 → 2 first. Each item = one agent prompt (≤7 coupled rows).

1. **Phase 0 · 1.1.1 + 1.1.3 + 1.1.4** — boot `main.dart`, GoRouter shell + route constants, ARB en/vi baseline. Goal: app boots.
2. **Phase 0 · 1.1.5** — Drift `AppDatabase` + migration infra + first table (folders). Then `flutter pub get`, re-baseline doc_guard, regenerate `docs/_generated/`. Goal: `tool/verify` code chain runs.
3. **Phase 1 · 1.1.2 + 1.2.1** — theme tokens + surfaces/shell widgets (`MxContentShell`/`MxCard`/`MxListTile`); goldens.
4. **Phase 1 · 1.2.2 + 1.2.3** — nav/app-bar + actions/inputs widgets; goldens.
5. **Phase 1 · 1.2.4 + 1.2.5** — state widgets + dialog/sheet shells; goldens. Goal: core widget kit complete.
6. **Phase 2A · 2.1.1 + 2.2.1 + 2.3.1 + 2.4.1** — Folder CRUD BE (create/rename/delete/move) + unit tests.
7. **Phase 2A · 2.7.1 + 2.8.1 + 2.9.1 + 2.19.1** — Deck CRUD BE + tests.
8. **Phase 2A · ~~2.11.1 + 2.12.1 + 2.13.1 + 2.15.1~~ (done — Flashcard CRUD + create-time tags BE, schema v3) → next: 2.9.1 (deck delete, now unblocked) + 3.7.1 (deck/folder card+due counts)**.
9. **Phase 2A · 3.1.1 + 3.2.1 + ~~3.4.1~~ (done — flashcard list-load BE) + 3.5.1** — remaining Library/folder-detail/search read models BE.
10. **Phase 3 · 3.1.2 + 3.2.2 + 2.1.2** — first assembled screens (Library overview, Folder detail, Create-folder dialog) on the widget kit + Phase-2A BE.

### Current frontier (2026-06-20, post overnight PRs #4–#8)

Items 1–9 above are landed (Phase 0/1 + content/tag/counts BE). The real queue now is **the three
§4.3 critical-path enablers first**, then two parallel tracks:

1. **`4.0.2`** — bury/suspend columns (smallest unblock; frees 2.17.1 / 5.2.1 / 7.1.1).
2. **`6.0.1`** — import type-contract (contract-only; opens the whole import subsystem).
3. **`2.22.1`** — folder color/icon schema (Wave-1 prerequisite for the Phase-3 FE screens).
4. **`4.0.1`** — study persistence schema + repo skeleton (largest; schema-design review first; gates
   the entire study/SRS epic). Converge with `4.5.12`.
5. Then parallelize: **Phase-3 FE waves** (§4.2, on `2.22.1`) ‖ **import BE** `6.2.1→6.9.1` (on `6.0.1`)
   ‖ **study epic** `4.1.1→…` (on `4.0.1/4.0.2`).
6. Low-risk standalone any time: **`8.7.1`** deck export CSV BE.

## 6. Deferred / Future / Rejected Register

| Item | Status | Reason / unblock condition |
| --- | --- | --- |
| Onboarding wizard (UI kit screen 01) | Rejected | V1 boots to Library; `lib/app/router/route_paths.dart` comment forbids onboarding. |
| Card history (UI kit screen 09) | Specified | Promoted 2026-06-13; `last_reset_at` shipped v6. See WBS 7.6.1–7.6.3. Suspend/unsuspend overflow + timeline-row→result remain deferred (4.11.x). |
| Tags/recent/popular search sections | Future | Requires tag subsystem promotion (8.3.1) and SharedPreferences approval. |
| Excel import | Future | Requires dependency approval (6.8.1). |
| Daily goal / reminders / engagement persistence | Future | SharedPreferences-backed; not promoted. |
| Independent Korean/English TTS setting sets | Target | Requires migration/product decision. |
| Stats screen (UI kit screen 18) | Future | Needs aggregate read model; distinct from Progress screen (7.5.1). |
| Export with metadata | Rejected | Use Drive sync for backup instead. |
| Drive sync / account linking | Specified, deferred | High-risk; after core offline loop incl. progress (8.6.x). |
| Bulk reset progress | Blocked | Requires `last_reset_at`. |

## 7. Known Risks / Review Notes

| Risk | Impact | Mitigation |
| --- | --- | --- |
| Settings screens are static mock previews | Agents may assume settings persist; they do not | 8.1–8.4 rows mark shells as Partial with BE rows Specified; wire FE only after BE lands. |
| Prior WBS over-claimed in-session bury/suspend as implemented | Wrong baseline for study-action prompts | Resolved 2026-06-10: WBS rows reset earlier; `docs/business/study-actions/bury-suspend.md` status header rewritten to Partial (schema + exclusion Current; actions Specified) in the docs-reconciliation commit. |
| Stale SRS doc claims (2026-06-10 verification) | `docs/business/srs/srs-review.md` claimed a runtime mismatch and pending columns that already shipped | Resolved 2026-06-10: srs-review header/interval notes rewritten to match verified runtime; table-driven tests pin the contract (4.6.2 Implemented). |
| Docs written against a previous project iteration (schema v10–v13, Prompts 20/45/46/47) | Agents build on phantom APIs (`ResumeStudySessionUseCase`, `DropCurrentStudyItemUseCase`, TTS DAO, progress feature) or refuse work blocked by nonexistent migrations | Reconciled 2026-06-10 across business docs/wireframes/contracts: false "Current/Implemented" claims downgraded, shipped-but-marked-pending columns corrected, phantom file refs replaced, scope-gate refs repointed to §6 of this file. Open product decisions are now explicit notes: C1 SRS demotion reachability (`srs-review.md`), `perfect` vs `initial_passed` definition (`glossary.md`), streak-pause persistence (`dashboard-engagement.md`). |
| Docs can be ahead of source | Agent may implement Future behavior accidentally | Check source + docs; respect §6 register. |
| Schema changes are high risk | Missing migration breaks existing DBs | 9.5 gate: version bump + onUpgrade + migration test + docs per change. |
| CI not visible | Cannot claim full pass from GitHub checks | 9.10 Specified; report source-level verification until CI exists. |
| Commit anchors for docs-baseline rows | History spans many commits; no single anchor | Rows 1.1.7/9.x use `TBD` by design; §10 log carries per-commit history. |
| Study epic has no persistence backbone (B1) | ~20 Group-4 rows + resume + study stats are all gated; agents may start mid-epic and stall | Promoted to explicit enabler **4.0.1** on the critical path (§4.3); all Group-4 rows now depend on it; schema-design review required before coding. |
| Bury/suspend columns missing (B2) | Status filter, due-today, due counts silently 0-out or can't be built | Promoted to enabler **4.0.2** (§4.3); consumers 2.17.1/5.2.1/7.1.1 moved to `Blocked` until columns ship. |
| Import contract deleted in rebuild (B3) | Each import row would re-invent the preview/issue contract → rework | Promoted to enabler **6.0.1** (§4.3); 6.2.1 depends on it; contract pinned in `docs/contracts/types-catalog.md` before parsing logic. |
| Latent blockers discovered mid-task (general) | Velocity loss when a row hits un-shipped schema/contract | Discipline rule (§4.3): any BE row needing un-shipped schema lists the **enabler** in `Depends on` and is `Blocked`, not `Specified`. |

## 8. Legacy WBS ID Mapping

The Commit Traceability Log (§10) rows dated before 2026-06-10 reference the **legacy** WBS IDs from the previous revision of this file. Mapping of the most-referenced legacy IDs:

| Legacy ID(s) | Legacy meaning | Current ID(s) |
| --- | --- | --- |
| 3.8, 5.1–5.10 | Deck import route + import feature | 6.1.1–6.9.1 |
| 6.1–6.6 | Global search | 3.5.1–3.5.3 |
| 4.2, 4.3 | Flashcard create/edit screens | 2.11.2, 2.12.2 |
| 9.6–9.13 | Study entry/session/resume | 4.1.x–4.5.1, 4.8.1–4.10.1 |
| 10.4, 10.8 | Recall mode / review shell | 4.5.11, 4.3.2 |
| 11.5, 11.6 | Attempt classification / finalization | 4.4.1, 4.6.1 |
| 13.2, 13.3 | Dashboard today CTA / resume card | 5.2.2, 5.1.2 |
| 1.3.3 | Library branch routes | 1.1.3, 3.3.1 |
| 19.3 | Widget tests | 9.7 |
| 20.6, 18.9 | WBS document / UI kit contract | this file, 1.1.2 |

## 9. WBS Maintenance Rules

Update this WBS when:

- A placeholder route becomes a real screen.
- A Future/Target feature is promoted to Current.
- A schema migration changes the current DB version.
- A function row's implementation status changes (BE, FE, or Integration separately).
- New docs or decision-table rows materially change scope.
- Source reveals a doc-code parity drift that affects planning.

When updating, include:

- New baseline commit.
- Changed WBS rows only.
- Evidence paths.
- Any new priority/next-task adjustment.

Status discipline:

- BE and FE status are tracked on separate rows; never mark a feature Implemented because one side exists.
- UI shells without real data wiring stay Partial (FE) with the BE row Specified.
- Implemented requires source + test/docs evidence and, where practical, a verified commit anchor.
- Never invent commit hashes; use `TBD` when history cannot be verified.
- Do not write the current WBS-update commit hash into rows; report it in the task output and §10.

### Commit tracking rule

Every commit that creates, advances, or completes a WBS work package MUST append a row to the **Commit Traceability Log (§10)** in the same commit, listing the short commit id, date, the WBS ID(s) it touches, and a one-line summary. This keeps a bidirectional link (WBS ID ↔ commit id) without bloating the feature tables with per-row commit churn.

- Use the 8-char short hash (e.g. `5fbdf96d`).
- One commit may map to multiple WBS IDs; list them comma-separated.
- Feature/source commits should be logged by the WBS update that reviews them.
- Pure WBS traceability-maintenance commits do not need their own log row.
- Pure tooling/formatting commits that touch no WBS row may be omitted.
- The "Baseline reviewed" line at the top still tracks the latest reviewed commit; the log tracks per-task history.
- Log rows dated before 2026-06-10 use legacy WBS IDs; see §8 for the mapping.

## 10. Commit Traceability Log

Append-only, newest first. Each row links a landed commit to the WBS work package(s) it advanced. See the Commit tracking rule in §9.

> **Rebuild note (2026-06-19):** Entries below are from the previous build iteration (pre-rebuild).
> They are kept for historical reference only. New commits from the rebuild will be appended above this note.

| Commit | Date | WBS IDs | Summary |
| --- | --- | --- | --- |
| `08dcb50` | 2026-06-22 | 4.1.2 | **Study `today` route + `?study_type=` override** (WP-SR1b-1, object 6). Literal `today` route (`RouteNames`/`RoutePaths` `studyToday` → `StudyScope(today, null, srsReview)`); the entry routes thread `RouteParams.studyTypeQueryParam` into `StudyEntryScreen.studyTypeRaw`; `_resolveStudyType` overrides the entry default via the new canonical `StudyType.storageValue`/`fromStorage` (`new_cards`/`srs_review`, `types-catalog` §StudyType), unrecognized → gate error. `study_session_mapper` delegates to the enum (behaviour-preserving). URL contract corrected `new`→`new_cards`. Decision **S85** (scope FE); the WP-SR1a gate row **S32→S84** (collided with a pre-existing S32 "Study session card" — so the `c5b2a25` log row's "Decision S32" now reads as **S84**). 11 gate tests. 2-reviewer fan-out folded (drift PASS; stale-label fixes). **Remaining (object 6):** WP-SR1b-2 (8-variant empty matrix), WP-SR2..SR5. |
| `c5b2a25` | 2026-06-22 | 4.1.2, 4.2.3, 4.5.3 | **Study route scaffold + entry gate** (WP-SR1a, object 6 — greenfield FE over ready BE). Top-level immersive study routes (`RouteNames`/`RoutePaths` `studyEntry`+`studySession`, `study_routes.dart` composed into `app_router.dart`, session route before the `:entryType/:entryRefId` gate). `StudyEntryController` (`@riverpod` family on `StudyScope`) resolves `ResolveStudyEntryStartUseCase` (no silent resume); auto-creates a session on `canStart` (`ResolveEligibleStudyCardsUseCase`+`CreateStudySessionUseCase`) → `ready`; `startOver` cancels+creates. `StudyEntryOutcome` union; `StudyEntryScreen` renders preparing / generic empty (blocks zero-card) / Resume-Start-over-Back / error via `AppAsyncBuilder` → `pushReplacement` on ready. `StudySessionScreen` placeholder shell. `MxIconButton.toolbar` added (guard `header_actions`). Decision S32 + S27 reword; ARB ×13 (en+vi); 8 gate tests + 4 goldens. DEFER overturned (`ddca661` plan). 3-reviewer fan-out folded (ui-parity PASS; drift fix wireframe-24 path; code-reviewer criticals: wireframe-12 Inputs `study_type` row + wireframe-24 path + navigation-ui N12 + dashboard.visual-contract today-CTA stale study-route constants corrected; startOver wiring test + autoDispose note added). WBS 4.1.2 Implemented, 4.2.3 Partial. **Remaining (object 6):** WP-SR1b (today route + `study_type` + 8-variant empty matrix), WP-SR2..SR5. |
| `d3aa162` | 2026-06-22 | 2.11.2, 2.12.2 | Card editor **loading skeleton + load-error/Retry** (WP-FL2b3b, object 5). The editor reuses the deck list stream, so loading/load-error were already reachable — fleshed to the kit `07`/`08`: loading renders `_EditorLoadingSkeleton` (skeleton breadcrumb + FRONT/BACK label+field blocks, hairline divider, Details chips; mirrors `LibraryLoadingSkeleton`) instead of a bare shell; the stream erroring / returning no detail / the edited card being gone all route to `MxErrorState` (cloud-off, `cardLoadFailed*` copy) with a **Retry** that re-subscribes (`ref.invalidate(flashcardListStreamProvider)`), the app-bar leading as the back-escape. Decision rows **C28** (reworded) + **C46** (loading); index range C1–C45→C1–C46. Wireframes 07/08 state tables + status notes; `cardLoadFailedTitle/Message` ARB (en+vi). Tests + loading/load-error goldens (light+dark). 3-reviewer fan-out (code-reviewer APPROVE; ui-parity FAIL→folded breadcrumb-during-loading blocker + replay icon + `MxStroke.hairline` divider + doc `last_updated` bumps; load-error `MxErrorState` card/tile variances documented as app-wide error-surface consistency). **Only WP-FL2b2b (Tags input) remains** before object 5 DONE. |
| `34ae424` | 2026-06-22 | — | l10n cleanup: rename `libraryRetryLabel`→`commonRetryLabel` (shared Retry across library/folder/dashboard/list error states + editor save-failed banner); 5 widget usages migrated, description generalised, copy unchanged. (No WBS row — pure rename; logged for the WP-FL2b3a code-reviewer follow-up.) |
| `6437f66` | 2026-06-22 | 2.11.2, 2.12.2 | Card editor **Saving spinner + Save-failed banner** (WP-FL2b3a, object 5). `MxPrimaryButton` gains a `loading` param (Save pill → spinner at the disabled accent/danger fill, `MxOpacity.disabled`=op:0.38); a `saving` useState guards double-submit. On failure an inline danger banner (`_SaveErrorBanner`, dangerSoft/r:14/alert+message+Retry) renders below the app bar **above the fields** (kit spec `abs:[21,95]`) and **replaces** the failure snackbar, keeping the draft. Decision rows **C29** (reworded: banner replaces snackbar) + **C45** (in-flight guard); index range C1–C41→C1–C45. Wireframes 07/08 prose + States tables corrected (snackbar→banner, spinner). `cardSaveFailedMessage` ARB (en+vi). `strokeWidth`→`MxStroke.emphasis`. Tests: in-flight spinner, save-failed banner (draft kept), retry round-trip, saving/save-failed goldens (light+dark). 3-reviewer fan-out (ui-parity PASS; drift none; code-reviewer fixes folded: index range, WBS rows, strokeWidth token, retry-round-trip test). **WP-FL2b3b (`07`/`08` loading + load-error-full) + WP-FL2b2b (Tags input) remain** before object 5 DONE. |
| `23a4193` | 2026-06-22 | 2.11.2, 2.12.2 | Card editor **Details expander** (WP-FL2b2, object 5). A `Details · Optional` row toggles optional example/pronunciation/hint `MxTextField`s (auto-opens in edit; trailing label swaps Optional→summary on open). Extended `FlashcardActionController.create/update` to forward example/pronunciation/hint + tags; `save()` passes them (blank→null) + **preserves** existing tags on edit. ARB ×6 (en+vi). **PRECEDENCE #1:** mock's deck-selector = Future (deck retargeting), single "Note" → business example/pronunciation/hint fields (built instead of mock layout). Decision row C44. Editor tests + `07` details-open golden. 3-reviewer fan-out (ui-parity PASS — PRECEDENCE #1 accepted; fixes folded: dynamic trailing label, Flexible/ellipsis overflow guard, wireframe 07/08 diagrams). **WP-FL2b2b (Tags input) + WP-FL2b3 (save/load states) remain** before object 5 DONE. |
| `583d347` | 2026-06-22 | 2.12.2 | Edit trash/delete-from-editor (WP-FL2b1, object 5). Edit-mode app bar gains an `Icons.delete_outline` action before Save (mock `08` loaded danger zone) → shared destructive `MxConfirmDialog` (`cardDelete*`) → `FlashcardActionController.delete` → `cardDeletedSnack` + pop; create mode shows no trash. Reuses the `runDeleteCard` pattern. New `cardDeleteTooltip` ARB (en+vi). Widget tests + `08` edit-loaded golden regen (trash icon). 2-reviewer fan-out (ui-parity PASS — back→title→trash→Save order; code-reviewer APPROVE, tooltip-key a11y fix folded). **WP-FL2b2 (Details expander) + WP-FL2b3 (save/load states) remain** before object 5 DONE. |
| `ce07ee1` | 2026-06-22 | 2.11.2, 2.12.2 | Card editor **screen shell** FE (WP-FL2a, object 5). Re-audit overturned the prior "needs-decision" DEFER (PRECEDENCE #2: mock `07`/`08` screen is the contract; G1 → shell). New routes `flashcardCreate` (`new`) + `flashcardEdit` (`:flashcardId/edit`) as children of the deck flashcard-list route; `flashcard_editor_screen.dart` (async loading/load-error shells) + `flashcard_editor_body.dart` (`FlashcardEditorForm`: X/back-arrow + Save app bar, breadcrumb, FRONT/BACK, front+back gating, create/edit by cardId, save→snackbar→pop, dirty-discard PopScope confirm). `runAddCard`/`runEditCard`→`pushNamed`; deleted `flashcard_card_dialog`. ARB cardDiscard×3 (en+vi). Editor tests + `07`/`08` base goldens. Rows 2.11.2/2.12.2 Specified→Implemented (shell). 3-reviewer fan-out (ui-parity PASS; fixes folded: edit back-arrow, discard-confirm built, phantom `features/flashcards` paths fixed in wireframes 06/07/08 + WBS). **WP-FL2b (Details expander + trash/delete + full `07`/`08` state matrix) remains.** |
| `94de1f4` | 2026-06-22 | 3.4.2 | Card-row SRS subtitle FE (WP-FL1, object 5). Re-audit overturned the prior "spec-unclear" DEFER: the subtitle is derivable; only the mock's 4-state status chip needs the New/Learning/Review/Mastered taxonomy the business model lacks → chip is a documented mock visual gap (PRECEDENCE #1, `srs-review.md` New/Due only). `FlashcardListDetail` += `progressById` (repo reuses `FlashcardMapper.progressFromRow`, no schema); `flashcard_tile` title `{front} — {back}` + SRS meta (`New · not studied` / `Box N · due in Xd` / `· due today`, injected now, chevron 20px); ARB ×3 (en+vi); decision row C43. BE read-model test + isolated tile goldens (new/due-in/due-today) + screen golden regen. 3-reviewer fan-out (ui-parity PASS; docs-drift PASS; code-reviewer fixes folded). **Object 5 NOT done — WP-FL2a (editor screen shell) remains.** |
| `c16ea0a` | 2026-06-22 | 3.4.2 | Persistent flashcard-list search dock FE (WP-D2, object 4 re-audit). Re-audit found flashcard-list search shipped as an app-bar swap (`FlashcardListSearchAppBar` + `FlashcardSearchActive` toggle) but kit `06` Loaded BASE tree ships a **persistent** `search-dock` (always present with cards, removed in reorder) — unlike the toggle docks of objects 1–2. Per PRECEDENCE #2, rebuilt: `FlashcardListSearchDock` in the `bottomNavigationBar` slot (gated `totalCount>0 && !reordering`); removed the app-bar search icon + the dead `FlashcardSearchActive` notifier (filtering keys on `flashcardSearchQueryProvider`). Regenerated 6 goldens; added dock present/absent tests. 3-reviewer fan-out (ui-parity PASS; code-reviewer APPROVE + fixed stale `features/flashcards` paths; docs-drift: fixed decision N16 breadcrumb hidden-in-reorder). Pre-existing FAB-in-empty gap logged. **Object 4 re-audit-confirmed DONE** (container 3.4.2 + WP-D1 due badge + WP-D2 dock). |
| `db3d948` | 2026-06-22 | 2.19.2 | Move-sheet picker goldens (WP-FD11, object 2). Closed ui-parity Gap #1: the move-sheet state (kit `04-folder-detail--move-sheet`) had no golden. New `move_picker_golden_test.dart` goldens the deck + folder move pickers (light+dark, 390×780), each covering 4 row variants (selectable / nested-path-subtitle / current-parent-check / blocked-reason-subtitle). ui-parity-checker PASS (coverage closed). Bundled DEFER logged: kit picker restyle (radio + "Move here" + per-destination `MxIconTile` icons) needs folder/deck color+icon in the move-target models — one deferred refinement. **Object 2 (Folder detail) re-audit-confirmed DONE** (FD1–FD5, FD9, FD10 search-dock, FD11 move-goldens; FD6/FD7/FD8 DEFERred). |
| `ecbd6cd` | 2026-06-22 | 3.2.2 | Folder-detail search **bottom-dock** FE (WP-FD10, object 2 re-audit). Re-audit found folder-detail search shipped as an app-bar swap (`FolderDetailSearchAppBar`+Cancel) but kit `04` Search-empty spec defines a flat bottom `search-dock` (regular title+sort+overflow app bar retained, FAB removed) — same class as Library WP-L10. Per PRECEDENCE #2 (visual→mock), rebuilt: `FolderDetailSearchDock` (surface fill + top hairline) hosts the provider-synced autofocused `FolderDetailSearchField`, mounted in the `bottomNavigationBar` slot; app-bar `Icons.search` toggles search on/off (early-return `_toggleSearch`); deleted `FolderDetailSearchAppBar`. Regenerated 2 search-no-results goldens; added dock present/absent + toggle-exit tests (folder + library tests pass). Docs: wireframe 05 + loop-plan FD10. 3-reviewer fan-out (ui-parity: PASS; docs-drift: 0 new; code-reviewer: minHeight-floor nicety dropped as redundant — dock ≥68dp naturally). Kept provider-synced field (not shared `MxSearchDock`, onChanged-only) for the no-results Clear CTA sync. Object 2 NOT yet done — WP-FD11 (move-sheet picker goldens) remains. |
| `5c16d05` | 2026-06-22 | 3.1.2 | Library search **bottom-dock** FE (WP-L10, object 1 re-audit). TRUST-POLICY re-audit of object 1 found the Search state diverged from kit `03` spec §State: Search (the `search-dock` node: flat bottom bar, `border-t:1px divider`, FAB removed, regular Library+sort app bar retained) — the code shipped an app-bar swap (`LibrarySearchAppBar`+Cancel). Per PRECEDENCE #2 (visual→mock) + IA-redesign (bottom dock), rebuilt: new `LibrarySearchDock` (surface fill + top hairline hosting the autofocused `LibrarySearchField`) mounted in the `bottomNavigationBar` slot while searching; app-bar `Icons.search` toggles search on/off (early-return `_toggleSearch`); deleted dead `library_search_app_bar.dart`. Regenerated 2 search + 2 search-no-results goldens; added dock present/absent + toggle-exit widget tests (21 pass). Docs parity: visual-contract rev.3, `02-library` wireframe, `navigation-flow`, loop-plan + stale-comment cleanups. 3-reviewer fan-out (ui-parity: PASS; code-reviewer + docs-drift: 9 stale `LibrarySearchAppBar` refs cleaned same commit). Activation bridge: app-bar search icon kept until app-shell bottom-nav (kit `05`) lands. |
| `e6e6024` | 2026-06-21 | 2.14.2 | Card reorder mode FE (WP-FL3, object 5) — deck overflow kebab → `flashcard_deck_overflow_sheet` (Reorder cards / Delete deck); Reorder cards enters reorder mode via `FlashcardReorderActive`: X / "Reorder · {deck}" / Done (`MxPrimaryButton` pill) app bar, `{n} CARDS · DRAG TO REORDER` overline + hint, `ReorderableListView` (`ValueKey('flashcard_reorder_list')`, `buildDefaultDragHandles:false` + trailing `ReorderableDragStartListener`); drop persists via `ReorderFlashcardsUseCase` (`runReorderCards`). 5 ARB keys (en+vi). Widget tests (enter/handles/Done/X-exit) + reorder golden (light+dark). Decision C33/C34. 3-reviewer fan-out (code-reviewer + ui-parity: Done icon→pill, rendered hint sentence; docs-drift: wireframe line 21 + sheet registered in wireframe 25). Wireframe handle note leading→trailing. **Object 5 DONE** (FL1 SRS-state-mapping + FL2 editor-screen remain DEFERred owner decisions). |
| `f7152d1` | 2026-06-21 | 2.13.2 | Card delete FE verified + flipped Specified→Implemented (WP-FL4, object 5). `runDeleteCard` (long-press → `MxConfirmDialog` destructive → `FlashcardActionController.delete` → snackbar) was already built; added the missing FE affordance widget test (long-press → confirm → cancel keeps card); BE cascade is C6-tested. Corrected phantom `features/flashcards/...` WBS paths → real `features/decks/`. |
| `71da748` | 2026-06-21 | 3.4.1, 3.4.2 | Flashcard-list overline due badge (WP-D1, object 4 Deck-detail) — `FlashcardListDetail.dueCount` = active due over the full deck (F13 exclusion; search/filter-invariant), computed in `watchFlashcardList` via `_matchesStatus(due)`; `flashcard_list_body` overline renders a SOLID `{m} due` pill (kit 06 spec) when `>0`. Decision row C42; repo + widget tests + `loaded-due` golden. 3-reviewer fan-out (code-reviewer: C41→C42 collision + buried-card test; ui-parity: badge soft→solid per spec, overline weight/tracking deferred as token gap; docs-drift: collision confirmed). Object 4 DONE. |
| `ffebde7` | 2026-06-21 | 3.2.2, 3.7.1 | Folder-detail deck "last studied" line (WP-FD9) — `folderDeckSummaries` gains `last_studied_at` = `MAX(study_session_items.answered_at)` (correlated subquery, no count fan-out); `DeckSummary.lastStudiedAt`; new pure `relativeTimeFrom` bucket (`core/util/relative_time.dart`) + 5 ARB keys (en+vi); `deck_tile` renders `{n} cards · last {time} ago` (compact, matches mock 04). Read-time approach — no persistent `flashcard_progress.last_studied_at` column (schema-contract notes supersession; srs-review/study.md persistent-column refs flagged pre-existing drift in loop-deferred for an srs-reviewer pass). Decision row D13. BE/bucket/tile tests + studied golden (light+dark). 3-reviewer fan-out (code-reviewer: isUtc + justNow + wireframe fixes; docs-drift: wireframe 245 + schema note; ui-parity CONDITIONAL PASS — per-deck icon/color + due-badge fill are pre-existing gaps, deferred). |
| `ccc8c86` | 2026-06-21 | 2.19.2 | Deck move FE (WP-FD5b) — completes 2.19.2 (BE+FE). Deck action sheet `DeckAction.move` + Move row → `runDeckActions` loads targets (`LibraryActionController.deckMoveTargets`) → `showDeckMovePicker` (new `deck_move_picker_sheet.dart`, tap-to-select, blocked/current disabled, no Library-root) → `moveDeck` → snackbar. Dead-end guard (`deckMoveNoTargets`). `folder_failure_message` maps `folder_contains_subfolders`. 5 ARB keys (en+vi). Behavior widget tests. Variance: tap-based (not kit radio+confirm); per-folder picker icons deferred for both pickers (need icon/color on move-target read models). 3-reviewer fan-out (code-reviewer: failure mapping + all-blocked guard + doc comment; ui-parity: CONDITIONAL PASS, icon gap documented; docs-drift: clean). 2.19.2 → Implemented. |
| `f3ab11f` | 2026-06-21 | 2.19.2 | Deck move-targets BE read path (WP-FD5a) — `DeckMoveTarget` model + `DeckMoveBlock.lockedToSubfolders`, `FolderRepository.getDeckMoveTargets({deckId})` (all folders annotated `isCurrentParent`/`block`, no Library-root, subfolders-locked blocked, NotFound on missing deck), `GetDeckMoveTargetsUseCase` + DI — mirrors `getFolderMoveTargets`/`_deckParentGuard`. Verified deck move is in the kit mock. Tests: repo annotation + use-case forwarding. Decision rows D11/D12; deck.md/deck-repository/wireframe-05/WBS-2.19.2 docs. 2-reviewer fan-out (code-reviewer: added D11/D12 + contract no-op note + WBS row; docs-drift-detector: wireframe/repo-note/decision-row). FE (WP-FD5b: deck-sheet move action + picker) remains. |
| `3559328` | 2026-06-21 | 3.7.1 | Library read-model enrichment — added `FolderSummary.newCount` (active cards never yet studied: `due_at IS NULL` + F13 suspended/buried exclusion; stricter than the study new-queue) + `mastery` (double?, `AVG(COALESCE(box_number,1))/SrsBox.max` over all subtree cards incl. suspended/buried, null when empty) via the `rootFolderSummaries`/`childFolderSummaries` subtree CTE (`new_count` + `avg_box` columns), mapped in `folder_repository_impl._summary`. No schema. Tests for newCount-excludes-suspended/buried + mastery mean-box + empty→null. Decision rows F23/F24. FE wiring (mastery bar + new badge) is WP-L8. 2-reviewer fan-out: code-reviewer (fixed false new-queue-parity claim → "unseen cards", added F23/F24 + buried test, documented mastery corpus scope); docs-drift-detector (fixed stale loop-deferred + FolderWithCount→FolderSummary phantom). |
| `577faf4` | 2026-06-21 | 3.7.1, 4.0.2 | Folder/deck/dashboard due-count F13 fix — drift-check found `due_count` queries did not exclude suspended/currently-buried cards despite the columns shipping (4.0.2) and F13 + bury-suspend.md §238 mandating it. Applied `COALESCE(is_suspended,0)=0 AND (buried_until IS NULL OR buried_until <= :now)` to all four `folder_queries.drift` queries + `dashboardDueSummary`, mirroring `study_scope_queries.drift`; expired bury counts as due, `cardCount` still includes them. Tests: folder exclusion across root/child/subtree/deck summaries + dashboard `loadSummary`. Removed stale "trivially satisfied until columns ship" phrasing from F12/F13 rows, folder_summary/deck_summary docs, drift comments, folder-repository contract. 2-reviewer fan-out (code-reviewer caught the dashboard-query miss + child/deck test gap; docs-drift-detector caught wbs 3.7.1 + folder-repository stale notes). |
| `28bc4bed` | 2026-06-21 | 2.23.1 | Content sort — per-scope state + sheet icons (UX fixes). (1) Sort is no longer one global pref: `librarySortProvider`/`librarySortModeProvider` became a **family keyed by scope** (`library` / `folder:<id>` / `deck:<id>`, via `sortScope*` helpers), persisted per scope under `library.sort.<scope>` (`ContentSortStore`/`ContentSortRepository` now take a scope). Each object keeps its own sort — choosing on a folder no longer bleeds into a deck. (2) Sort-sheet rows gain a leading mode glyph (`contentSortModeIcon`: `format_list_numbered` / `sort_by_alpha` / `schedule`) so the sheet no longer looks empty. Screens pass their scope to `_openSort` + the body sort. Tests assert the leading icons; no golden change (screens render manual by default; sheet has no golden). Docs: storage-boundaries, types-catalog, wireframes 02/05/06/25, visual-contract updated global→per-scope. |
| `48247da4` | 2026-06-21 | 2.23.1 | Content sort — expose the control on the remaining screens (Folder detail, Flashcard list) so the user can actually sort everywhere, completing 2.23.1. Extracted the shared `sortByContentMode<T>` core (`content_sort.dart`; `sortLibraryFolders` now delegates). Folder detail: `swap_vert` app-bar icon → shared sheet; body sorts subfolders (name/createdAt) + decks (`deck.name`/`deck.createdAt`). Flashcard list: `swap_vert` icon; body sorts cards (`front`/`createdAt`). Both reuse the same global `library.sort` pref + `_openSort`. Widget tests assert the sort icon is present (decks mode / loaded); folder_detail + flashcard_list goldens regenerated (added icon). Flip 2.23.1 Partial→Implemented. |
| `da3533a8` | 2026-06-21 | 2.23.1 | Content sort — foundation + Library slice. New shared `showContentSortSheet` (MxBottomSheet; Manual/Name/Newest rows + active check) + global pref `library.sort` behind `ContentSortRepository` (domain) → `ContentSortRepositoryImpl` → `ContentSortStore` (prefs), wired via `contentSortRepositoryProvider` (DI) and `librarySortProvider`/`librarySortModeProvider` (presentation). `contentSortModeFromToken` (domain) parses tokens (unknown/deferred→manual). Library: `swap_vert` icon (was disabled) opens the sheet; body applies `sortLibraryFolders` (manual=DB order, name=case-folded A→Z, newest=created_at desc) presentation-side — no `.drift`/repo change. `lastStudied` deferred (no last-studied aggregate read model). 5 ARB keys (en+vi). Unit + sheet-widget tests; library goldens regenerated (enabled sort icon). Docs: types-catalog, storage-boundaries §Content sort, wireframe 02 §Sort options (resolves enum-vs-wireframe drift) + 25 (sort sheet Current), visual-contract. Fixed an architecture-guard breach (presentation→data import) by routing through the domain repository. **Folder detail / Deck / Flashcard wiring still pending (2.23.1 In progress).** |
| `9561eb69` | 2026-06-21 | 3.1.3, 3.2.2, 3.4.2 | Unify the root crumb across the whole Library hierarchy: the nested breadcrumb (Folder Detail, Flashcard List) now starts with the same `🏠 Root` marker as the root anchor instead of the text `Library`. Added optional `MxBreadcrumbItem.icon` (rendered `MxIconSize.sm`, inherits crumb colour); `buildLibraryBreadcrumb` renames `libraryLabel`→`rootLabel` and gives the first crumb `Icons.home_outlined`. `LibraryRootAnchor` reverts to reusing `buildLibraryBreadcrumb` (single current `Root` crumb) + count, so the `🏠 Root` rendering has one source. Callers (`folder_detail_screen`, `flashcard_list_screen`) pass `libraryRootLabel`. Breadcrumb unit test asserts root glyph + `Root` labels; goldens regenerated (folder_detail decks/subfolders/empty, flashcard_list loaded/empty, library_overview loaded — light+dark). Docs: navigation-flow §Breadcrumb trail, wireframes 05 + 06. No new ARB key (reuses `libraryRootLabel`). |
| `b204046e` | 2026-06-21 | 3.1.3 | Refine the Library root anchor: show a home glyph (`Icons.home_outlined`) + localized `Root` label (`libraryRootLabel`, en `Root` / vi `Gốc`) + trailing `{n} folders` count, instead of reusing the `Library` breadcrumb crumb. `LibraryRootAnchor` no longer depends on `MxBreadcrumb`/`buildLibraryBreadcrumb`; uses `MxText` + `MxIconSize.md` tokens. Widget test asserts the icon + `Root` label + count; loaded goldens (light+dark) regenerated. New ARB key `libraryRootLabel` (en+vi). Docs: visual-contract (§Scope Decision + §State Matrix `03a`) + wireframe 02 updated. |
| `472fbca8` | 2026-06-21 | 1.1.8 | Fix bootstrap binding init: `_setupSystemChrome` called `SystemChrome` platform channels before the binding existed → "Binding has not yet been initialized" (a latent bug the old empty zone handler swallowed; now logged SEVERE by 1.1.8). Added `WidgetsFlutterBinding.ensureInitialized()` as the first statement inside the guarded zone (same zone as `runApp`), before any platform-channel use, so edge-to-edge + orientation-lock actually apply and the uncaught error is gone. |
| `ce24696b` | 2026-06-21 | 1.1.5 | Fix local-store version skew blocking app launch on web. A pre-rebuild dev store held v6 study objects with `user_version` ≤ 5, so `onUpgrade` re-ran `migrateV5ToV6` → `CREATE INDEX idx_study_sessions_resumable` failed (`already exists`), aborting DB open before the UI loaded. Migration chain is correct; root cause is a stale store from the 2026-06 schema renumber. Added `AppConstants.localStoreGeneration` (=2) embedded in the store name via `AppConstants.guestDatabaseStore` (`memox_guest_g2`); web + native connections use it, so drift opens a fresh store (`onCreate` at v7) and abandons the incoherent one (pre-release, no production data). Docs: storage-boundaries §Local store generation, schema-contract isolation table, migration-contract (skew note + idempotent-step ban). Test `test/core/constants/app_constants_test.dart`. No schema/migration change. |
| `da3d3be1` | 2026-06-21 | 1.1.8 | Observability / logging config (foundation): new `lib/core/logging/log_config.dart` (`MxLog.init` — root `package:logging` → one shared `Talker` sink; level `kReleaseMode ? INFO : FINE`; console only in debug; in-memory history always; level mapping SEVERE/WARNING/INFO/FINE/lower → Talker error/warning/info/debug/verbose). Rewired `app_bootstrap.dart`: `MxLog.init` + app-boot INFO log; the previously **empty** `runZonedGuarded` handler now logs `uncaught_zone_error` SEVERE; `FlutterError.onError` + `PlatformDispatcher.onError` log SEVERE; root `ProviderScope` gains `TalkerRiverpodObserver`. 7 unit tests (`test/core/logging/log_config_test.dart`). Docs: observability-contract.md updated to as-built (Talker = Current sink; persistent file writer + in-app viewer screen Deferred). New WBS row 1.1.8 (Implemented). No schema/l10n/dep change (logging/talker deps pre-existed). |
| `f959fc82` | 2026-06-21 | 3.1.3 | Library root anchor FE: new `LibraryRootAnchor` docked under the app bar on the loaded-with-folders Library Overview — a single non-tappable `Library` breadcrumb crumb (reuses `MxBreadcrumb`/`buildLibraryBreadcrumb` with empty ancestry) + a `{n} folders` count (`libraryFolderCountHeader`), giving the root the same orientation dock nested Library screens carry. Gating matches the FAB (`!searching && hasFolders`); hidden in search/loading/empty/error. Widget tests (anchor present + count in loaded; absent in empty/loading/search) + loaded goldens (light+dark) regenerated. No schema/BE/l10n-key change. Owner-requested variance from kit `03a` documented in `docs/design/screens/library-overview.visual-contract.md` (§Scope Decision + §State Matrix) and `docs/wireframes/02-library.md`. New WBS row 3.1.3 (Implemented). |
| `653cf6fc` | 2026-06-21 | 2.17.1 | Flashcard status filter BE: `FlashcardStatusFilter` (all/active/due/suspended/buried) threaded through `WatchFlashcardListUseCase` → `FlashcardRepository.watchFlashcardList`; state derived from `flashcard_progress` (no row = new active card) via new `FlashcardDao.progressForFlashcards`, `now` read once per emission from injected clock; active excludes suspended+currently-buried (expired bury active), due = active AND due_at<=now; composes with search+tags, totalCount filter-independent. Boundary semantics match 4.11.1 queue exclusion. C36/C37 + BS8/BS9 tests. Docs: bury-suspend.md, usecase + repository contracts, decision tables, backlog. Flip 2.17.1 Specified→Implemented (unblocked: deps 2.15.1 + 4.0.2 met). |
| `d5b899f8` | 2026-06-21 | 4.5.8 | Fill mode BE: extended `TypedAnswerStudyModeStrategy.evaluate` with `hintUsed` + `markCorrect` flags. Trim-only case-sensitive strict match → `perfect`; `markCorrect` override (on a wrong answer) and `hintUsed` taint both cap at `recovered` (passing-but-imperfect, never perfect); else `forgot`. Backward-compatible signature (new flags default false). Grading flows through the one-terminal-attempt 4.4.1 path; `recovered` keeps the card in its box at finalization. 6 evaluator tests. Docs: study-flow.md (fill row + TypedAnswer strategy bullet), study-srs.md (S68/S69/S72 BE test refs), wbs.md (4.5.8 Specified→Implemented + §10), backlog. Flip 4.5.8 Specified→Implemented. |
| `cda2bab1` | 2026-06-21 | 4.5.6 | Guess mode BE: `GuessStudyModeStrategy.buildOptions(targetId, targetBack, pool, random?)` builds the multiple-choice set — the correct option (target's own back, `isCorrect=true`) + up to `optionCount`(5)−1 distinct distractor backs from the session's other cards, shuffled with an injectable seeded `Random` (deterministic for tests). Skips the target card, blank-after-trim backs, and trimmed-duplicate backs (never a second correct answer / two identical options); degrades to a smaller set on small pools instead of throwing. New `GuessOption` freezed value object (`flashcardId`/`back`/`isCorrect`). Grading stays the binary one-terminal-attempt 4.4.1 path. 5 builder tests. Decision S47 ref + new S80-S83. Flip 4.5.6 Specified→Implemented. |
| `600eb1c3` | 2026-06-21 | 6.9.1 | Structured-text import BE: `ParseDeckImportCsvUseCase.call` now takes `ImportTextSeparator` (default comma) → delimiter char via `CsvTokenizer`; `auto` = frequency analysis of first non-empty line (strict-highest wins; tie/none → fail closed: malformedRow issue + no rows, I8). Same validate/dedup/commit pipeline. 6 new tests (tab/semicolon/pipe explicit, auto-comma, auto strict-win, auto tie + no-separator fail-closed). Flip 6.9.1 Specified→Implemented. |
| `10108a4d` | 2026-06-21 | 6.4.1 | Transactional commit BE: `CommitDeckImportUseCase` (reject blank deckId → ValidationFailure(empty); reject empty previewItems → ValidationFailure(insufficientContent)) → `FlashcardRepository.commitDeckImport` — all-or-nothing batch insert in one `runInTransaction`: each row → flashcard + default progress (box 1/due NULL/zero), appended sort_order after existing; missing deck→NotFound, write error rolls back→Storage; returns count. DI provider. 3 usecase + 3 repo tests (+ fake stubs). Flip 6.4.1 Specified→Implemented. |
| `f2131482` | 2026-06-21 | 6.6.1 | Duplicate detection BE: `PrepareDeckImportUseCase` (skipExactDuplicates) — trim+case-insensitive front back key; over a clean preview, skip rows matching an existing deck card (source=deck) or an earlier kept file row (source=importFile, first kept), deck precedence; returns `FlashcardImportPreparation` (previewItems + skippedDuplicates). New `FlashcardRepository.loadDeckCardContents` read + impl. DI provider. 5 usecase + 2 repo tests (+ fake stubs). Wired I7. Flip 6.6.1 Specified→Implemented. |
| `257d68a7` | 2026-06-21 | 6.2.1, 6.2.2 | Refactor: swap the hand-rolled RFC-4180 tokenizer for the `csv` pub package via a new `lib/core/util/csv_tokenizer.dart` wrapper (user-approved dependency — fewer latent parser bugs; parser kept OUT of the domain layer). Added `csv: ^6.0.0` to pubspec. Domain layer keeps header-drop / blank-skip / C7 / required-after-trim validation; `lineNumber` now the 1-based record index (was physical line). Behavior-preserving except embedded-newline line numbering; 14 tests still green (CRLF-in-quote line expectation 3→2). No WBS status change (both already Implemented). |
| `59275fc0` | 2026-06-21 | 6.2.2 | Row validation BE: extended `ParseDeckImportCsvUseCase` with per-row content validation — front/back required-after-trim (parity with manual create) → `missingFront`/`missingBack` line-numbered `ImportValidationIssue`; invalid row excluded from committable `rows` (decision C30). No max-length exists system-wide so `*TooLong` stays reserved. 3 new tests (missingFront/missingBack/both). Flip 6.2.2 Specified→Implemented. |
| `1b4eafc0` | 2026-06-21 | 6.2.1 | CSV parse BE: `ParseDeckImportCsvUseCase` (pure, on the 6.0.1 `FlashcardImportPreview` contract) — RFC-4180 field parsing (quoted comma/newline/`""`-escape), optional `front,back` header drop, blank-line skip, ≥2-col→first-two (extras ignored per C7), <2-col→malformedRow issue; `now`-free pure transform + DI. 8 tests. Wired C7/C32 test refs. Content validation (6.2.2) + separators (6.9.1) deferred. Flip 6.2.1 Specified→Implemented. |
| `f04d038f` | 2026-06-21 | 7.0.1 | Card-history schema enabler (schema v6→v7, additive — pre-authorized): new `card_events.drift` (`card_events` + `idx_card_events_flashcard`, FK→flashcards cascade); `flashcard_progress.last_reset_at` + `study_attempts.duration_ms` nullable columns; `v7_add_card_history.dart` migration (createTable + 2 addColumn) wired into app_database (include + currentSchemaVersion 7 + onUpgrade step). schema-contract + migration-contract synced (v7, pending→done). Migration test (v6→v7 reduce/migrate/cascade) + schema test (v7 + 4 new cases). No read logic (lands 7.6.1). Flip 7.0.1 Specified→Implemented; unblocks 7.6.1. |
| `690e9d34` | 2026-06-21 | 8.7.1 | Deck export CSV BE (rebuilt post-reset): `DeckCsvExport` model; `FlashcardExportWriter` (RFC-4180 `escapeCsvCell`, `buildCsv` header+rows, `sanitizeFileName` path-sep/unsafe/control→_, collapse, deck_{id} fallback); `FlashcardRepository.exportDeckCsv` read-only (deck order, empty→header-only, missing→NotFound); `ExportDeckCsvUseCase` + DI. 3 test files (writer/repo/usecase). Corrected stale export.md status + phantom evidence path. Flip 8.7.1 Specified→Implemented. |
| `5d5843ca` | 2026-06-21 | 4.5.10 | Daily new limit BE: `StudyEntryRepository.dailyNewLimit` (20); `study_scope_queries.drift` `newCardItemsUsedInWindow` (count new_cards session items in local-day window, cancelled incl.) + `StudyScopeDao.newCardsUsedInWindow`; `resolveEligibleCardIds` trims new-card ids to remaining quota (`_capByDailyNewLimit`, local-day in Dart); review unaffected. 6 tests. Decision S77–S79. Flip 4.5.10 Specified→Implemented. |
| `d9747194` | 2026-06-21 | 7.4.1 | Progress read model BE: `ProgressReadModel` model (dueSummary+boxDistribution+statistics); `ProgressRepository.loadProgressReadModel` composes the three reads (first-failure short-circuits, empty DB zero-safe); `LoadProgressReadModelUseCase` + DI. 1 usecase + 2 repo tests. Wired P11 ref. Flip 7.4.1 Specified→Implemented. |
| `01f87bbb` | 2026-06-21 | 7.3.1 | Study statistics BE: `progress_queries.drift` `completedSessionCount` + `attemptStatistics` + `lastAttemptTime` (added study_tables.drift import so attempt/session column types resolve); `ProgressDao.completedSessions`/`attemptStats`; `ProgressRepository.loadStudyStatistics` (correct=total−forgot, last-studied, pure read); `StudyStatistics` model; `LoadStudyStatisticsUseCase` + DI. 2 tests. Wired P10 ref. Flip 7.3.1 Specified→Implemented. |
| `086db3d8` | 2026-06-21 | 7.2.1 | Box distribution query BE: `progress_queries.drift` `boxDistribution` (count flashcard_progress by box_number); `ProgressDao.boxCounts`; `ProgressRepository.loadBoxDistribution` (zero-fill 1..8 via SrsBox.min/max, fail-fast IntegrityFailure on box∉1..8); `BoxDistribution` model; `LoadBoxDistributionUseCase` + DI. 3 tests. Wired P9 test ref. Flip 7.2.1 Specified→Implemented. |
| `aa77fb90` | 2026-06-21 | 7.1.1 | Due summary query BE: new `progress_queries.drift` `dueCountsByDeck` (per-deck due counts excl. suspended/currently-buried/future-due/new, predicate-identical to 4.11.1 queue); `ProgressDao`; `ProgressRepository.loadDueSummary` (global total = sum of per-deck); `DueSummary`/`DeckDueCount` models; `LoadDueSummaryUseCase` + new `progress_providers.dart` DI. 3 tests. Wired P8 test ref. Unblocked (deps 4.0.2+4.11.1 met): Blocked→Implemented. |
| `648a7c11` | 2026-06-21 | 4.11.2 | In-session bury/suspend action BE: `StudyRepository.buryStudySessionCard`/`suspendStudySessionCard` + shared `_applyCardAction` (validate in_progress + unanswered queued card → transactional `removeCardFromSession`: upsert progress one-field preserving box/due/counters, delete queue item, touch updated_at; NO attempt). Bury→tomorrow-local-midnight+1s (Dart-local), suspend→is_suspended; new card → SRS-safe default progress. `StudySessionDao.itemBySessionAndFlashcard` + `removeCardFromSession`; logic extracted to `StudySessionCardActions` collaborator (keeps repo impl within line budget). 2 use cases + DI. 10 repo + 2 usecase tests; BS1/BS2/BS4/BS5 refs. Corrected stale 4.11.2 evidence paths. Flip 4.11.2 Specified→Implemented. |
| `afffed84` | 2026-06-21 | 4.11.1 | Bury/suspend queue exclusion BE: new `study_scope_queries.drift` (6 ordered eligible-id queries: deck/folder/today × due/new) excluding suspended + currently-buried (mirrors 4.1.1 counts); `StudyScopeDao`; `StudyEntryRepository.resolveEligibleCardIds` (due→due_at order, new→active-by-sort_order); `ResolveEligibleStudyCardsUseCase` + DI. 8 repo + 1 usecase tests. Wired BS3/BS10-BS14 test refs; bury-suspend.md queue-exclusion now Current (index deferred). Flip 4.11.1 Specified→Implemented. |
| `c4a0e5c0` | 2026-06-21 | 4.7.1 | Result summary BE: `StudySessionResult`/`StudySessionResultItem` models (total/answered/forgot/passed getters); `StudyRepository.loadStudySessionResult` (header + ordered flashcard-joined items + terminal `AttemptResult` per item via the V1 last-attempt classifier shared with finalization; unanswered→null; missing→NotFound; empty→ValidationFailure); `StudySessionMapper.toResultItem`; `LoadStudySessionResultUseCase` + DI; 6 tests. Flip 4.7.1 Specified→Implemented. |
| `1ca3e22d` | 2026-06-21 | 2.22.1, 2.17.1, 7.0.1 | **WBS hygiene (no code).** Removed a duplicate `2.22.1` row (stale `Specified` copy mis-placed before 2.21.1; kept the `Implemented (2026-06-20)` row). Corrected stale `Blocked` status → `Specified` for `2.17.1` (deps 2.15.1 + 4.0.2 both Implemented) and `7.0.1` (dep 4.0.1 Implemented); both are now eligible/ready. No source or schema change. |
| `TBD` | 2026-06-21 | 5.2.1, 5.2.2, 5.3.1 | **Design redesign — Dashboard `/home` (E1 read-model + E3 screen).** No-schema due read model: `dashboardDueSummary` query (`flashcard_progress.due_at <= :now`, folder_queries semantics — buried/suspend deferred) → `DashboardDao` → `DashboardRepository(Impl)` → `LoadDashboardSummaryUseCase` (owns clock) → `DashboardSummary` + keepAlive DI. `DashboardScreen` (quiet shell, no app-bar search) + `DashboardBody` (owns watch): `MxDueSummary` (count + decks, or caught-up) + `MxShortcutRow`→Progress/Library; loading/error states. **Divergence:** no Today CTA/minutes (quiet refer) and no goal/streak (Progress, pending engagement BE). Replaced `/home` placeholder with `dashboardBranchRoutes()`. 8 l10n keys (en+vi). Tests: `dashboard_test.dart` (3 state + 8 goldens); router test stubs the provider. Flip 5.2.1/5.2.2/5.3.1 → Implemented. Verify: full code chain + tests PASS; guard 0 errors. |
| `64e432f` | 2026-06-21 | 3.5.2, 3.5.3 | **Redesign — keep search-result nav inside the Search tab.** Registered folder-detail + deck-flashcard-list as child routes of `/search` (`RouteNames.searchFolderDetail`/`searchDeckFlashcards`), so a result tap pushes within the Search branch (Back → `/search`) instead of switching to Library. `GlobalSearchResultsView` uses the Search-branch routes; navigation-flow + wireframe 11 updated. (Addresses the code-review CRITICAL.) Verify: full code chain + search/router tests PASS; guard 0 errors. |
| `8320e69` | 2026-06-21 | 3.5.2 | **Redesign — auto-review fixes.** `global_search_viewmodel` `ref.read`→`ref.watch` the use case (keep its repo/DAO chain alive across the debounce gap) + stale-comment fix; fixed dashboard doc drift the detector flagged (`01-dashboard.md` + `dashboard.visual-contract.md` still referenced `/library/search`/`pushLibrarySearch`); `global-search.md` agent rule → Current; wireframe 11 documents result-tap nav + missing-data gaps. Verify: analyze + search tests PASS; doc_guard 0 new errors. |
| `f55e879` | 2026-06-21 | 1.2.6 | **Guard cleanup — LibraryOverviewBody StatefulWidget→ConsumerWidget** (cleared `no_stateful_widget_in_features`; behaviour unchanged, tests pass). With this the local guard reports **0 errors**. MxText positional `data` + the 694-line `folder_repository_impl` handled as documented exclusions in the gitignored local guard config (a true repo split is a tracked follow-up). |
| `b086c16` | 2026-06-21 | — | **Guard cleanup — 22 pre-existing errors (26→4).** `@Riverpod(keepAlive:true)` on flagged DI providers; `// guard:allow-screen-watch` hatches on Library/FolderDetail/FlashcardList; removed `!` non-null assertions (mx_app_bar, app_async_builder, folder_routes, library_create_folder_action, flashcard_list_screen, tag rename/merge usecases, global_search_usecase); `_Row`→`_DeckActionRow`. Pre-existing repo-wide debt, unrelated to the redesign. |
| `55a22cf` | 2026-06-21 | 1.2.3 | **Design redesign — engagement/summary shared widgets (Phase 5, widgets-first).** Built the four kit `_shared.jsx` redesign primitives as token-driven, golden-tested shared widgets: `MxShortcutRow` (flat "refer/navigate" card — added an `elevated:false` option to `MxCard`), `MxDueSummary` (quiet Dashboard due card), `MxInsight` (`MxInsightTone` info/good/warn/down/accent nudge), `MxGoalRing` (daily-goal arc; center copy via caller-localized `label`). All own no user-facing copy (caller-localized params). 4 behaviour tests + 10 light/dark goldens (`mx_engagement_widgets_test.dart`). Docs: `ui-ux-contract.md` (components built). **Deferred (per owner decision):** the consuming **Dashboard (`/home`, WBS 5.x) and Progress (`/progress`) screens** + their read-model BE, and the **goal/streak engagement persistence** (Future/Target — needs schema/migration/approval); these widgets are ready to assemble once that BE lands. Verify: analyze + targeted tests + goldens PASS; guard at the pre-existing baseline (no new violations). |
| `55a22cf` | 2026-06-21 | 3.5.2, 3.5.3 | **Design redesign — global Search screen at top-level `/search` (Phase 3).** Replaced the `/search` placeholder with `GlobalSearchScreen`: an `MxAppBar` title + `GlobalSearchBody` over an `MxSearchDock` at the foot (thumb-reachable; shell stays provider-watch-free — dock drives the query via `ref.read`, body owns the watch). `GlobalSearchBody` watches `globalSearchResultsProvider` (debounced via `GlobalSearchUseCase.inputDebounce` = 300ms, 2-char min, failure kept in-band in `Result`) and renders idle/loading/results/no-results/error; `GlobalSearchResultsView` groups Folders/Decks/Flashcards with per-section cap + "+N more", rows `pushNamed` to folder detail / deck flashcard list / owning deck. Built shared `MxSearchDock` + `MxIconTile` (the kit `.icon-tile`; `FolderIconTile` now delegates to it — dedup). Added 13 search l10n keys (en+vi). Docs: wireframe 11 (route → `/search`, bottom dock, states), `global-search.md`/`overview.md`/`MANIFEST.md`/`dashboard.visual-contract.md` (`/library/search` → `/search`), decision rows SR5/SR6 + new SR-screen. Tests: `global_search_test.dart` (4 state + 10 light/dark goldens). Flip 3.5.2/3.5.3 Specified→Implemented. Verify: analyze + targeted tests + goldens PASS; `code-verification-guard` total unchanged at the pre-existing baseline (no new violations). |
| `55a22cf` | 2026-06-21 | 3.2.2, 3.4.2 | **Design redesign — breadcrumb trail on nested Library screens (Phase 4).** Wired the existing (previously unused) shared `MxBreadcrumb` into Folder detail + Flashcard list, docked under the app bar. Added shared builder `buildLibraryBreadcrumb` (`lib/presentation/shared/widgets/navigation/library_breadcrumb.dart`): `Library › …ancestors › {leaf}`, chevron separators, horizontal scroll, last crumb = current/non-tappable; `Library` taps to the branch root, ancestor folders push their detail. Folder detail leaf = current folder (from the `breadcrumb` read-model list incl. current); Flashcard list leaf = deck name (every folder crumb tappable). Hidden in search mode / until loaded. Docs: wireframes 05/06 (breadcrumb rows → real component), `navigation-flow.md` (§Breadcrumb trail), decision rows **N15/N16**. Tests: `library_breadcrumb_test.dart` (builder leaf/tappability for both cases); regenerated Folder-detail + Flashcard-list loaded/empty goldens (breadcrumb added). Verify: analyze + targeted tests + goldens PASS (quick chain). |
| `55a22cf` | 2026-06-21 | 1.2.6, 3.5.2, 5.8.1 | **Design redesign — IA foundation (Phases 0–2).** Re-exported ui-kit shots/specs to match the changed `index.html`. **Phase 0 (docs reconcile):** fixed pre-existing drift — `navigation-flow.md` + `global-search.md` falsely claimed `/library/search` was a Current route with nonexistent `RouteNames.librarySearch`/`RoutePaths.librarySearchTemplate` constants; corrected to reality (in-place folder search only; `GlobalSearchUseCase` domain/data ready but unwired) and recorded the new top-level `/search` direction in `overview.md`. **Phase 1 (token doc-parity):** documented new kit tokens `--memox-size-search-dock` / `--memox-safe-top` + new shared components in `ui-ux-contract.md` + DS `README.md` (widget creation folded into consuming phases). **Phase 2 (5-tab nav + Search destination):** added `RouteNames.search`/`RoutePaths.search` (`/search`) + a Search shell branch (placeholder until 3.5.2) in `app_router.dart`; `MxAppShell` now hosts **5 tabs** — Home · Library · Search · Progress · Settings; added `searchTitle` l10n (en+vi); updated `navigation-flow.md` (top-level table/shell/deep-links), decision rows N8/N9 (drift fix) + new **N14** (5-tab order + goBranch), and `app_router_test.dart` (loop covers `/search`). Marked **5.8.1 Rejected** (Dashboard app-bar search shortcut superseded by the Search tab); re-scoped **3.5.2** to top-level `/search` + bottom dock. Verify: analyze + router/nav targeted tests PASS (quick chain). NOTE: repo-wide `code-verification-guard` is red on a pre-existing baseline (26 errors unrelated to this work — see session notes); per owner decision this work verifies via analyze + targeted tests and is not auto-committed. |
| `c48d86f` | 2026-06-21 | 4.6.1, 4.6.2, 4.6.4 | SRS finalization keystone (gộp 1 slice). `StudyRepository.finalizeStudySession({sessionId,now})` + `StudySessionDao.finalizeSession` (one transaction: upsert each card's `flashcard_progress` + mark session `completed`; rollback as a unit). Per item: terminal result via `_terminalResult` (V1 last-attempt classifier) → `box_after = SrsBox.nextBox(box_before, result)` → `due_at = localMidnight(studyDay + BoxIntervals.daysFor(box_after))` (Dart local time, not SQL `localtime`) → `review_count`+1, `lapse_count`+1 on forgot; new card finalizes from box 1; suspend/bury preserved across upsert. Validate all answered (else `FinalizationFailure`, session stays open); missing→NotFound; terminal→UnsupportedAction. New `BoxIntervals` (ladder 1-5 linear, 6/7/8 = 12/30/60). `FinalizeStudySessionUseCase` (owns clock) + DI. Tests: 7 table-driven transition cases (S11-S15/S17/S18 + local-midnight + new-card) + 5 finalize-flow cases (success+completed+progress, unanswered→FinalizationFailure stays-open, terminal, missing, suspend/bury preserved). Docs: study.md/srs.md/srs-review.md finalize+source refs corrected (prior-iteration phantom paths → actual), decision S9/S10/S11-S15/S17/S18. Flip 4.6.1/4.6.2/4.6.4 Specified→Implemented. |
| `a36f632` | 2026-06-21 | 5.1.1 | Dashboard Continue-Studying summary BE. New `DashboardResumeSessionSummary` model (sessionId/scope/answeredCount/totalCount/lastActiveAt + `progress` getter). `dashboardResumeSession` `.drift` query (study_sessions LEFT JOIN items, status draft/in_progress, updated_at > cutoff, COUNT items + COUNT(answered_at), ORDER BY updated_at DESC LIMIT 1) + `DashboardDao.resumeSession`. `DashboardRepository.loadResumeSessionSummary({now})` (cutoff = now − 30d resumeWindow; maps tokens→enums via StudySessionMapper) → summary or null. `LoadDashboardResumeSummaryUseCase` (owns clock) + DI. Tests: 5 cases (most-recent+progress+today-null-scope, none→null, terminal-excluded, 30d-window, empty-items→0 progress). Docs: engagement usecase contract note. Flip 5.1.1 Specified→Implemented. |
| `7ee272b` | 2026-06-21 | 4.2.2 | No-silent-resume gate BE. `StudyRepository.findResumable({scope,now})` + `StudySessionDao.findResumableSession` (NULL-safe entry_ref_id match, status in draft/in_progress, updated_at within 30-day window, ORDER BY updated_at DESC LIMIT 1) → resumable `StudySession?`. New `StudyEntryStartResult` sealed type (resumeRequired/canStart/blocked) + `ResolveStudyEntryStartUseCase` gate that returns resumeRequired when a resumable session exists (S28, never silent) else composes eligibility (4.1.1) into canStart/blocked. DI wired. Tests: 5 findResumable repo cases (most-recent, scope-miss, terminal-excluded, 30d-window, today NULL ref) + 3 gate use-case cases. Docs: study.md FindResumableSessionUseCase V1 note, decision S28 ref. Richer ResumableSession projection → 5.1.1. Flip 4.2.2 Specified→Implemented. |
| `237160c` | 2026-06-21 | 4.4.1 | Submit self-grade BE. New `SrsBox.nextBox` Leitner box transition (perfect→+1 cap8, recovered→stay, forgot→1). `StudyRepository.recordStudySessionAnswer({sessionId,sessionItemId,result,studyMode,now})` → loads session (terminal→UnsupportedAction)/item (missing/wrong-session→NotFound; answered→UnsupportedAction)/progress box (none→box 1), computes box_after, and `StudySessionDao.recordAnswer` inserts `study_attempts` (result+study_mode tokens via mapper, box_before/box_after) + marks `study_session_items.answered_at` + touches `study_sessions.updated_at` in one transaction; `flashcard_progress` untouched. `RecordStudySessionAnswerUseCase` (owns clock) + DI. Tests: 7 repo cases (perfect transition+progress-unchanged S35, forgot→1 S36, new-card box1, already-answered, terminal, missing session/item) + 5 SrsBox unit cases. Docs: study.md V1 note, decision S6/S35/S36 refs. Box-only; due_at/interval = 4.6.x. Flip 4.4.1 Specified→Implemented. |
| `d238c9d` | 2026-06-21 | 4.5.1 | Study mode strategy V1 BE (pure domain). New `AttemptResult` + `StudyMode` enums (flipped types-catalog status Target→Current). Sealed `StudyModeStrategy` base with 3 interaction families as direct subtypes — `BinaryGradeStudyModeStrategy` (review/recall/guess; V1 mapGotIt→perfect/mapForgot→forgot), `TypedAnswerStudyModeStrategy` (fill; strict trim-match evaluator), `BoardStudyModeStrategy` (match; no grading API) — plus 5 concrete leaves. `StudyModeStrategyFactory.resolve` exhaustive switch (compile-time safety for new modes). Per-mode deepening deferred to 4.5.2/4.5.6/4.5.8. Tests: 6 factory cases (family resolution per mode, one-family-per-mode S58, binary got-it/forgot, fill strict-match). Decision S48/S49/S58 refs. Flip 4.5.1 Specified→Implemented. |
| `275f6d7` | 2026-06-21 | 4.3.1 | Session item loading BE. New `StudySessionReview` + `StudySessionReviewItem` entities (header + ordered flashcard-joined items with `answeredAt`; `total`/`answeredCount`/`isComplete`/`firstUnansweredIndex` getters). `StudyRepository.loadStudySessionReview({id})` → `StudySessionDao.sessionById` (missing → `NotFoundFailure`) + new `StudySessionDao.reviewItems` (query-builder inner join `study_session_items`×`flashcards` ordered by `sort_order`); empty items → `ValidationFailure(insufficientContent)`; read error → `StorageFailure(read)`. `StudySessionMapper.toReviewItem` join mapper; `LoadStudySessionReviewUseCase` + DI. Tests: 3 repo cases (ordered+joined+answered, not-found, empty-items integrity). Docs: study.md LoadStudySessionReviewUseCase V1 note. Powers review shell 4.3.2 + recovery 4.8.1. Flip 4.3.1 Specified→Implemented. |
| `793e542` | 2026-06-21 | 4.10.1 | Cancel/discard session BE. `StudyRepository.cancelSession({id})` + `StudySessionDao.markCancelled` (single `study_sessions` UPDATE status→`cancelled`, row never deleted, attempts/items untouched); UPDATE guarded by `status IN (draft, in_progress)`; affected-row count + a `sessionById` fallback classify the result (1 → success; 0 → `NotFoundFailure` if missing else `UnsupportedActionFailure` for a terminal session). `CancelStudySessionUseCase` + DI. Powers transactional start-over (4.2.3). Tests: 4 repo cases (cancel preserves attempts+items, not-found, completed→unsupported, re-cancel→unsupported) + 1 use-case delegation. Docs: study.md CancelSessionUseCase V1 note + Errors (added UnsupportedActionFailure). Flip 4.10.1 Specified→Implemented. |
| `15a4d74` | 2026-06-21 | 4.2.4 | Session batch limit BE V1. `CreateStudySessionUseCase.maxSessionItems` named const (20) + cap: when the resolved list exceeds the cap, only the first N (caller's resolved order) are persisted as session items. Cap lives in the use case (batching is a use-case rule); the repo persists what it receives. Reconciled the 4.2.1 "cap applied by the caller" notes (study.md, study_repository port doc) to "applied in the use case". Tests: 2 use-case cases (over-cap truncation keeping first-N order, at-cap pass-through). Decision rows S1/S2/S3 cap test refs updated. Flip 4.2.4 Specified→Implemented. |
| `201fe72` | 2026-06-21 | 4.2.1 | Session creation BE (transactional insert). New `StudySession` entity + `SessionStatus` enum + `StudySessionMapper` (enum↔storage tokens, row→entity). `StudyRepository.createSession({scope, flashcardIds, now})` generates ids (`IdGenerator`), builds session (status `in_progress` per V1) + ordered item companions, and inserts via new `StudySessionDao.createSessionWithItems` (one `_db.transaction` → session insert + `batch.insertAll` items; rolls back as a unit). `CreateStudySessionUseCase` owns the clock; DI added. Empty `flashcardIds` → `ValidationFailure(insufficientContent)`; caller supplies the list (cap=4.2.4, ordered-id resolution separate). Tests: 5 cases (persist+order S1, today NULL ref S3, empty-list guard, FK-violation rollback, mapper round-trip). Docs: study.md CreateSessionUseCase V1 note, study-repository.md create-status reconcile (in_progress not draft), decision rows S1/S2/S3 test refs (BE; cap→4.2.4). Flip 4.2.1 Specified→Implemented. |
| `640ef46` | 2026-06-21 | 4.1.1 | Study entry eligibility BE (count-based classification). New domain types `EntryType` / `StudyType` / `StudyScope` (freezed) + `EntryRefId` typedef; `StudyEntryEligibility` + `StudyScopeEmptyReason` model; `StudyEntryRepository` port + impl classifying scope counts against the empty-scope matrix (deck/folder-recursive/today via `study_entry_queries.drift` + `StudyEntryDao`); `ResolveStudyEntryEligibilityUseCase` (owns clock) + DI. New study counts active non-buried cards, SRS counts due only; missing-progress = new active (S23); precedence noCards→allSuspended→allBuried→eligible→noDue. Tests: 14 repo branch cases (S4/S4b/S4c/S4d/S4e/S4j/S4f/S4g/S22/S23/S24/S25/S3) + use-case clock delegation. Docs: study-flow Implementation-status table corrected (BE done 4.1.1 / FE pending 4.1.2, stale prior-iteration file refs replaced), study.md usecase contract V1 note. Card-list+batch=4.2.1, FE gate=4.1.2, tag deferred. Flip 4.1.1 Specified→Implemented. |
| `ce978d1` | 2026-06-21 | 4.0.1 | Ship enabler B1 (study persistence): schema v6 adds `study_sessions` / `study_session_items` / `study_attempts` via `v6_add_study_tables.dart` (additive `createTable` + indexes, no back-fill) — FK chain `study_sessions → study_session_items → study_attempts` (+ `study_session_items.flashcard_id → flashcards`), with resume / queue-order / attempt indexes. `StudyRepository` port + `StudyRepositoryImpl` skeleton (only `expireOldSessions` 30-day retention sweep — no SRS/grading/scope logic) + `StudySessionDao` persistence primitives + DI (`study_providers.dart`); added `SessionId` typedef. `study_flow`/`current_mode` deferred to 4.5.12, `duration_ms` to 7.0.1. Tests: v5→v6 migration test, schema-test bump (v6) + study-table FK-chain/cascade/null-scope coverage, repo `expireOldSessions` (window boundary + non-resumable exclusion). Docs: schema-contract (frontmatter v6, current-schema tables, corrected stale v4/v5/v6/v7/v8 "DONE" study/card-history markers → accurate v6 + Pending), migration-contract (v6 row + "schema v6" + phantom-file note), WBS B1 resolved. Used `lib/domain/repositories/**` convention (not prior-iteration `lib/domain/study/ports/`). Flip 4.0.1 Specified→Implemented. |
| `15c852f` | 2026-06-20 | 3.1.2, 2.1.2, 3.2.2, 3.3.1, 3.4.2 | **Library content+navigation landed on `main`** (squash of the `feat/wbs-3.1.2-library-full-parity` branch — Library Overview full parity + Folder Create — plus the Folder Detail + Flashcard List navigation chain; the granular `2c107f4`/`cfef991` commits live on that origin branch). **Folder Detail + Flashcard List FE** — folder/deck navigation chain. **3.2.2**: extended `FolderDetail` with child `DeckSummary` decks + `subtreeDeckCount` (`folderDeckSummaries` query + `subtree_deck_count` column); built `FolderDetailScreen` (breadcrumb app bar, 3-stat summary card with highlighted Due, content-aware create FAB, grouped subfolder/deck list, search mode, kit-04 states) with create subfolder/deck + folder/deck action sheets (rename/move/delete · rename/delete). **3.3.1**: deck tap → `/library/deck/:deckId/flashcards` (child route); Library row tap → `/library/folder/:id`. **3.4.2**: built `FlashcardListScreen` under `features/decks/` over the existing `watchFlashcardList` read model — card list (front/back), loaded/empty/loading/error/search states, minimal front/back add/edit/delete dialog + deck delete (advances 2.7.2/2.8.2/2.9.2/2.11.2/2.12.2/2.13.2 at V1 scope). Added `RouteNames.folderDetail/deckFlashcards` + `RouteParams.id/deckId`; ~50 l10n keys (en+vi); goldens for both screens (5+4 × light+dark). Deferred: SRS box/status chips, full card editor (notes/tags/example), reorder, import, deck move, study entry. |
| `2c107f4` | 2026-06-20 | 3.1.2, 2.1.2 | Library Overview **full mock-parity** revision + Folder Create FE (**2.1.2**). Rebuilt the screen to the kit `03a–03f` visuals: app-bar **search mode** (`Icons.search` → `LibrarySearchAppBar` field + Cancel; `librarySearchActiveProvider`) with a visual-only `swap_vert` sort icon; **grouped list-card** of rows (tinted `FolderIconTile` from the OQ-2 `note-*`/icon palette via `folder_visual_tokens.dart`, title, meta, **chevron** — kebab removed; tap + long-press → action sheet) separated by `MxDivider` inset hairlines; `New folder` **`MxFab`** + true-empty **Create folder** CTA → `folder_create_dialog.dart` (name + color + icon pickers, `FolderDraft`) via `runCreateFolder`/`LibraryActionController.create`; **loading skeleton**; enriched overflow sheet header (tile+name+meta, danger delete). Added theme tokens `MxOpacity`/`MxIconSize`/`MxStroke` (mirror existing css) + `MxTappable.onLongPress` + shared `MxDivider`; `StringUtils.upperFold`; 9 l10n keys (en+vi). Goldens: loaded/empty/loading/error/**search**/search-no-results × light+dark. Flip 2.1.2 Specified→Implemented; 3.1.2 re-revised (already Implemented). Supersedes the rev. 1 kebab/no-FAB/inline-search deferrals in `docs/design/screens/library-overview.visual-contract.md`. |
| `TBD` | 2026-06-20 | 1.2.7, 3.1.2, 2.2.2, 2.3.2, 2.21.1, 2.4.2 | Wave-1 Library management (one pass). Built the missing feature-enabling foundation tier **1.2.7** (`AppAsyncBuilder`, `MxText`+`MxTextRole`, `showMxSnackbar`, `showMxDialog`, `useMx*` text/search hooks, `StringUtils.trimmed`; ported from backup, resolving the `MxTypography`/`no_direct_text_theme` contradiction via `MxText`), then the **Library Overview screen 3.1.2** (`LibraryOverviewScreen`/`Body`/`Tile`/`SearchField` + `library_overview_viewmodel` stream/search + folder-route registry wired into the shell branch; loading/loaded/true-empty/search-no-results/error states; light+dark goldens per state + widget tests) and the folder actions off its kebab sheet: **2.2.2** rename, **2.3.2**+**2.21.1** delete with blast-radius confirm (subtree deck/card counts), **2.4.2** move (`getFolderMoveTargets`/`moveFolder`, blocked rows disabled). `LibraryActionController` returns `Result`; failures → typed `folderFailureMessage` snackbar. ~35 l10n keys (en+vi). Flip 1.2.7/3.1.2/2.2.2/2.3.2/2.21.1/2.4.2 Specified→Implemented. Excluded: **2.5.2** reorder (wireframe marks root drag-reorder Future → Folder Detail / Wave 2) and **2.1.2** create (needs the color/icon palette — OQ-2). |
| `TBD` | 2026-06-20 | 1.2.6 | Build screen-shell foundation (unblocks all Phase-3 FE): `MxScaffold` (appBar + `MxContentShell` gutter + FAB/bottomNav/bottomSheet slots + `useShell`), `MxListScaffold` (separated list + pinned header), `MxFab` (icon-only + `.extended`), and `MxAppShell` — `StatefulShellRoute.indexedStack` over the 4 top-level branches with `MxBottomNav` + `goBranch` reset semantics. Refactored `app_router.dart` to the shell; added `home/library/progress/settingsTitle` l10n (en+vi); light+dark goldens for both scaffolds + behaviour tests + router/shell tests (incl. tab-switch). Updated navigation-flow doc (shell §). `MxFormScaffold` deferred until a form screen needs it. Reference impl `origin/backup/main-2026-06-19`, adapted to current `Mx*`/token kit. Flip 1.2.6 Specified→Implemented. |
| `TBD` | 2026-06-20 | §4.2, 1.2.6 | Plan-only (Phase-3 overnight run finding): add foundation row **1.2.6** (screen-shell family `MxScaffold`/`MxListScaffold` + `MxFab` + `lib/app/app_shell.dart`) as the hard prerequisite gating ALL Phase-3 FE — the rebuild reset (`717f5b82`) deleted the prior screen shell and no row re-added it, while guard `memox.screen_shell.no_raw_scaffold` (error) blocks raw `Scaffold(` in feature code, so no feature screen can pass `tool/verify`. Reference impl on `origin/backup/main-2026-06-19`. Record 1.2.6 + 2.22.1 as §4.2 prerequisites; note 2.1.2 still needs the folder color/icon palette defined (OQ-2). Product owner chose to formalize as a foundation row before building. No code. |
| `TBD` | 2026-06-20 | 6.0.1 | Ship enabler B3: pin the import type-contract. New domain value objects `ImportSourceFormat` / `ImportTextSeparator` / `FlashcardImportDuplicatePolicy` / `FlashcardImportDuplicateSource` / `ImportRowIssueType` (`lib/domain/types/`) + freezed preview/preparation model family `FlashcardImportPreview` / `FlashcardImportPreparation` / `FlashcardImportRow` / `ImportValidationIssue` / `FlashcardImportSkippedDuplicate` (`lib/domain/models/flashcard_import_preview.dart`). Contract-only (no parse/dedup/commit logic). Reconciled the prior `DeckImportPreview` synonym → canonical `FlashcardImportPreview` in the use-case contract; pinned the three-stage parse→prepare→commit signatures (usecase-contract) + enums (types-catalog) + data-model note (business spec). Getter-semantics test added. Gates 6.2.1–6.9.1. |
| `TBD` | 2026-06-20 | 4.0.2 | Ship enabler B2: schema v4 adds `flashcard_progress.is_suspended` (BOOLEAN NOT NULL DEFAULT 0) + `buried_until` (INTEGER NULL) via `v4_add_bury_suspend.dart` (additive, data-preserving). Columns only — no eligibility behavior yet (lands 4.11.1 / 2.17.1). Migration + schema tests added; schema-contract + migration-contract docs synced (corrected stale "DONE (current)" markers for these two columns + deferred the eligibility index to 4.11.1). Unblocks 2.17.1 / 4.11.x / 5.2.1 / 7.1.1. |
| `TBD` | 2026-06-20 | 2.22.1 | Folder color/icon schema + BE (Wave-1 prerequisite): added nullable `folders.color` + `folders.icon` TEXT columns (migration v4→v5 `v5_add_folder_color_icon.dart`, additive `addColumn`, no backfill); extended `Folder` entity + `FolderMapper` + `createRootFolder`/`createSubfolder`/`renameFolder` (repo + use cases) to carry optional `color`/`icon` tokens (NULL = no custom token → theme default; rename overwrites only when non-null, clearing deferred). No CHECK — allowed palette/icon set is an FE concern (2.1.2). Tests: v4→v5 migration test, schema-test version bump + color/icon round-trip, repo create/subfolder/rename token carry + null-default + restyle-on-unchanged-name. Docs: schema-contract (frontmatter v5, current/target folders rows, DONE-v5 pending row), migration-contract (v5 row + "schema v5"), folder-management fields, folder use-case + repository contracts. Branch feat/wbs-2.22.1-folder-schema (the pre-existing feat/wbs-2.22.1 held an unrelated run's 4.0.2 work). Flip 2.22.1 Specified→Implemented. |
| `TBD` | 2026-06-20 | §4.3, 7.0.1, 7.6.1, 8.4.1, 8.4.3 | Plan-only (status audit): verified all 39 `Implemented` rows have real source+tests (accurate). Surfaced a 4th latent blocker B4 — card-history schema (`card_events` + `flashcard_progress.last_reset_at` + `study_attempts.duration_ms`) missing → new enabler **7.0.1**; repoint `7.6.1` (Specified→Blocked, deps 7.0.1/4.0.1) and strip its phantom v6/v7 migration evidence. Fix misleading "No action — implemented" next-action on `8.4.1`/`8.4.3` (neither is built; files don't exist) → marked NOT built. |
| `TBD` | 2026-06-20 | §4.3, 4.0.1, 4.0.2, 6.0.1, 2.17.1, 4.11.1, 4.11.2, 5.2.1, 7.1.1, 6.2.1 | Plan-only (PM critical-path hardening): promote the three latent blockers surfaced by the overnight run into explicit enabler work packages — new §4.3 Critical Path & Blocker Register (B1/B2/B3 + build sequence + discipline rule), new rows **4.0.1** (study persistence schema + repo skeleton), **4.0.2** (bury/suspend `flashcard_progress` columns), **6.0.1** (import type-contract & preview model). Repoint dependents' `Depends on` to the enablers (4.1.1→4.0.1/4.0.2; 4.11.1/4.11.2→+4.0.2; 6.2.1→+6.0.1) and move schema-gated consumers 2.17.1/5.2.1/7.1.1 to `Blocked`. Update §4.1 phase map (enablers first), §5 current-frontier queue, §7 risk register (B1/B2/B3 + latent-blocker discipline). No code. |
| `TBD` | 2026-06-20 | §4.2, 2.1.2, 2.22.1 | Plan-only: decompose Phase 3 (Content/Library FE, ~28 rows) into 4 screen-centric delivery waves (new §4.2); add prerequisite row 2.22.1 (folder color/icon schema + BE); record resolved gating decisions for 2.1.2 (folder color/icon → schema-backed; folder-create/rename dialog owns submit + inline errors, returns `Future<Folder?>`). No code — planning/WBS restructure for product-owner review. |
| `TBD` | 2026-06-20 | 2.18.1 | Flashcard tag filter BE V1: `watchFlashcardList` (use case + repo interface + impl) gains a `List<TagName> tags` param — multi-select AND filter keeping only cards that carry every selected tag, each normalized with the storage rule (`_normalizeTags`) to match stored tags; empty selection imposes no filter. Composes with the existing front/back search; `totalCount` stays the full deck total (filter-independent) so no-results stays distinguishable. In-Dart filter over the already-built `tagsByCard` map (no new query). Updated the two existing flashcard-repo test fakes for the new param. Tests: repo C38 (empty/single/multi AND) + C39 (compose-with-search, no-results totalCount) + use-case delegation. Docs: flashcard `WatchFlashcardListUseCase` contract (signature + reconciled the stale status-filter over-claim → 2.17.1 Future), decision rows C38/C39. Status filter blocked on suspend/bury columns. Stacked on feat/wbs-2.9.1. Flip 2.18.1 Specified→Implemented. |
| `TBD` | 2026-06-20 | 8.3.1 | Tag management BE V1 over `flashcard_tags`: `TagValidator` (trim/lowercase via `StringUtils.normalizeTag`, reject empty/comma/>50) + `TagWithCount`/`MergeResult` models + `TagRepository`(`Impl`) over new `FlashcardTagDao` (`tag_queries.drift` `tagsWithCount`) + 4 use cases (watch-with-count, rename, merge, delete) + DI `tag_providers.dart`. Rename no-ops on equal and returns `ConflictFailure` on a name collision (never auto-merges); merge re-tags source→dest with per-card `INSERT OR IGNORE` de-dup then deletes source rows in one transaction (returns affected count); delete strips the tag from all cards, keeping the cards. Tests: repo/DAO integration (watch order, rename/conflict/no-op, merge dedup, delete) + use-case validation/delegation. Docs: tag-system status + impl pointer, tag use-case/repository contract impl notes, filled `decision-tables/tags-bulk-export.md` rows TG1/TG5/TG6/TG7/TG9/TG10 (incl. leading-`#` strip in `StringUtils.normalizeTag` per TG1), overview status. Stacked on feat/wbs-2.9.1. Flip 8.3.1 Specified→Implemented. |
| `TBD` | 2026-06-20 | 8.2.1 | Learning settings BE persistence (SharedPreferences, outside Drift): `LearningSettings` entity (dailyNewLimit default 20 / 5..200 step 5; goalDisabledSince nullable local date) + `LearningSettingsRepository`(`Impl`) over a thin `LearningSettingsStore` (keys `learning.dailyNewLimit`/`learning.goalDisabledSince` per storage-boundaries) + `Load`/`UpdateLearningSettingsUseCase` (range/step validation → `outOfRange`, local-date normalization). Defaults + corrupt/out-of-range recovery on load; `YYYY-MM-DD` round-trip. New centralized `sharedPreferencesProvider` in `app_providers.dart` (only allowed `getInstance()` caller per guard); async DI chain in `learning_settings_providers.dart`. Tests: use-case validation/delegation + repo/store persistence (defaults, recovery, round-trip, clear). Docs: types-catalog `LearningSettings`, use-case + repo contracts impl notes. Stacked on feat/wbs-2.9.1. Flip 8.2.1 Specified→Implemented. |
| `TBD` | 2026-06-20 | 3.7.1 | Folder/deck due+card counts BE (unblocked by schema v3 flashcards/progress): `rootFolderSummaries`/`childFolderSummaries`/`folderSubtreeCounts` drift queries compute `deckCount` (direct child decks) + recursive subtree `cardCount`/`dueCount` via a whole-forest recursive CTE joined to folder rows (reactive over folders+decks+flashcards+flashcard_progress); `dueCount` = `due_at IS NOT NULL AND due_at <= :now` (NEW cards excluded). `FolderDao` plumbs `:now`; `FolderRepositoryImpl` populates `FolderSummary`/`FolderDetail` counts (no longer hardcoded 0). Suspend/bury exclusion trivially satisfied until those columns ship. Tests: `folder_deck_due_counts_test.dart` (F12/F13: direct deck count, recursive card/due, subtree roll-up, empty baseline). Stacked on feat/wbs-2.9.1. Flip 3.7.1 Specified→Implemented; decision rows F12/F13. |
| `TBD` | 2026-06-20 | 2.9.1 | Deck Delete BE (now unblocked — `flashcards`/`flashcard_progress`/`flashcard_tags` shipped at schema v3): `FolderRepository.deleteDeck` + `DeleteDeckUseCase` (`Result<void>`); transactional deck-row delete cascades to flashcards + progress + tags via the `flashcards.deck_id` ON DELETE CASCADE chain; source folder reverts to `unlocked` when its last deck leaves; missing deck → `NotFoundFailure`; DI `deleteDeckUseCaseProvider`; tests (use case + repo cascade/revert/not-found, row D3). Study attempt/session cascade left **Future** (WBS 4.x). Also routed `GlobalSearchUseCase` case-folding through new `StringUtils.caseFold` to clear a pre-existing `string_normalization_via_string_utils` guard error; flip 2.9.1 Specified→Implemented |
| `TBD` | 2026-06-20 | 2.16.1, 2.20.1, 3.5.1 | Global search + duplicate soft-warning + parent-child validation BE (on top of the flashcards table from `cc45839e`): `GlobalSearchUseCase` over `SearchRepository`(`Impl`)+`SearchDao` (`search_queries.drift`, LIKE `%`/`_`/`\` escape + `ESCAPE '\'`, normalize, 2-char-min `tooShort`, exact→starts-with→substring→recency ranking, cap-5 + totals) + `SearchResults`/`StringUtils`; `CheckManualDuplicateFlashcardUseCase` + `FlashcardRepository.checkManualDuplicate` (trimmed+case-insensitive, deck-scoped, `excludeId`, non-blocking) + `FlashcardDuplicateCheckResult`; parent-child (2.16.1): non-null `deck_id`/`folder_id` FKs + repo missing-parent `NotFoundFailure` checks + FK/cascade schema tests; DI `search_providers.dart` + dup provider in `flashcard_providers.dart`; tests (search repo escape, search use-case rank/cap, dup behavior + use case); rows SR1-SR4/SR-rank/SR-cap/SR-empty/SR-err, C40, C41; flip 2.16.1/2.20.1/3.5.1 Specified→Implemented |
| `cc45839e` | 2026-06-20 | 2.11.1, 2.12.1, 2.13.1, 2.14.1, 3.4.1, 2.15.1 | Flashcard CRUD + list-load BE (schema v3): `flashcards.drift` (`flashcards` FK→decks cascade + `idx_flashcards_deck`; `flashcard_progress` 1:1 PK=FK cascade, box 1 / due_at NULL / zero counters; `flashcard_tags` FK cascade + `idx_flashcard_tags_tag`) + `v3_add_flashcards.dart` migration; `Flashcard`/`FlashcardProgress` entities + `FlashcardListDetail` model + `FlashcardId`/`TagName`/`ContentSortMode`/`FlashcardProgressEditPolicy` types + `FlashcardMapper` + `FlashcardDao`; `FlashcardRepository`(`Impl`) create/update/delete/reorder + `watchFlashcardList` (deck+breadcrumb+cards+search-independent totalCount, front/back search); 5 use cases + DI `flashcard_providers.dart`; migration + schema + repo + use-case tests (rows C1/C2/C3/C5/C6/C8/C33/C34/C35). `move`, bulk, status/tag filters + `CardState` left **Future** (bury/suspend + bulk epics); flip 2.11.1/2.12.1/2.13.1/2.14.1/3.4.1 Specified→Implemented |
| `TBD` | 2026-06-20 | 2.7.1, 2.8.1, 2.10.1, 2.19.1, 2.9.1, 3.7.1 | Deck BE layer (schema v2 `decks` table): `decks.drift` (FK→folders cascade, `target_language` default `'korean'`, `idx_decks_folder`) + `v2_add_decks.dart` migration; `Deck` entity + `TargetLanguage`/`DeckId` types + `DeckMapper` + `DeckDao`; create/rename/reorder/move on `FolderRepository.{createDeck,renameDeck,reorderDecks,moveDeck}` (folder-owned; symmetric `folder_contains_subfolders` guard, mode lock/revert, sort append, dup/empty/missing rejection) + 4 use cases + DI; migration + schema + repo + use-case tests (rows D1/D2/D4/D6-D10). Deck delete (2.9.1) and folder/deck due+card counts (3.7.1) left **deferred** — blocked on the flashcards/flashcard_progress tables (WBS 2.11.x), noted in their WBS rows; flip 2.7.1/2.8.1/2.10.1/2.19.1 Specified→Implemented |
| `TBD` | 2026-06-20 | 2.4.1, 2.5.1 | Folder move + reorder BE (folders-only V1): `MoveFolderUseCase`/`GetFolderMoveTargetsUseCase`/`ReorderFoldersUseCase` + `FolderRepository.{moveFolder,getFolderMoveTargets,reorderFolders}` + `FolderMoveTarget`/`FolderMoveBlock` model + `FolderDao.listAllFolders`; transactional cycle-safe move (sort_order recompute, destination lock, emptied-parent revert), blocked-not-hidden move-target list, full-sibling-list reorder validation; DI providers; rows F7/F10/F11/F14-F18 tested; flip 2.4.1/2.5.1 Specified→Implemented |
| `TBD` | 2026-06-20 | 2.1.1, 2.2.1, 2.3.1, 2.6.1, 3.1.1, 3.2.1 | Folder BE layer (folders-only V1): `Folder` entity + `ContentMode`/`FolderId` types + `FolderDetail`/`LibraryOverview`/`FolderSummary` read models; `FolderRepository`(`Impl` + mutation-helpers part) over `FolderDao` (`folder_queries.drift` recursive/aggregate reads) + `FolderMapper` + `IdGenerator`; create-root/create-sub/rename/delete use cases + watch-overview/watch-detail; DI in `folder_providers.dart`; 20 tests (rows F1-F4, F8, F9 + read models). Deck/flashcard cascade, deck rows + non-zero card/due counts (F12/F13) and deck-side guard (F6) deferred until the decks/flashcards tables ship (WBS 2.7.x/2.11.x); flip 2.1.1/2.2.1/2.3.1/2.6.1/3.1.1/3.2.1 Specified→Implemented |
| `TBD` | 2026-06-19 | 1.2.5 | Core widget kit — dialogs & sheets: `MxDialog` shell, `MxConfirmDialog` (`show()`→`Future<bool>`, destructive=error fill + modal-locked barrier), `MxBottomSheet` + `showMxBottomSheet` host (drag handle, rounded top, safe-area, 90% cap, root navigator); `MxPrimaryButton.destructive` + `MxRadius.topSheet` token additions; goldens + behaviour tests; flip 1.2.5 Specified→Implemented |
| `TBD` | 2026-06-19 | 1.2.2 | Core widget kit — navigation & app bar: `MxAppBar` (flat large-title PreferredSizeWidget), `MxBottomNav` (token-themed NavigationBar, accent-soft selected pill), `MxBreadcrumb` (scrollable ancestry trail), `MxIconButton` (toolbar primitive, deferred from 1.2.3); shell golden + behaviour tests; flip 1.2.2 Specified→Implemented |
| `TBD` | 2026-06-19 | 1.2.3 | Core widget kit — actions & inputs: button system per action-hierarchy-contract (`MxPrimaryButton`/`MxSecondaryButton` primitives, `MxButtonSize`, intent-driven `MxActionButton`/`MxActionIntent`, `MxCardActions`) + `MxTextField` base + `MxSearchField` (owns search/clear geometry); intent density contract test + behaviour tests + light/dark goldens; flip 1.2.3 Specified→Implemented (base button is Primary/Secondary, reconciling the WBS "MxButton" wording) |
| `TBD` | 2026-06-19 | 1.2.1 | Core widget kit — surfaces & shell: `MxContentShell` (gutter + optional max-width cap), `MxCard` (token surface/border/sm-shadow + shaped ink), `MxListTile`, `MxAvatar` (rounded/circle, icon or initials), and the `MxTappable` shaped-ink primitive (sole `InkWell` owner per guard); light/dark goldens + functional tests; flip 1.2.1 Specified→Implemented |
| `TBD` | 2026-06-19 | 1.2.4 | Core widget kit — state widgets: `MxEmptyState`/`MxErrorState`/`MxNoResultsState`/`MxLoadingState` (card-agnostic centered panels, token-driven, CTA via `action` slot); add `BorderRadius` token helpers to `MxRadius`; golden harness + light/dark goldens + functional tests; flip 1.2.4 Specified→Implemented |
| `TBD` | 2026-06-19 | 9.13 | Make `doc_guard generate` resilient to the incremental rebuild (guard the hardcoded `study_usecases.dart` read with `existsSync`); regenerate `docs/_generated/{repo-map,where-is}.md` to current state (schema v1, 5 routes/placeholders, 0 screens) — restores the cold-start snapshot lost in the rebuild reset |
| `TBD` | 2026-06-19 | 1.1.5 | Drift/database baseline: `AppDatabase` (schema v1), `folders.drift` first table + `idx_folders_parent`, `onCreate`/`onUpgrade`/`beforeOpen` migration infra (FK + WAL), native/web platform connection, `AppConstants.localDatabaseName`, `appDatabaseProvider` (keepAlive), schema test; update schema/migration contracts to the rebuild baseline; flip 1.1.5 Specified→Implemented |
| `c159be24` | 2026-06-19 | 1.1.4 | l10n baseline: add `app_vi.arb`, wire `gen-l10n` output into `MaterialApp` (`localizationsDelegates`/`supportedLocales`/`onGenerateTitle`), en/vi `AppLocalizations` tests; flip 1.1.4 Specified→Implemented |
| `b1c08600` | 2026-06-19 | 1.1.3 | Routing baseline: GoRouter shell with top-level destinations (`/home`/`/library`/`/progress`/`/settings`) from navigation-flow, `/`→Library `rootRedirect`, `RouteNames`/`RoutePaths`/`RouteDefaults` constants, `RoutePlaceholder` placeholder discipline; router widget tests; flip 1.1.3 Specified→Implemented |
| `TBD` | 2026-06-19 | 1.1.2 | Theme tokens + design-system baseline: `MxColors`/`MxShadows` ThemeExtensions, `MxSpacing`/`MxRadius`/`MxTypography` token classes, and `MxTheme` light/dark builders derived from `colors_and_type.css` (token-pinned `ColorScheme`, replaces seed-only theme); barrel `theme.dart`; `memox_app` wired to `MxTheme`; theme unit tests; flip 1.1.2 Specified→Implemented |
| `f0b4060f` | 2026-06-19 | 1.1.1 | Complete DI providers: replace manual `ProviderContainer` stub with `@riverpod` codegen wiring (DAO→repository→use case) + `ExampleStubDao`; add DI chain unit test; flip 1.1.1 Specified→Implemented |
| `60e096de` | 2026-06-19 | 1.1.1 | Implement Clean Architecture skeleton with error handling, app bootstrap, theme/router setup, domain/data layer pattern examples, and dependency injection infrastructure |
| `7dd04e0` | 2026-06-19 | 9.18 | Add `tool/prompt_gen/run.mjs`: generates Claude Code prompts from WBS IDs embedding the 6-step dev loop; supports single/batch/phase/list/next modes; registered in `tool/README.md` (§3.6, trigger matrix, portability table) |
| TBD | 2026-06-19 | 1.1.1–1.2.5, §2, §4.1, §5 | Restructure WBS for optimized rebuild workflow: phased delivery strategy in §2, new §4.1 Rebuild Delivery Phases (phase→WBS map), split 1.1.2 into theme + explicit core-widget rows 1.2.1–1.2.5, 1.1.6/1.1.7 corrected to Implemented (tool/+docs/ present), §5 Next Tasks aligned to Phase 0/1/2A |
| *(rebuild start)* | 2026-06-19 | all | Code deleted; all WBS rows reset to Specified; rebuild begins from scratch. |
| TBD | 2026-06-16 | 3.2.3, 4.5.10 | New cards count as NEW not due: stop writing `due_at=now` on create + migration v11 (clear due_at for `review_count=0`); `new_count` read model on folder-detail deck/subfolder rows; folder summary `{n} new`/`{m} due` + Study new / Review due CTAs; deck-row new badge; srs-review/flashcard/contract/wireframe/WBS parity |
| TBD | 2026-06-16 | 4.5.12, 4.1.2 | Study mode-chain persistence backbone: `StudyFlow`/`StudyFlowPlan` + `StudyFlowResolver`, schema v10 (`study_sessions.study_flow`/`current_mode`) + migration/test, persist flow+firstMode at create (new deck/folder → `new_full_cycle`), behavior-preserving; plus study-entry deep-link fixes (`goBackOrLibrary` safe-back, NotFound vs invalid-params copy) |
| TBD | 2026-06-15 | 8.5.1 | Mock-parity (recursive review vs kit shot 21-account-sync--signed-out + business doc): rebuild signed-out as hero MxCard + "What stays local" 3-row MxListTile card + disabled Google sign-in; AppBar "Account & Drive sync"; l10n en/vi reworked; goldens regenerated; wireframe 19 notes canonical kit shot |
| TBD | 2026-06-15 | 8.5.1 | Recursive-review fixes: raw CircleAvatar→MxAvatar (design-system hard rule), assert display-only sign-in button disabled + unsupported-state widget test; goldens regenerated |
| TBD | 2026-06-15 | 8.5.1 | Account Settings V1 (display-only) + cloud-account foundation: CloudAccountLink model/enums + corruption-tolerant store, repository/use case (Result), Riverpod DI + accountSettingsView resolver, AccountSettingsScreen replacing the placeholder (7 states) with Mx components, en/vi l10n; store/entity/resolver/usecase/widget tests + 5-state goldens (light/dark). flutter analyze clean |
| TBD | 2026-06-15 | 8.6.5, 8.6.2 | Record V1 phasing decision: replace-DB blob backup now, conflict-free multi-device sync deferred to a future server-API-with-own-DB (not client-side CRDT/oplog); document the accepted V1 concurrent-divergence non-goal; add Future row 8.6.5 |
| TBD | 2026-06-14 | 8.6.3, 8.6.4, 8.6.1, 8.6.2 | Fix wireframe-19 model-B drift (fingerprint/token-store/12-field manifest); document silent token refresh for the ~1h access-token expiry (SY23) + industry-standard opt-in auto-backup proposal (SY24) across business spec/contracts/wireframe; add Future WBS rows 8.6.3 (silent refresh + reconnect banner) and 8.6.4 (auto-backup) |
| TBD | 2026-06-14 | 8.6.1, 8.6.2 | Reconcile Drive sync repository/use-case contracts with the business spec (model A): DriveSyncRepository + DriveSyncRunResult, composite-string fingerprint, .zip snapshot, drop obsolete flutter_secure_storage token store (google_sign_in 7.x owns tokens), add restore-of-older-schema migration rule (SY22) + V1 no-encryption decision; fix stale 5-field manifest cross-refs. Rows stay Specified |
| TBD | 2026-06-14 | 4.5.9 | Fill mode FE V1: typed-input study shell, strict trim-only evaluator, hint/retry/feedback affordances, and fill-mode tests/docs parity |
| TBD | 2026-06-14 | 4.5.11 | Recall mode FE V1: dedicated timed reveal/self-grade shell, countdown token, edit-pause resume, and result navigation wiring |
| TBD | 2026-06-14 | 7.6.1, 7.6.2, 7.6.3 | Card History activity feed (all 5 kit states): schema v7 (`study_attempts.duration_ms` + `card_events`); created/edited/reset event logging + duration capture in study viewmodel; unified attempt+lifecycle feed (full load, "Beginning of history") replacing cursor pagination; "All events" filter; partial-row + duration-not-logged affordances; resolved attempts-only→feed doc conflict; ARB/tests/docs parity |
| TBD | 2026-06-13 | 7.6.1, 7.6.2, 7.6.3 | Card History mock parity: breadcrumb + Box n/8 header chip + CURRENT PROGRESS card (box stepper + 6-stat grid: Due/Reviews/Recall rate/Lapses/Correct streak/Since added) + timeline rail with status chips/descriptions; single CustomScrollView; Edit pill + Reset/Delete overflow; BE header enriched (event count, correct streak, deck/breadcrumb); duration omitted (no schema column); ARB/tests/docs parity |
| TBD | 2026-06-13 | 7.6.1, 7.6.2, 7.6.3, 1.1.5 | Card History promoted to V1: schema v6 `flashcard_progress.last_reset_at` (+migration/test); BE history query/DAO/repo + header/page/reset use cases + DI; `CardHistoryScreen` (header + cursor-paginated timeline + reset divider, 5 states) on route `/library/deck/:deckId/flashcards/:flashcardId/history` via row-action "View history"; reset keeps cumulative counters; ARB en/vi; widget/repo/migration tests; docs + decision table (H1-H7, N8/N9) + WBS parity |
| TBD | 2026-06-13 | 2.11.2, 2.12.2 | Editor input consistency: drop non-functional TTS speaker from Pronunciation field (all optional inputs share one box style); fix doubled "+" on Add-tag chip (ARB label drops literal "+", Icons.add provides it) |
| TBD | 2026-06-13 | 2.11.2, 2.12.2 | Polish editor field labels to mock 07/08: optional fields get leading icon + uppercase tracked label + muted "optional" suffix; Front/Back + edit "OPTIONAL DETAILS" uppercased via `StringUtils.uppercased` (ARB stays title-case) |
| TBD | 2026-06-13 | 2.11.2 | Flatten create Add-details section: drop the nested bordered card so the toggle pill + expanded example/hint/pronunciation fields match mock 07 details-open (edit mode unchanged) |
| TBD | 2026-06-13 | 2.11.2, 2.12.2, 3.4.2 | Flashcard List/Create/Edit FE mock-first refinement: 06 count/reorder headers + leading handles + reframed empty + delete-card preview; 07 Saving…/Retry-save bottom framing; 08 back-arrow + always-open Optional details + real last-edited·reviews meta strip (recall/History Future); shared confirm-dialog content slot + FlashcardDeletePreview; real data only, no schema change |
| TBD | 2026-06-13 | 3.2.2 | Folder detail FE rebuild: subfolders detail body reworked to mock — search-empty rendered as `MxCard` state, skeleton/secondary-button affordances, typography token wiring; folder_detail_test updated |
| TBD | 2026-06-13 | 3.1.2 | Library overview FE: remove fabricated folder mastery fallback — mastery bar only renders when a real mastery read-model value exists (no synthetic due/total ratio); library_overview_test updated |
| TBD | 2026-06-13 | 3.1.2 | Library overview UI refresh: mock-aligned loading, empty, search-no-results, and error states with localized copy and tighter shell spacing |
| TBD | 2026-06-12 | 4.5.1 | Seal `StudyModeStrategy` into three interaction families (BinaryGrade/TypedAnswer/Board), remove UnsupportedError grading stubs from Fill/Match, full pattern dartdoc, decision rows S48/S49/S58, study-flow hierarchy doc |
| TBD | 2026-06-12 | 5.4.1, 7.7.1 | Dashboard progress summary backend V1: progress summary read model, provider wiring, local-day attempt aggregation, and consistency coverage |
| TBD | 2026-06-12 | 4.5.1, 4.5.2, 4.5.6, 4.5.8, 8.2.1 | Restore study mode strategy wiring and WBS parity for previously implemented Review/Guess/Fill and learning-settings rows |
| TBD | 2026-06-12 | 4.5.4 | Match persistence backend wave: append-only Match evaluation schema/migration, repository API, transactional finalization derivation, factory wiring, tests, and contract/docs updates |
| TBD | 2026-06-12 | 7.4.2, 7.5.1, 7.5.2 | Progress screen V1: overview BE (range activity, study-day streak, card states), kit-mock screen with all 7 states, widget + golden tests, docs reconciled |
| TBD | 2026-06-12 | 9.15 | Enforce single-entry verification after piecemeal relapse: `--quick` inner loop qua cùng entry, pass-marker gắn content-state, hook `--check-marker` chặn commit chưa verify; CLAUDE.md hard rule cấm lệnh verify rời |
| TBD | 2026-06-11 | 9.17, 9.15, 9.11, 1.1.7 | Agent-friendliness pass: `.gitattributes` (hết CRLF noise), pre-commit hook chạy doc_guard, CLAUDE.md 393→263 dòng (gỡ khối bash/verify lỗi thời), implementation-checklist trỏ verify entry, decision-table section index, tool/README thêm trigger matrix + portability guide; checkArb sửa đếm key top-level |
| TBD | 2026-06-11 | 9.16 | Where-is feature index (42 features, live-resolved, doc_guard-linted) + AGENTS.md fast-lookup table + repo-map/where-is into required reading |
| TBD | 2026-06-11 | 9.15 | Unified verify entry (`tool/verify/run.mjs`): scope auto-detection, canonical chain order, single summary; CLAUDE.md points agents at one command |
| TBD | 2026-06-11 | 9.11, 9.12, 9.13, 9.14 | Token-saving toolchain: doc_guard gate (path/symbol/test-ref/WBS/ARB/schema checks + baseline of 57 pre-existing findings), repo-map cold-start generator, golden-diff runner; status headers fixed on account-sync/state-contract/tag-system docs; wired into CLAUDE.md verification chain |
| TBD | 2026-06-11 | 1.1.2, 1.1.7, 9.8 | DOM-spec exporter for low-vision agents: 23 `specs/*.md` (measured element trees + token-resolved styles + per-state deltas) via `export_specs.mjs`; routed in kit README, mock-design-index, CLAUDE.md |
| TBD | 2026-06-11 | 1.1.2, 1.1.7, 9.8 | UI kit agent-consumption path: export 270 per-state mock PNGs (23 screens × 135 states × light/dark) + `shots/INDEX.md` manifest via `tool/ui_kit_shots/`; line-index in kit README; CLAUDE.md + mock-design-index route UI tasks to shots with all-states parity rule |
| TBD | 2026-06-10 | 2.19.1, 2.20.1, 8.3.1, 8.9.1 | Add backend content operations for deck move, manual duplicate soft-warning, tag management, and bulk delete |
| TBD | 2026-06-10 | 3.7.1, 4.11.1, 4.11.2 | Add in-session bury/suspend backend, due-count coverage, and study eligibility regressions |
| TBD | 2026-06-10 | 4.5.10 | Bound daily new quota to the local-day window and keep cancelled new-card sessions consuming quota |
| TBD | 2026-06-10 | 4.2.4, 4.5.10, 4.6.4, 4.10.2 | Add study session batch cap, daily new cap, local-midnight due normalization, and updated_at resumable expiry |
| TBD | 2026-06-10 | 8.7.1 | Guard-compliance cleanup for deck export filename sanitizing helper |
| TBD | 2026-06-10 | 2.19–2.21, 4.1.3, 4.1.4, 4.2.4, 4.4.3, 4.5.10, 4.6.4, 4.10.2, 8.1.2, 8.2.1, 8.7.1, 8.7.2 | Adopt BA-review improvements: resolve C1/M2/M3 (first-attempt SRS, result terms, streak pause), spec recall-default SRS review, session batch limit, daily new limit, due-day normalization, deck move, dup soft-warning, delete blast-radius, export priority, mock-state release rules; reorder next-10 |
| TBD | 2026-06-10 | 8.7.1 | Implement deck export CSV backend V1 with deterministic front/back CSV and safe filename generation |
| TBD | 2026-06-10 | 6.6.1 | Prevent import duplicate commit bypass with repository guard + tests |
| TBD | 2026-06-10 | 6.6.1, 6.9.1 | Import duplicate detection + structured text backend |
| TBD | 2026-06-10 | 2.17.1, 2.18.1 | Flashcard list status + tag backend filters |
| TBD | 2026-06-10 | 7.1.1, 7.2.1, 7.3.1, 7.4.1 | Progress read model backend V1 |
| TBD | 2026-06-10 | 1.1.7, 9.8 | Reconcile 43 docs with source reality: downgrade phantom Implemented claims (bury/suspend, resume banners, TTS, progress screen, Prompt 45/46/47), correct shipped-but-marked-pending schema claims, repoint scope gate to WBS §6, flag open product decisions (SRS demotion, perfect vs initial_passed, streak pause) |
| TBD | 2026-06-10 | 4.6.2 | Verify SRS box transition + interval ladder vs doc contract; table-driven tests S11–S15; fix phantom decision-table test refs |
| TBD | 2026-06-10 | 3.8, 5.2, 5.5, 5.7, 5.8, 5.9, 5.10 | Commit deck import with CSV paste preview and transactional insert flow |
| TBD | 2026-06-09 | 20.6, 18.9, 5.1–5.9, 6.1–6.6 | Update WBS and UI kit references for accuracy and traceability |
| TBD | 2026-06-10 | 6.1–6.6 | Refactor `GlobalSearchScreen` / `SearchAppBarField` query handling |
| TBD | 2026-06-09 | 4.2, 4.3 | `FlashcardEditorDraft` focus management in editor view |
| TBD | 2026-06-09 | 3.8, 5.2, 5.5, 5.7 | Deck import CSV paste + parse + validation preview |
| TBD | 2026-06-09 | 3.8, 5.1 | Deck import route shell |
| TBD | 2026-06-09 | 1.3.3 | Fix folder route registry comment (docs/source) |
| TBD | 2026-06-09 | 9.11 | Align persistence recovery decision table |
| TBD | 2026-06-09 | 9.11, 19.3 | Test session persistence recovery |
| TBD | 2026-06-09 | 13.3 | Simplify continue-studying card v1 |
| TBD | 2026-06-09 | 9.10 | Confirm active-session exit |
| TBD | 2026-06-09 | 9.13, 10.4 | Study mode strategy v1 |
| TBD | 2026-06-09 | 9.6 | Make start-over restart transactional |
| TBD | 2026-06-09 | 9.6 | Resume / start-over choice v1 |
| TBD | 2026-06-09 | 9.9, 10.8 | Study result screen v1 |
| TBD | 2026-06-09 | 11.6 | Study session finalization |
| TBD | 2026-06-09 | 10.4, 11.5 | Study session self grading |
| TBD | 2026-06-09 | 9.8 | Study session card navigation |
| TBD | 2026-06-09 | 13.2 | Dashboard today CTA |
