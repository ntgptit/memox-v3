# Parity Loop — Deferred items

One line per deferred item: `date · screen/node · reason · suggestion`.
Valid reasons: no-FE-yet · needs-token · needs-schema · behavior-conflict · spec-stale ·
verify-fail · review-blocker.

- 2026-06-23 · 03 Library / app-bar search icon · behavior-conflict · kit loaded state has only the sort icon; FE adds a search-toggle that mounts the in-screen folder-search dock (approved redesign, `[[design-redesign-ia-2026-06-21]]`). Keep the affordance; visual gap is behavior-owned.
- 2026-06-23 · 03 Library / LibraryRootAnchor row · behavior-conflict · kit loaded state goes app-bar → card with no anchor row; FE adds the breadcrumb-root anchor (approved redesign). Keep; visual gap is nav-owned.
- 2026-06-23 · 04 Folder detail / move-sheet (folder_move_picker) · behavior-conflict + needs-schema · kit move-sheet = tinted icon-tile + trailing radio + "Move here" button (select-then-confirm); FE = MxListTile tap-to-move, which wireframe `docs/wireframes/25-shared-bottom-sheets.md:490` mandates (radio/"Move here" styling explicitly deferred at :496). Keep tap-to-move behavior; icon-tile also needs folder icon/color in the move-target model. Visual gap is behavior/schema-owned.
- 2026-06-23 · 04 Folder detail / DECKS overline count-suffix shade · low-value · kit splits "DECKS" (text-2) + count (text-3); FE `_Overline` is one color. Needs a RichText/TextSpan split for a 1-shade diff on a single digit; deferred as low-impact.
- 2026-06-23 · shared MxTextField field fill (accent-contrast vs surfaceMuted) · app-wide-coordinated · kit field token is `bg:accent-contrast` (= colors.surface, what MxCard uses; white in light, raised surface in dark) but `mx_text_field.dart` fills with `colors.surfaceMuted` (gray). Real divergence (light exact white vs gray). BUT MxTextField feeds editor + 3 dialogs (deck/folder create, folder rename) + MxSearchField (ALL search docks) + study fill + shared mx_inputs goldens → ~20 goldens. Too wide for a minimal loop step; needs a dedicated WP regenerating every field golden in one pass. (diff.py payoff modest: dark Δ within tolerance; light fields gray→white.) Fix: `fillColor: colors.surface`, regen all field goldens, verify.
- 2026-06-23 · 01 onboarding, 09 history, 10 import, 11 tags, 18 stats, 19 progress, 20-25 settings/account/learning/audio/appearance/language · no-FE-yet · no `lib/presentation/features/**` screen built for these (only dashboard/decks/folders/search/study have FE). Out of visual-loop scope (loop doesn't build greenfield). Confirmed by feature-dir + screen-file inventory 2026-06-23.
- 2026-06-23 · 02 Dashboard / kit-02 engagement layout (streak, accuracy, Continue-Studying, Recent-Decks, resume, onboarding, offline, multi-resume) · behavior-conflict (redesign/scope) · kit 02 is the PRE-REDESIGN engagement dashboard; the FE is the approved redesigned "quiet refer" surface (MxDueSummary + Progress/Library shortcuts) per `[[design-redesign-ia-2026-06-21]]` + screen doc + `docs/design/screens/dashboard.visual-contract.md` (engagement moved to Progress). Source of truth for FE dashboard = visual-contract, not kit 02. Keep redesign; visual gap is scope-owned.
- 2026-06-23 · 12-17 Study modes / per-mode interaction divergences (12 review flip-card, 13 match board, reveal vs swipe etc.) · behavior-conflict · the FE study spine shows both-sides + swipe/board/reveal-grade per `docs/business/study/study-flow.md` + wireframes 13-17; several kit per-mode mocks (e.g. `12-study-review--default` flip card) are EXPLICITLY documented as visual gaps (behaviour follows §Rules per PRECEDENCE #1). Keep FE interactions; mode-specific visuals that differ are behavior-owned. Shared-token visuals already close.
- 2026-06-23 · 17 Study result / goal-off + tough-empty states (+ accuracy ring, goal & streak block, "Due next", "Keep studying" CTA) · needs-schema · `study_result_screen` is a documented V1 slice (hero + Correct/Wrong/Answered + Done). The kit's goal block (goal-off variant), tough-cards/keep-studying (tough-empty), accuracy ring + due-next need the engagement read model + an SRS due-projection the result read model doesn't carry. Future per the screen doc. No distinct render to golden today.
- 2026-06-23 · 07/08 Flashcard editor / validation inline errors · behavior-conflict · kit `validation` state shows inline field-error messages; FE has no inline-error UI — it disables Save (`canSubmit`) when front/back empty. Keep disable-Save validation behavior (precedence rule 2); the "validation" visual ≈ empty form, no separate golden.
- 2026-06-23 · 07/08 Flashcard editor / Details DECK selector · behavior-conflict · kit Details shows a DECK selector row (deck retargeting); FE omits it — deck retargeting is Future (`docs/business/flashcard/flashcard-management.md` §V1, FE comment in flashcard_editor_body). Keep omitted; visual gap is behavior-owned.
- 2026-06-23 · 07/08 Flashcard editor / single "Note" vs 3 fields · behavior/data-owned · kit Details has one "Note" field; FE renders example/pronunciation/hint (business model has 3). Keep 3 fields; visual gap is data-owned.
- 2026-06-23 · 06 Flashcard list / empty-state "Import cards" secondary button · behavior/needs-verification · kit empty has Add card + Import cards; FE has only Add card. No import-into-existing-deck UI entry exists (no runImport, no import route constant, no trigger in lib/presentation). Wiring it needs a new import flow/route — out of visual-loop scope. Suggestion: add the import flow/route (approved task), then add `MxSecondaryButton(outlined)` to the empty CTA.
- 2026-06-23 · 06 Flashcard list / FlashcardTile status chip + per-status tile color (Review/Learning/Mastered) · needs-schema · kit row draws a status chip + status-tinted tile, but the SRS model is New/Due only (`docs/business/srs/srs-review.md`); no Learning/Review/Mastered taxonomy to back it. Documented in `flashcard_tile.dart`. Keep New/Due; visual gap is data-owned.
- 2026-06-23 · MxTextRole missing 13/600 (stat/overline label) + 22/800 (empty-state title, displaySmall) + 11/700 (due chip) roles · needs-token · the type scale (`MxTypography`) + `MxTextRole` enum lack these slots; several kit nodes need them. Suggestion: add the missing roles (displaySmall 22/800 -0.4, a 13/600 label, an 11/700 chip) to MxTypography + MxText, then bump the consumers.
- 2026-06-23 · shared MxBottomSheet modal elevation · needs-token · kit overflow-sheet spec is shadow:8/28 (large airy drop); `mx_bottom_sheet.dart` doesn't pin modal elevation so Flutter's default is smaller. No MemoX modal-elevation token for 8/28. Suggestion: add an `MxShadows` sheet token + pass elevation in showMxBottomSheet (shared — affects all bottom sheets).
- 2026-06-23 · shared MxEmptyState/MxErrorState/MxNoResultsState inner panel (56px tile, solid-accent empty tile, 22/800 title -0.4) · needs-token · no 56 size token + no 22/800/-0.4 title slot. NOTE: the card-wrap/top-anchor half is now solved by the opt-in `MxStateCard` wrapper (used by 03+04); this line is only the INNER-panel typography/tile. Suggestion: add size+title tokens, then bump tile/title in the shared state widgets (affects all consumers — regen all empty/error/no-results goldens).
