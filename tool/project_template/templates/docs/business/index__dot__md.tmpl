---
last_updated: {{DATE}}
status: contract
---

# Business spec map — {{PROJECT_NAME}}

The feature map. Each feature has its own spec under `docs/business/<feature>/`. Start
a new one by copying `docs/business/_feature-template.md`.

## Features

| Feature | Spec | Status |
| --- | --- | --- |
| <!-- FILL: feature name --> | `docs/business/<feature>/<spec>.md` | Specified |

> Status values: Specified · Partial · Implemented · Future · Rejected (see
> `docs/business/system/overview.md` for the canonical status table).

## Reading order for a new contributor

1. `docs/business/glossary.md` — the vocabulary.
2. This map → the feature you are touching.
3. The feature's use-case + repository contracts under `docs/contracts/`.
4. The decision-table rows for that feature.
