/// Parsed study entry route input shared between the route screen and provider.
typedef StudyEntryRouteInput = ({
  String entryType,
  String? entryRefId,
  String? studyTypeQuery,
  String? modeQuery,
});
